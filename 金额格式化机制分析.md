# 金额格式化机制分析

## 一、整体架构概览

本项目的金额格式化机制采用 **分层设计**，从币种规则定义到最终展示经过以下四层协作：

```
┌─────────────────────────────────────────────────────────┐
│                     展示层 (Views)                       │
│  ERB 模板调用 format_money() 或 money.format()           │
│  Stimulus 控制器处理输入交互                             │
├─────────────────────────────────────────────────────────┤
│                   辅助层 (Helpers/Services)              │
│  ApplicationHelper#format_money    CurrenciesService    │
│  CurrenciesController (JSON API)                        │
├─────────────────────────────────────────────────────────┤
│                   核心层 (Money 类)                      │
│  Money 主类  +  Money::Formatting 模块                   │
├─────────────────────────────────────────────────────────┤
│                   规则层 (Currency)                      │
│  Money::Currency  <--  config/currencies.yml             │
└─────────────────────────────────────────────────────────┘
```

## 二、币种规则从哪里来

### 2.1 规则定义文件

所有币种的格式化参数统一配置在 [currencies.yml](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/config/currencies.yml) 中，目前支持 200+ 种币种。每个币种包含以下与格式化直接相关的字段：

| 字段 | 含义 | 示例值 |
|------|------|--------|
| `symbol` | 货币符号 | `"$"`, `"€"`, `"¥"`, `"kr"` |
| `separator` | 小数分隔符 | `"."` 或 `","` |
| `delimiter` | 千分位分隔符 | `","`, `"."`, `" "`(空格) |
| `default_format` | 符号与数字的位置模板 | `"%u%n"` 或 `"%n %u"` |
| `default_precision` | 小数位数 | `0`, `1`, `2`, `3`, `4`, `8` |
| `minor_unit_conversion` | 主辅币换算比 | `1`, `5`, `100`, `1000`, `100000000` |
| `smallest_denomination` | 最小面值 | `1`, `5`, `50`, `100`, `5000` |

### 2.2 币种加载与缓存

[Money::Currency](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/lib/money/currency.rb#L1-L80) 负责加载并封装币种规则：

- **单例缓存**：通过 `@@instances` 类变量缓存已创建的币种实例，避免重复解析 YAML
- **延迟加载**：`all` 方法使用 `@all` 实例变量缓存 YAML 解析结果，首次访问时才读取文件
- **宽松匹配**：`new` 方法接受 `String`、`Symbol` 或 `Money::Currency` 对象，统一转为小写做 key 匹配

```ruby
# 关键代码片段 (currency.rb#L12-L23)
def new(object)
  iso_code = case object
  when String, Symbol
    object.to_s.downcase
  when Money::Currency
    object.iso_code.downcase
  else
    raise ArgumentError, "Invalid argument type"
  end
  @@instances[iso_code] ||= super(iso_code)
end
```

## 三、格式化助手如何决定小数位、符号位置与千分位

核心格式化逻辑位于 [Money::Formatting](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/lib/money/formatting.rb#L1-L43) 模块中，最终委托给 Rails 内建的 `number_to_currency` 方法。

### 3.1 格式化决策流程

`format` 方法的执行流程：

```
format(options = {})
    │
    ├─ 确定 locale (options[:locale] || I18n.locale)
    │
    ├─ format_options(locale) 构建默认参数
    │      │
    │      ├─ 从 Currency 取 5 个基础参数
    │      │    ├─ unit:       get_symbol  (处理 $ 歧义)
    │      │    ├─ precision:  currency.default_precision
    │      │    ├─ delimiter:  currency.delimiter
    │      │    ├─ separator:  currency.separator
    │      │    └─ format:     currency.default_format
    │      │
    │      └─ locale_options(locale) 做语言覆盖
    │
    └─ 将默认参数与用户 options 合并 → 调用 number_to_currency
```

### 3.2 小数位的决定（precision）

小数位主要由 `currency.default_precision` 决定，典型币种差异：

| 币种 | default_precision | 说明 |
|------|-------------------|------|
| USD, EUR, GBP, CNY 等大多数 | 2 | 标准 2 位小数 |
| JPY, KRW, HUF, VND, CLP | 0 | 无小数（辅币面值过小已废弃） |
| MGA, MRU | 1 | 1 位小数（5 进制辅币） |
| OMR, BHD, KWD, JOD, IQD, LYD, TND | 3 | 3 位小数（辅币换算比 1:1000） |
| CLF | 4 | 4 位小数 |
| BTC | 8 | 8 位小数（对应 1 Satoshi） |

用户也可以通过传入 `precision:` 选项覆盖默认值，例如：
```erb
<%= format_money(balance, precision: 0) %>
```

### 3.3 符号位置的决定（format）

符号位置由 `currency.default_format` 决定，支持两种模板：

- **`"%u%n"`**：符号在前，数字在后（无空格），如 `$1,000.00`、`€1.000,00`
  - 适用币种：USD、EUR、GBP、CNY、JPY、BRL、TRY、BRL、MXN、ARS 等
- **`"%n %u"`**：数字在前，空格分隔，符号在后，如 `1.000,12 €`、`100 kr`
  - 适用币种：SEK、NOK、DKK、PLN、CZK、HUF、RUB、RON、BYN、ZAR、VND、UAH 等
- **`"%u %n"`**：符号在前，空格分隔，数字在后，如 `CHF 1,000.00`
  - 适用币种：CHF（瑞士法郎）

模板中的占位符：`%u` = 货币符号（unit），`%n` = 数字（number）。

### 3.4 千分位与小数点的决定（delimiter + separator）

千分位分隔符和小数分隔符是币种文化差异的重要体现：

| 模式 | delimiter (千分位) | separator (小数点) | 典型币种 | 示例 |
|------|--------------------|--------------------|----------|------|
| 英美模式 | `,` | `.` | USD, GBP, CNY, JPY, AUD, CAD, SGD | `1,234,567.89` |
| 欧洲大陆模式 | `.` | `,` | EUR, DE, FR, ES, IT, TRY, BRL, ARS, VND, IDR | `1.234.567,89` |
| 空格千分位 + 逗号小数 | ` `(空格) | `,` | SEK, NOK, DKK, PLN, CZK, HUF, RUB, BYN | `1 234 567,89` |
| 空格千分位 + 点小数 | ` `(空格) | `.` | ZAR | `1 234 567.89` |

### 3.5 $ 符号歧义处理

由于多个国家使用 `$` 作为货币符号，[get_symbol](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/lib/money/formatting.rb#L25-L31) 方法做了特殊处理：

```ruby
def get_symbol
  if currency.symbol == "$" && currency.iso_code != "USD"
    [ currency.iso_code.first(2), currency.symbol ].join
  else
    currency.symbol
  end
end
```

- **USD** → `"$"`
- **AUD** → `"AU$"`（ISO 前 2 位 + $）
- **CAD** → `"CA$"`
- **HKD** → `"HK$"`
- **NZD** → `"NZ$"`
- **SGD** → `"SG$"`

### 3.6 Locale 特定覆盖

除了币种默认规则外，[locale_options](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/lib/money/formatting.rb#L33-L42) 还根据当前 I18n locale 做二次覆盖：

```ruby
def locale_options(locale)
  case [ currency.iso_code, locale.to_sym ]
  when [ "EUR", :nl ], [ "EUR", :pt ]
    { delimiter: ".", separator: ",", format: "%u %n" }
  when [ "EUR", :en ], [ "EUR", :en_IE ]
    { delimiter: ",", separator: "." }
  else
    {}
  end
end
```

**欧元（EUR）的多 Locale 行为差异**：

| 用户 Locale | delimiter | separator | format | 示例输出 |
|-------------|-----------|-----------|--------|----------|
| :nl (荷兰) / :pt (葡萄牙) | `.` | `,` | `"%u %n"` | `€ 1.000,12` |
| :en / :en_IE (爱尔兰) | `,` | `.` | 币种默认 `"%u%n"` | `€1,000.12` |
| 其他 Locale | 币种默认 `.` | 币种默认 `,` | 币种默认 `"%u%n"` | `€1.000,12` |

### 3.7 实际格式化示例（来自测试用例）

[money_test.rb#L87-L91](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/test/lib/money_test.rb#L87-L91) 验证了以下行为：

```ruby
Money.new(1000.899).to_s              # => "$1,000.90"   (USD, 四舍五入到 2 位)
Money.new(1000.12, :eur).to_s         # => "€1,000.12"   (EUR, 默认 locale)
Money.new(1000.12, :eur).format(locale: :nl)  # => "€ 1.000,12"  (EUR, 荷兰 locale)
```

## 四、跨页面复用的规则来源

### 4.1 后端视图层：ApplicationHelper#format_money

跨页面复用时，所有 ERB 视图统一通过 [ApplicationHelper#format_money](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/helpers/application_helper.rb#L65-L69) 作为格式化入口：

```ruby
def format_money(number_or_money, options = {})
  return nil unless number_or_money
  Money.new(number_or_money).format(options)
end
```

这个 helper 是全局可用的（因为 `ApplicationHelper` 会被所有 Controller 自动 include），在 30+ 个视图模板中被调用：

- 仪表板：`_balance_sheet.html.erb`
- 交易：`_transaction.html.erb`、`_header.html.erb`
- 转账：`show.html.erb`
- 持仓：`_holding.html.erb`、`_cash.html.erb`、`show.html.erb`
- 资产概览：`properties/tabs/_overview.html.erb`、`vehicles/tabs/_overview.html.erb`、`loans/tabs/_overview.html.erb`
- 预算：`_budget_donut.html.erb`、`_budget_category_form.html.erb`
- 等等...

调用方式分两类：
1. **简洁调用**（使用币种默认规则）：`<%= format_money account.balance_money %>`
2. **参数覆盖**：`<%= format_money(balance, precision: 0) %>`

此外，Money 对象也可以直接调用 `.format()` 或 `.to_s`，例如在 [_balance_sheet.html.erb](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/views/pages/dashboard/_balance_sheet.html.erb#L14) 中：
```erb
<%= classification_group.total_money.format(precision: 0) %>
```

### 4.2 后端表单层：共享局部模板 _money_field.html.erb

所有需要「金额输入 + 币种选择」的表单统一复用 [_money_field.html.erb](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/views/shared/_money_field.html.erb) 局部模板。

模板从以下路径获取格式化规则：
1. **优先取 `currency_value_override`**（外部传入覆盖值）
2. **其次取 form 对象上的币种属性**（`form.object.send(currency_method)`）
3. **最后回退到 `default_currency` 或 "USD"**

```erb
<% currency_value = if options[:currency_value_override].present?
                     options[:currency_value_override]
                   elsif form.object && form.object.respond_to?(currency_method)
                     form.object.public_send(currency_method)
                   end
   currency = Money::Currency.new(currency_value || options[:default_currency] || "USD") %>
```

模板中直接使用 `currency` 对象的属性驱动 HTML：
- `currency.symbol` → 展示在输入框左侧
- `currency.default_precision` → 通过 `sprintf("%.#{precision}f", val)` 格式化初始值
- `currency.step`（即 `1 / 10^precision`）→ 设置 `<input type="number">` 的 `step` 属性

### 4.3 前端交互层：CurrenciesService + Stimulus 控制器

当用户在表单中切换币种时，前端通过 [CurrenciesService](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/javascript/services/currencies_service.js) 从后端 API 获取该币种的格式化规则：

```javascript
// currencies_service.js
export class CurrenciesService {
  get(id) {
    return fetch(`/currencies/${id}.json`).then((response) => response.json());
  }
}
```

后端对应的 [CurrenciesController#show](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/controllers/currencies_controller.rb#L1-L6) 返回币种的 JSON 表示（包含 `step` 字段）：

```ruby
def show
  currency = Money::Currency.all_instances.find { |currency| currency.iso_code == params[:id] }
  render json: currency.as_json.merge({ step: currency.step })
end
```

[money_field_controller.js](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/javascript/controllers/money_field_controller.js) 接收数据后更新三个 DOM 属性：

```javascript
updateAmount(currency) {
  new CurrenciesService().get(currency).then((currency) => {
    this.amountTarget.step = currency.step;
    if (Number.isFinite(this.amountTarget.value)) {
      this.amountTarget.value = Number.parseFloat(
        this.amountTarget.value,
      ).toFixed(currency.default_precision);
    }
    this.symbolTarget.innerText = currency.symbol;
  });
}
```

| 属性 | 更新方式 | 用途 |
|------|----------|------|
| `input.step` | `currency.step` | 浏览器数字步进器的增量（如 0.01） |
| `input.value` | `toFixed(currency.default_precision)` | 将现有值按新币种小数位重新格式化 |
| `symbol.innerText` | `currency.symbol` | 更新左侧显示的货币符号 |

### 4.4 前端规则来源总结

```
用户切换币种下拉框
        │
        ▼
money_field_controller.handleCurrencyChange()
        │
        ▼
CurrenciesService.get(currencyCode)
        │  GET /currencies/EUR.json
        ▼
CurrenciesController#show
        │  Money::Currency.find → as_json + step
        ▼
JSON: { symbol, default_precision, step, separator, delimiter, ... }
        │
        ▼
前端更新:  step / value.toFixed(precision) / 符号显示
```

## 五、关键文件索引

| 角色 | 文件路径 |
|------|----------|
| 币种规则配置 | [currencies.yml](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/config/currencies.yml) |
| 币种对象封装 | [currency.rb](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/lib/money/currency.rb) |
| Money 主类 | [money.rb](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/lib/money.rb) |
| 格式化核心逻辑 | [formatting.rb](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/lib/money/formatting.rb) |
| 金额运算逻辑 | [arithmetic.rb](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/lib/money/arithmetic.rb) |
| 视图 Helper 入口 | [application_helper.rb#L65-L69](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/helpers/application_helper.rb#L65-L69) |
| 表单共享模板 | [_money_field.html.erb](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/views/shared/_money_field.html.erb) |
| 币种 API 控制器 | [currencies_controller.rb](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/controllers/currencies_controller.rb) |
| 前端币种服务 | [currencies_service.js](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/javascript/services/currencies_service.js) |
| 前端 Stimulus 控制器 | [money_field_controller.js](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/javascript/controllers/money_field_controller.js) |
| Currency 测试 | [currency_test.rb](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/test/lib/money/currency_test.rb) |
| Money + Formatting 测试 | [money_test.rb](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/test/lib/money_test.rb) |

## 六、账户余额与持仓金额的上游数据库列来源

所有 `*_money` 方法并非数据库列，而是由 [Monetizable](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/models/concerns/monetizable.rb#L1-L22) concern 通过 `monetize` 宏动态生成。其底层依赖两个真实的数据库列：**数值列**（decimal 类型） + **币种列**（string 类型）。

### 6.1 Monetizable 宏机制

```ruby
# monetizable.rb#L5-L14
def monetize(*fields)
  fields.each do |field|
    define_method("#{field}_money") do |**args|
      value = self.send(field, **args)
      return nil if value.nil? || monetizable_currency.nil?
      Money.new(value, monetizable_currency)
    end
  end
end

# monetizable.rb#L19-L21
private
  def monetizable_currency
    currency
  end
```

生成规则：
- `monetize :balance` → 生成 `balance_money()` → `Money.new(self.balance, self.currency)`
- `monetize :amount` → 生成 `amount_money()` → `Money.new(self.amount, self.currency)`
- `monetize :price` → 生成 `price_money()` → `Money.new(self.price, self.currency)`

所有模型共用同一约定：数值列名 = `Money.new` 的第一个参数，币种列名固定为 `self.currency`。

### 6.2 Account.balance_money 的数据来源

对应模型：[Account](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/models/account.rb#L17-L17)

```ruby
monetize :balance, :cash_balance
```

**数据库表 `accounts`（schema.rb#L22-L48）：**

| 列名 | 类型 | 说明 |
|------|------|------|
| `balance` | `decimal(19,4)` | 账户总余额（主数值源） |
| `cash_balance` | `decimal(19,4)` | 现金部分余额（默认 0.0） |
| `currency` | `string` | 币种 ISO 代码，如 "USD"、"EUR" |

上游写入链路：
1. 创建账户时 `Account.create_and_sync`（account.rb#L58-L73）将初始 `attributes[:balance]` 同时写入 `balance` 和 `cash_balance`
2. 日常通过交易分录、估值、交易等产生 `entries`，再由 `Account::CurrentBalanceManager` 等同步器回写 `accounts.balance`
3. 前端展示：`account.balance_money` → `Money.new(accounts.balance, accounts.currency)`

### 6.3 Holding.amount_money 的数据来源

对应模型：[Holding](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/models/holding.rb#L1-L10)

```ruby
monetize :amount
```

**数据库表 `holdings`（schema.rb#L281-L294）：**

| 列名 | 类型 | 说明 |
|------|------|------|
| `amount` | `decimal(19,4), NOT NULL` | 持仓市值（= qty × price） |
| `qty` | `decimal(19,4), NOT NULL` | 持仓数量 |
| `price` | `decimal(19,4), NOT NULL` | 单价 |
| `currency` | `string, NOT NULL` | 币种 ISO 代码 |
| `date` | `date, NOT NULL` | 持仓快照日期 |

上游写入链路：
1. 导入证券交易或从经纪商同步时，按 `(account_id, security_id, date, currency)` 唯一键写入或更新
2. `amount` 由 `qty × price` 计算得到，随每日行情刷新
3. 前端展示：`holding.amount_money` → `Money.new(holdings.amount, holdings.currency)`

### 6.4 其他典型 monetize 字段一览

| 模型 | monetize 声明 | 数值列 | 币种列 |
|------|--------------|--------|--------|
| [Entry](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/models/entry.rb#L4-L4) | `monetize :amount` | `entries.amount` (decimal 19,4) | `entries.currency` |
| [Trade](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/models/trade.rb#L4-L4) | `monetize :price` | `trades.price` (decimal 19,4) | `trades.currency` |
| [Balance](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/models/balance.rb#L9-L13) | `monetize :balance, :cash_balance, :start_cash_balance, ...` | 多个 decimal(19,4) 列 | `balances.currency` (默认 "USD") |
| [Security](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/models/security.rb#L14-L18) | 手动实现 `current_price` | `security_prices.price` (decimal 19,4) | `security_prices.currency` |

> 注：`Security.current_price` 是手动实现而非 monetize 宏生成，它从关联的 `security_prices` 表取最新一条记录的 price + currency 构造 Money 对象。

## 七、format_options / locale_options / options 三者合并优先级

格式化参数的合并发生在 [Money::Formatting#format](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/lib/money/formatting.rb#L4-L9) 和 [#format_options](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/lib/money/formatting.rb#L12-L22) 中，通过两次 `Hash#merge` 实现 **从左到右优先级递增** 的覆盖规则。

### 7.1 源码级合并流程

```ruby
# formatting.rb#L4-L9
def format(options = {})
  locale = options[:locale] || I18n.locale
  default_opts = format_options(locale)
  number_to_currency(amount, default_opts.merge(options))
end

# formatting.rb#L12-L22
def format_options(locale = nil)
  local_option_overrides = locale_options(locale)
  {
    unit: get_symbol,
    precision: currency.default_precision,
    delimiter: currency.delimiter,
    separator: currency.separator,
    format: currency.default_format
  }.merge(local_option_overrides)
end
```

### 7.2 优先级图解

```
┌────────────────────────────────────────────────────────────┐
│  优先级最低（被覆盖）                                       │
│                                                            │
│  ① currencies.yml 默认值（5 项）                            │
│     { unit, precision, delimiter, separator, format }       │
│                                                            │
│         ↓ .merge(local_option_overrides)                   │
│                                                            │
│  ② locale_options(locale) 覆盖                              │
│     例如 EUR + :nl → { delimiter: ".", separator: ",",     │
│                        format: "%u %n" }                    │
│                                                            │
│         ↓ .merge(options)                                  │
│                                                            │
│  ③ 调用方传入 options（优先级最高）                          │
│     例如 format_money(m, precision: 0, locale: :en)         │
│                                                            │
│  优先级最高（覆盖一切）                                     │
└────────────────────────────────────────────────────────────┘
```

### 7.3 实际案例验证

以 EUR（欧元）在不同调用方式下的参数对比：

| 参数 | ① YAML 默认 | ② locale: :nl 覆盖 | ③ options: { precision: 0 } 覆盖 | 最终生效值 |
|------|------------|-------------------|----------------------------------|-----------|
| unit | `"€"` | — (未覆盖) | — (未覆盖) | `"€"` |
| precision | `2` | — (未覆盖) | `0` | `0` |
| delimiter | `"."` | `","` (被 ② 改) | — | `","` |
| separator | `","` | `"."` (被 ② 改) | — | `"."` |
| format | `"%u%n"` | `"%u %n"` (被 ② 改) | — | `"%u %n"` |

输出结果示例：
```ruby
Money.new(1000, :eur).format(locale: :nl, precision: 0)
# → 过程: {YAML 默认}.merge({locale: nl 覆盖}).merge({precision: 0})
# → 结果: "€ 1,000" （无小数，逗号千分位，符号与数字间有空格）
```

## 八、Currency.step 由 default_precision 推算的源码

`step` 方法用于 HTML `<input type="number">` 的 `step` 属性，表示数字输入框每次点击上下箭头时的增减步长，同时也用于浏览器原生的数值合法性校验。

### 8.1 源码实现

位于 [Money::Currency#step](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/lib/money/currency.rb#L72-L74)：

```ruby
def step
  (1.0/10**default_precision)
end
```

推算公式：`step = 10^(-default_precision)`，即「该币种最小可表示单位的 1 个单位」。

### 8.2 各币种 step 值一览

| 币种 | default_precision | 公式 | step 值 |
|------|-------------------|------|---------|
| USD, EUR, CNY 等大多数 | 2 | `1.0 / 10^2` | `0.01` |
| JPY, KRW, VND, CLP | 0 | `1.0 / 10^0` | `1.0` |
| MGA, MRU | 1 | `1.0 / 10^1` | `0.1` |
| OMR, BHD, KWD (中东 3 位小数) | 3 | `1.0 / 10^3` | `0.001` |
| CLF (4 位小数) | 4 | `1.0 / 10^4` | `0.0001` |
| BTC (比特币) | 8 | `1.0 / 10^8` | `0.00000001` |

### 8.3 step 在前后端的使用

- **后端渲染初始值**（[_money_field.html.erb#L58](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/views/shared/_money_field.html.erb#L58)）：
  ```erb
  step: currency.step
  ```
  → 写入 `<input type="number" step="0.01">`

- **前端切换币种后刷新**（[money_field_controller.js#L16](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/javascript/controllers/money_field_controller.js#L16)）：
  ```javascript
  this.amountTarget.step = currency.step;
  ```

- **后端 API 提供 step 字段**（[currencies_controller.rb#L4](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/controllers/currencies_controller.rb#L4)）：
  ```ruby
  render json: currency.as_json.merge({ step: currency.step })
  ```
  由于 `as_json` 默认只输出实例变量，`step` 作为动态方法需要手动合并进 JSON。

## 九、Stimulus 刷新 DOM 的完整事件链

从用户操作到 DOM 更新的完整链路涉及 **Rails 表单构建器 → ERB 模板 HTML → Stimulus 控制器 → 后端 JSON API** 四层协作。

### 9.1 完整时序图

```
用户操作                          前端 JS                     后端 Rails
   │                                 │                            │
   │  选择下拉框 (EUR→JPY)           │                            │
   ├────────────────────────────────▶│                            │
   │                                 │                            │
   │                                 │ handleCurrencyChange(e)    │
   │                                 │   selectedCurrency = "JPY" │
   │                                 │   updateAmount("JPY")      │
   │                                 │                            │
   │                                 │  GET /currencies/JPY.json  │
   │                                 ├───────────────────────────▶│
   │                                 │                            │
   │                                 │                            │ CurrenciesController#show
   │                                 │                            │   Money::Currency.find("JPY")
   │                                 │                            │   → as_json + step
   │                                 │                            │
   │                                 │      200 OK JSON           │
   │                                 │◀───────────────────────────┤
   │                                 │ {                         │
   │                                 │   symbol: "¥",             │
   │                                 │   default_precision: 0,    │
   │                                 │   step: 1.0,               │
   │                                 │   separator: ".",          │
   │                                 │   delimiter: ",",          │
   │                                 │   ...                      │
   │                                 │ }                         │
   │                                 │                            │
   │                                 │ 更新 3 处 DOM:             │
   │                                 │  ┌─ input.step = 1.0       │
   │                                 │  ├─ input.value =          │
   │                                 │  │    toFixed(0)  (无小数)  │
   │                                 │  └─ symbol.innerText = "¥" │
   │                                 │                            │
   ▼                                 ▼                            ▼
  用户看到:
  ┌─────────────────────────────┐
  │  ¥ [ 1000 ]  [ JPY ▼ ]       │   ← 符号变 ¥，值无小数
  └─────────────────────────────┘
```

### 9.2 事件绑定入口：Stimulus data-action / data-target

HTML 结构由 [_money_field.html.erb](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/views/shared/_money_field.html.erb#L10-L84) 渲染，关键 data 属性：

```erb
<div class="form-field ..." data-controller="money-field">         <!-- ① Stimulus 挂载点 -->
  ...
  <span class="text-subdued text-sm"
        data-money-field-target="symbol">                          <!-- ② 符号 DOM 目标 -->
    <%= currency.symbol %>
  </span>

  <%= form.number_field amount_method,
      data: { "money-field-target": "amount" },                    <!-- ③ 数值输入框目标 -->
      step: currency.step, ... %>

  <%= form.select currency_method,
      data: {
        "money-field-target": "currency",                          <!-- ④ 币种下拉框目标 -->
        "action": "change->money-field#handleCurrencyChange"       <!-- ⑤ change 事件绑定 -->
      } %>
</div>
```

对应的 Stimulus 控制器 [money_field_controller.js](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/javascript/controllers/money_field_controller.js#L1-L27)：

```javascript
export default class extends Controller {
  static targets = ["amount", "currency", "symbol"];   // 对应 ②③④ 的 target 名

  handleCurrencyChange(e) {                             // 对应 ⑤ 的 action 方法名
    const selectedCurrency = e.target.value;            // 读取 <select> 的新值，如 "JPY"
    this.updateAmount(selectedCurrency);
  }

  updateAmount(currency) {
    new CurrenciesService().get(currency).then((currency) => {
      // DOM 更新 1: 数值步进
      this.amountTarget.step = currency.step;
      // DOM 更新 2: 现有值按新精度重格式化
      if (Number.isFinite(this.amountTarget.value)) {
        this.amountTarget.value = Number.parseFloat(
          this.amountTarget.value,
        ).toFixed(currency.default_precision);
      }
      // DOM 更新 3: 货币符号文字
      this.symbolTarget.innerText = currency.symbol;
    });
  }
}
```

### 9.3 触发入口：StyledFormBuilder#money_field

用户代码中并不直接写 `data-controller="money-field"`，而是通过表单 Builder 统一封装。在 15+ 个视图中调用方式如下：

```erb
<!-- 例如 accounts/_form.html.erb -->
<%= form.money_field :balance, label: t(".balance"), required: true, default_currency: Current.family.currency %>
```

[StyledFormBuilder#money_field](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/helpers/styled_form_builder.rb#L38-L45) 负责将调用转发到共享模板：

```ruby
def money_field(amount_method, options = {})
  @template.render partial: "shared/money_field", locals: {
    form: self,
    amount_method:,
    currency_method: options[:currency_method] || :currency,
    **options
  }
end
```

典型调用方：
- `accounts/_form.html.erb` — 账户余额输入
- `transactions/_form.html.erb`、`transactions/show.html.erb` — 交易金额
- `trades/_form.html.erb`、`trades/show.html.erb` — 交易价格与金额
- `budgets/edit.html.erb` — 预算金额（`disable_currency: true` 隐藏币种选择）
- `valuations/new.html.erb`、`valuations/show.html.erb` — 估值金额
- `loans/_form.html.erb` — 贷款初始余额
- `credit_cards/_form.html.erb` — 信用卡最低还款额
- `properties/balances.html.erb` — 房产估值

### 9.4 DOM 更新三处细节剖析

| DOM 操作 | 源码行 | 目的 | 典型变化 |
|----------|--------|------|----------|
| `this.amountTarget.step = currency.step` | L16 | 更新 `<input type="number">` 的步长与浏览器校验精度 | EUR (0.01) → JPY (1) 后，步进器每次 ±1 |
| `.toFixed(currency.default_precision)` | L19-L21 | 已有数值按新币种精度截断/补零 | EUR 输入 "1000.50" 切换 JPY 后变 "1001"（四舍五入到整数） |
| `this.symbolTarget.innerText = currency.symbol` | L24 | 刷新输入框左侧的可见货币符号 | `"€"` → `"¥"` |

## 十、设计特点总结

1. **规则集中管理**：所有币种参数统一在 `currencies.yml`，避免散落各处的硬编码
2. **三层可覆盖**：YAML 默认值 → Locale 覆盖 → 调用方 `options` 覆盖，灵活性逐层递增（Hash#merge 右优）
3. **前后端同源**：前端格式化所需的 `default_precision`、`symbol`、`step` 等参数与后端来自同一 `Money::Currency` 对象，通过 JSON API 同步，保证一致性
4. **跨页面复用**：`format_money` Helper + `_money_field` 模板 + `StyledFormBuilder#money_field` 形成了从展示到输入的完整复用链路
5. **$ 歧义处理**：自动为非美元的 $ 类币种添加 ISO 前缀（AU$、CA$、HK$ 等），避免符号混淆
6. **Monetizable 宏约定**：`monetize :xxx` 统一生成 `xxx_money` 方法，约定数值列 + `currency` 列的两列存储模式，在 Account/Holding/Entry/Trade/Balance 等 8+ 模型中复用
7. **Currency.step 自动推算**：由 `1.0 / 10^default_precision` 统一派生，保证后端 `sprintf` 格式化精度、HTML `<input step>` 校验、前端 `toFixed()` 三者精度一致

## 十一、money_field 控制器中 Number.isFinite(input.value) 分支命中条件辨析

该判断位于 [money_field_controller.js#L18-L22](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/javascript/controllers/money_field_controller.js#L18-L22)：

```javascript
if (Number.isFinite(this.amountTarget.value)) {
  this.amountTarget.value = Number.parseFloat(
    this.amountTarget.value,
  ).toFixed(currency.default_precision);
}
```

### 11.1 这个分支实际上**永远不会命中**

这是一个隐藏的 JavaScript 类型陷阱。原因在于：

| 表达式 | 返回值 | 原因 |
|--------|--------|------|
| `this.amountTarget.value` | `"1000.50"`（字符串） | DOM `<input>` 的 `value` 属性**永远返回字符串**，即使内容是数字 |
| `Number.isFinite("1000.50")` | `false` | `Number.isFinite` 对**非 Number 类型**直接返回 `false`，不做隐式转换 |
| `Number.isFinite("")` | `false` | 空字符串不是数字 |
| `Number.isFinite(Number("1000.50"))` | `true` | 显式转为 Number 后才返回 true |

`Number.isFinite` 与全局 `isFinite()` 函数行为不同：
```javascript
isFinite("1000.50")           // true  (全局函数会隐式转 Number)
Number.isFinite("1000.50")    // false (ES6 Number.isFinite 不做类型转换)
```

### 11.2 实际行为

由于条件恒为 `false`，用户切换币种下拉框时：
- ✅ `input.step` 会被正确更新（如 EUR 0.01 → JPY 1）
- ✅ 货币符号会被正确更新（如 "€" → "¥"）
- ❌ **输入框内已有的数值不会按新币种精度重格式化**——用户输入的 "1000.50" 仍原样保留，不会变成 JPY 的 "1001"

### 11.3 修正建议

将判断修改为显式转换后再校验：

```javascript
const numericValue = Number.parseFloat(this.amountTarget.value);
if (Number.isFinite(numericValue)) {
  this.amountTarget.value = numericValue.toFixed(currency.default_precision);
}
```

## 十二、Monetizable.monetize 收 `as` 关键字的处理与「碰巧运行」机制

### 12.1 实际源码不识别 `as`

[Monetizable#monetize](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/models/concerns/monetizable.rb#L5-L15) 的方法签名只接受位置参数：

```ruby
def monetize(*fields)
  fields.each do |field|
    define_method("#{field}_money") do |**args|
      value = self.send(field, **args)
      return nil if value.nil? || monetizable_currency.nil?
      Money.new(value, monetizable_currency)
    end
  end
end
```

没有 `**options` 或 `as:` 关键字参数声明。

### 12.2 `monetize :total, as: :total_money` 调用时发生了什么

Ruby 中调用 `monetize :total, as: :total_money` 时，由于方法只声明了 `*fields`：
- 关键字参数 `as: :total_money` 会被 Ruby 自动收集进 `fields` 数组的**最后一个元素**，变成一个 Hash
- 实际的 `fields` 值为：`[:total, {:as=>:total_money}]`

遍历逻辑：
1. 第一个元素 `:total` → `define_method("total_money")`，**正确**生成了所需方法
2. 第二个元素 `{:as=>:total_money}` → `define_method("{:as=>:total_money}_money")`，**生成了一个垃圾方法名**，但由于无人调用，不会报错

### 12.3 为什么 ClassificationGroup 能跑通

[BalanceSheet::ClassificationGroup](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/models/balance_sheet/classification_group.rb#L4-L4) 调用：

```ruby
monetize :total, as: :total_money
```

能够正常运行完全是**巧合**——因为默认生成的方法名 `total_money`（由第一个参数 `:total` + `"_money"` 拼接）恰好就是 `as: :total_money` 想要指定的名字。`as` 参数虽被吞掉（变成了无用的 Hash 遍历元素），但结果刚好正确。

同样的巧合也发生在 [BalanceSheet::AccountGroup](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/models/balance_sheet/account_group.rb#L4-L4)。

### 12.4 风险提示

如果有人写 `monetize :balance, as: :money_balance`（方法名与默认不一致），就会只生成 `balance_money` 而不存在 `money_balance`，调用时报 `NoMethodError`。建议在 monetize 中显式支持 `as` 关键字，或移除冗余的 `as` 参数。

## 十三、ClassificationGroup 完整跑通链路剖析

### 13.1 数据构造

[BalanceSheet::ClassificationGroup](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/models/balance_sheet/classification_group.rb#L1-L61) 是一个 **Plain Old Ruby Object (PORO)**，不继承 ActiveRecord，所有数据由构造函数注入：

```ruby
def initialize(classification:, currency:, accounts:)
  @classification = normalize_classification!(classification)
  @currency = currency          # ← 由外部传入，来自 Family#currency
  @accounts = accounts          # ← 已做币种换算后的账户数组
end
```

### 13.2 monetizable_currency 的来源

Monetizable 默认实现是 `def monetizable_currency; currency; end`。ClassificationGroup 通过 `attr_reader :currency`（L6）暴露了 `@currency` 实例变量，所以 `monetizable_currency` 直接返回构造时注入的币种（通常为 `Current.family.currency`，即家庭记账本位币）。

### 13.3 total_money 的调用链

```
视图调用 classification_group.total_money.format(precision: 0)
        │
        ├─ total_money()                    # monetize 宏生成的方法
        │      │
        │      ├─ self.send(:total)         # 动态调用 total 实例方法
        │      │     │
        │      │     └─ accounts.sum(&:converted_balance)
        │      │            # L23-L25: 所有账户的 converted_balance 求和，返回 BigDecimal
        │      │
        │      ├─ monetizable_currency       # 返回 @currency（如 "USD"）
        │      │
        │      └─ Money.new(sum_result, "USD")
        │
        └─ .format(precision: 0)             # 进入 Formatting 模块，precision 被用户选项覆盖为 0
```

关键在于 `Account#converted_balance` 已将各账户的原币种金额**统一换算为分类组的本位币**，所以求和后直接用分类组的 `currency` 构造 Money 即可，无需二次换算。

## 十四、Category 与 IncomeStatement 重写 monetizable_currency 的去向

Monetizable 中 `monetizable_currency` 是 private 方法，默认实现为 `def monetizable_currency; currency; end`。两个模型对其做了重写，但命运截然不同：

### 14.1 IncomeStatement：重写**生效**，被 monetize 方法调用

[IncomeStatement](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/models/income_statement.rb#L1-L126)：
- L4：`monetize :median_expense, :median_income` — **确实调用了 monetize**
- L123-L125：
  ```ruby
  def monetizable_currency
    family.currency
  end
  ```

生效路径：
```
median_expense_money()
  ├─ self.send(:median_expense, **args)        # 返回 BigDecimal（如 1500.0）
  ├─ monetizable_currency                       # 走重写逻辑 → family.currency → "USD"
  └─ Money.new(1500.0, "USD")
```

IncomeStatement 是 PORO，没有 `self.currency` 属性/列，必须重写才能让 monetize 正常工作。同时还支持 `category:` 参数向下透传给 `median_expense(interval:, category:)`，由 monetize 生成方法时的 `**args` 转发。

### 14.2 Category：重写**不生效**，属于残留代码

[Category](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/models/category.rb#L126-L128)：
- L126-L128：同样定义了 `def monetizable_currency; family.currency; end`
- **但 Category 模型中从未调用过 `monetize`**，没有任何 `*_money` 方法被生成

这个重写是**死代码**。可能的来源：
1. 早期规划有 `category.budgeted_amount_money` 等功能，后被移除但残留了这个 private 方法
2. Category 作为 `has_many :transactions` / `has_many :budget_categories` 的关联方，币种被 Transaction、BudgetCategory 等持有，Category 自身不需要金额字段

## 十五、Money.new 接 Money 实例时第二个参数被忽略的语义

### 15.1 源码实现

[Money#initialize](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/lib/money.rb#L33-L40)：

```ruby
def initialize(obj, currency = Money.default_currency, store: ExchangeRate)
  @source = obj
  @amount = obj.is_a?(Money) ? obj.amount : BigDecimal(obj.to_s)
  @currency = obj.is_a?(Money) ? obj.currency : Money::Currency.new(currency)
  @store = store
  validate!
end
```

当 `obj.is_a?(Money)` 为 `true` 时：
- `@amount` 从 `obj.amount` 取，忽略 `currency` 参数
- `@currency` 从 `obj.currency` 取，**忽略第二个参数**

### 15.2 语义：这是「克隆构造」，不是「币种转换」

```ruby
usd = Money.new(1000, :usd)     # → $1,000.00
eur = Money.new(usd, :eur)      # → $1,000.00（仍为 USD！第二个参数被忽略）
```

用户传 `:eur` 的意图通常是「把 1000 美元换算成欧元」，但 Money.new 不会做换算，只会克隆。正确做法是调用 `exchange_to`：

```ruby
usd.exchange_to(:eur)           # → 走汇率查找逻辑，返回新的 Money 对象（币种为 EUR）
```

### 15.3 设计意图与潜在误用

**设计意图**：`Money.new(money_instance)` 作为 `dup`/`clone` 的替代，用于：
- 传入同一个 Money 对象给不同上下文时创建独立副本
- 测试场景中快速构造相同值的 Money
- Monetizable concern 中 `Money.new(value, monetizable_currency)` 调用时，若 value 恰好是 Money 对象也不会出错（保持幂等）

**潜在误用风险**：代码库中若有人写 `Money.new(account.balance_money, :eur)` 期待得到换算后的欧元金额，实际得到的仍是原币种，可能导致报表错误。建议在 `initialize` 中当 obj 为 Money 且 currency 参数被显式传入（非默认值）时发出 warning 或直接报错。

## 十六、Currency.as_json 默认输出字段集合

### 16.1 Currency 没有自定义 as_json

[Money::Currency](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/lib/money/currency.rb#L1-L80) 类中**没有定义 `as_json` 方法**。它是普通 Ruby 对象（不继承 ActiveRecord::Base），所以使用 Rails 提供的默认 `Object#as_json` 实现。

Rails 对普通 Ruby 对象的默认序列化逻辑是：**调用 `instance_values` 获取所有实例变量名和值的 Hash**。

### 16.2 由 attr_reader 反推的字段集合

由 [currency.rb#L49-L69](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/lib/money/currency.rb#L49-L69) 的 `attr_reader` 和 `initialize` 中赋值的 `@` 变量，可确定完整的默认输出字段：

| 字段 | 来源 | 示例值 (USD) |
|------|------|--------------|
| `name` | `@name` | `"United States Dollar"` |
| `priority` | `@priority` | `1` |
| `iso_code` | `@iso_code` | `"USD"` |
| `iso_numeric` | `@iso_numeric` | `"840"` |
| `html_code` | `@html_code` | `"&#36;"` |
| `symbol` | `@symbol` | `"$"` |
| `minor_unit` | `@minor_unit` | `2` |
| `minor_unit_conversion` | `@minor_unit_conversion` | `100` |
| `smallest_denomination` | `@smallest_denomination` | `1` |
| `separator` | `@separator` | `"."` |
| `delimiter` | `@delimiter` | `","` |
| `default_format` | `@default_format` | `"%u%n"` |
| `default_precision` | `@default_precision` | `2` |

共 **13 个字段**。

### 16.3 CurrenciesController 对 JSON 的二次加工

[CurrenciesController#show](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/app/controllers/currencies_controller.rb#L1-L6) 中：

```ruby
render json: currency.as_json.merge({ step: currency.step })
```

由于 `step` 是**动态方法**（不是实例变量），默认 `as_json` 不会包含它，所以需要手动 `merge` 进去。最终返回给前端的 JSON 共 **14 个字段**（13 个实例变量 + 1 个 step）。

### 16.4 与 Money#as_json 的对比

作为参照，[Money#as_json](file:///d:/fz/0601-1/solo-dogfeeding/code/62-maybe/lib/money.rb#L57-L59) **自定义了输出**，只返回 3 个字段，避免把整个 Currency 对象序列化进去：

```ruby
def as_json
  { amount: amount, currency: currency.iso_code, formatted: format }.as_json
end
```

```json
{ "amount": 1000.0, "currency": "USD", "formatted": "$1,000.00" }
```
