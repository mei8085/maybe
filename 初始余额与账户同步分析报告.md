# 初始余额与账户同步分析报告

## 1. 初始余额在余额模型中的存储结构

### 1.1 核心数据模型

#### Account 表（账户主表）

| 字段 | 类型 | 说明 |
|------|------|------|
| `balance` | decimal(19,4) | 账户总余额（缓存/派生值） |
| `cash_balance` | decimal(19,4) | 现金余额 |
| `currency` | string | 货币类型 |
| `accountable_type` | string | 账户类型（Depository/CreditCard/Investment/Crypto等） |

#### Balance 表（每日余额快照）

| 字段 | 类型 | 说明 |
|------|------|------|
| `account_id` | uuid | 关联账户 |
| `date` | date | 余额日期 |
| `balance` | decimal(19,4) | 当日总余额 |
| `cash_balance` | decimal(19,4) | 当日现金余额 |
| `start_cash_balance` | decimal(19,4) | 日初现金余额 |
| `start_non_cash_balance` | decimal(19,4) | 日初非现金余额 |
| `cash_inflows` | decimal(19,4) | 现金流入 |
| `cash_outflows` | decimal(19,4) | 现金流出 |
| `non_cash_inflows` | decimal(19,4) | 非现金流入（如买入证券） |
| `non_cash_outflows` | decimal(19,4) | 非现金流出（如卖出证券） |
| `net_market_flows` | decimal(19,4) | 市场价值变动 |
| `cash_adjustments` | decimal(19,4) | 现金调整项 |
| `non_cash_adjustments` | decimal(19,4) | 非现金调整项 |
| `flows_factor` | integer | 流量方向因子（资产=1，负债=-1） |

**虚拟计算字段（存储生成）：**
- `start_balance` = `start_cash_balance + start_non_cash_balance`
- `end_cash_balance` = `start_cash_balance + (cash_inflows - cash_outflows) * flows_factor + cash_adjustments`
- `end_non_cash_balance` = `start_non_cash_balance + (non_cash_inflows - non_cash_outflows) * flows_factor + net_market_flows + non_cash_adjustments`
- `end_balance` = `end_cash_balance + end_non_cash_balance`

#### Entry 表（账目条目）

初始余额通过 `opening_anchor` 类型的 Valuation 存储在 entries 表中：
- `kind = 'opening_anchor'` - 期初余额锚点
- `kind = 'current_anchor'` - 期末余额锚点（关联账户专用）
- `kind = 'reconciliation'` - 对账余额

### 1.2 初始余额管理机制

#### OpeningBalanceManager 核心逻辑

**位置：** `app/models/account/opening_balance_manager.rb`

```ruby
# 设置期初余额
def set_opening_balance(balance:, date: nil)
  # 验证日期必须早于最早账目日期
  # 创建或更新 opening_anchor 类型的 Valuation
end
```

**锚点创建逻辑：**
```ruby
account.entries.create!(
  date: resolved_date,
  name: Valuation.build_opening_anchor_name(account.accountable_type),
  amount: balance,
  currency: account.currency,
  entryable: Valuation.new(kind: "opening_anchor")
)
```

**日期默认规则：**
- 如有最早账目日期：取 `[最早账目日期 - 1天, 2年前]` 中较小值
- 无账目：使用 `2年前`

### 1.3 账户类型与余额构成

| 账户类型 | balance_type | 余额构成 |
|---------|--------------|----------|
| Depository, CreditCard | `:cash` | 仅现金余额 |
| Property, Vehicle, OtherAsset, Loan, OtherLiability | `:non_cash` | 仅非现金余额 |
| Investment, Crypto | `:investment` | 现金 + 持仓（非现金） |

---

## 2. 余额重算触发链路完整分析

### 2.1 同步任务架构

#### Sync 状态机

**状态流转：**
```
pending → syncing → completed
               ↘ failed
               ↘ stale (24小时未完成)
```

**核心状态字段：**
- `status` - 同步状态
- `window_start_date` / `window_end_date` - 同步时间窗口
- `parent_id` - 父同步任务（支持层级同步）

#### SyncJob 执行流程

**位置：** `app/jobs/sync_job.rb`

```ruby
SyncJob.perform_later(sync)
  → sync.perform
    → syncable.perform_sync(sync)  # 调用对应 Syncer
      → [Family] 规则应用 + 子任务下发
      → [PlaidItem] 处理账户数据
      → [Account] materialize_balances  # 余额计算核心
```

### 2.2 账目操作的同步窗口计算机制

#### 窗口起始日期计算

**位置：** `app/models/entry.rb:46-49`

```ruby
def sync_account_later
  # 关键：日期变更时，取变更前后的最小值作为重算起点
  # 确保历史余额连续性不受影响
  sync_start_date = [ date_previously_was, date ].compact.min unless destroyed?
  account.sync_later(window_start_date: sync_start_date)
end
```

**窗口计算逻辑：**
| 操作类型 | 窗口起始日期 | 说明 |
|---------|------------|------|
| 新增交易 | `date` | 仅从该交易日期开始重算 |
| 编辑交易 | `min(原日期, 新日期)` | 覆盖整个变更范围 |
| 删除交易 | `原日期` | 从原交易日期开始重算 |

#### 触发点分布（Controller 层显式调用）

| 触发场景 | 文件位置 | 触发方式 |
|---------|---------|---------|
| 创建交易 | `app/controllers/transactions_controller.rb:61` | `@entry.sync_account_later` |
| 更新交易 | `app/controllers/transactions_controller.rb:88` | `@entry.sync_account_later` |
| 删除交易 | `app/controllers/concerns/entryable_resource.rb:34` | `@entry.sync_account_later` |
| 更新交易 | `app/controllers/trades_controller.rb:33` | `@entry.sync_account_later` |
| 批量删除 | `app/controllers/transactions/bulk_deletions_controller.rb:4` | 对每个受影响账户调用 `sync_later` |
| API创建 | `app/controllers/api/v1/transactions_controller.rb:83` | `@entry.sync_account_later` |

### 2.3 转账操作触发链路

#### Transfer::Creator 创建流程

**位置：** `app/models/transfer/creator.rb:10-23`

```ruby
def create
  transfer = Transfer.new(...)
  
  if transfer.save
    # 关键：转账同时触发两个账户同步
    source_account.sync_later       # 转出账户
    destination_account.sync_later  # 转入账户
  end
end
```

#### Transfer 匹配确认流程

**位置：** `app/controllers/transfer_matches_controller.rb:17`

```ruby
# 用户手动确认转账匹配后
@transfer.sync_account_later
  → inflow_transaction.entry.sync_account_later   # 流入端同步
  → outflow_transaction.entry.sync_account_later  # 流出端同步
```

### 2.4 Family 级同步下发机制

#### 层级同步架构

```
Family.sync_later(窗口参数)
  ↓
Family::Syncer.perform_sync(sync)
  ├─ 同步试用状态
  ├─ 调度规则应用（每条规则调用 rule.apply_later）
  └─ 下发子同步任务
      ├─ PlaidItem 同步（所有链接账户）
      └─ Account.manual 同步（所有手动账户）
          ↓ 透传窗口参数
          syncable.sync_later(
            parent_sync: sync, 
            window_start_date: sync.window_start_date, 
            window_end_date: sync.window_end_date
          )
```

**位置：** `app/models/family/syncer.rb:8-21`

#### 子任务下发范围

```ruby
def child_syncables
  family.plaid_items + family.accounts.manual
end
```

### 2.5 同步去重与窗口扩展机制

#### sync_later 核心逻辑

**位置：** `app/models/concerns/syncable.rb:14-35`

```ruby
def sync_later(parent_sync: nil, window_start_date: nil, window_end_date: nil)
  Sync.transaction do
    with_lock do
      sync = self.syncs.incomplete.first  # pending 或 syncing 状态
      
      if sync
        # 已有进行中的同步，扩展时间窗口（仅对pending生效）
        sync.expand_window_if_needed(window_start_date, window_end_date)
      else
        # 创建新同步任务
        sync = self.syncs.create!(...)
        SyncJob.perform_later(sync)
      end
    end
  end
end
```

#### 窗口扩展算法

**位置：** `app/models/sync.rb:107-127`

```ruby
def expand_window_if_needed(new_window_start_date, new_window_end_date)
  return unless pending?  # 仅pending状态可扩展，syncing中不中断
  return if self.window_start_date.nil? && self.window_end_date.nil?  # 已是全窗口

  # 取最早开始日期（向左扩展）
  earliest_start_date = if self.window_start_date && new_window_start_date
    [ self.window_start_date, new_window_start_date ].min
  else
    nil  # 任一为nil表示全量同步
  end

  # 取最晚结束日期（向右扩展）
  latest_end_date = if self.window_end_date && new_window_end_date
    [ self.window_end_date, new_window_end_date ].max
  else
    nil  # 任一为nil表示全量同步
  end

  update(window_start_date: earliest_start_date, window_end_date: latest_end_date)
end
```

**窗口扩展对重算时机的影响：**

| 场景 | 现有窗口 | 新请求窗口 | 扩展后窗口 | 重算范围变化 |
|------|---------|-----------|-----------|------------|
| 场景1 | 2025-01-15 → nil | 2025-01-10 → nil | 2025-01-10 → nil | **向左扩展5天**，重算更早数据 |
| 场景2 | 2025-01-15 → 2025-01-20 | 2025-01-18 → 2025-01-25 | 2025-01-15 → 2025-01-25 | **向右扩展5天** |
| 场景3 | 2025-01-15 → 2025-01-20 | nil → nil | nil → nil | **升级为全量同步** |
| 场景4 | syncing中（任何窗口） | 任意窗口 | 窗口不变 | **不扩展**，等待当前同步完成 |

**关键优化：**
- 避免重复创建同步任务
- 动态扩展同步窗口，确保数据完整性
- 高优先级队列执行（`queue_as :high_priority`）
- 同步中任务不中断，确保一致性

---

## 3. 触发与不触发余额重算的任务边界

### 3.1 直接触发余额重算的任务/场景

| 任务/场景 | 类型 | 触发方式 | 窗口参数 |
|----------|------|---------|---------|
| 账户创建 | 模型操作 | `account.sync_later` | 无（全量） |
| 期初余额变更 | 模型操作 | `sync_later` | 无 |
| 当前余额变更 | 模型操作 | `sync_later` | 无 |
| 交易新增/编辑/删除 | Controller操作 | `entry.sync_account_later` | `window_start_date` |
| 批量删除交易 | Controller操作 | `account.sync_later` | 无 |
| 创建转账 | 模型操作 | 双账户 `sync_later` | 无 |
| 确认转账匹配 | Controller操作 | 双账户 `sync_account_later` | 计算窗口 |
| 持仓删除 | 模型操作 | `account.sync_later` | 无 |
| 对账操作 | 模型操作 | `sync_later` | 无 |
| Plaid Webhook | 回调处理 | `plaid_item.sync_later` | 无 |
| Plaid 连接创建 | Controller操作 | `plaid_item.sync_later` | 无 |
| 数据导入完成 | Job | `family.sync_later` | 无 |
| 撤销导入 | Job | `family.sync_later` | 无 |
| 数据缓存清理 | Job | `family.sync_later` | 无 |
| 家庭重置 | Job | `family.sync_later` | 无 |
| 登录自动同步 | Concern | `Current.family.sync_later` | 无 |

### 3.2 不直接触发余额重算的后台任务

#### 状态维护类

| Job名称 | 队列 | 功能描述 | 为何不触发重算 |
|---------|------|---------|-------------|
| `SyncCleanerJob` | scheduled | 清理24小时未完成的同步，标记为stale | 仅状态维护，不改变业务数据 |
| `DestroyJob` | default | 异步删除标记为pending_deletion的账户 | 删除操作本身无需重算 |
| `UserPurgeJob` | default | 用户数据清理 | 数据销毁操作 |

#### 规则处理类

| Job名称 | 队列 | 功能描述 | 为何不触发重算 |
|---------|------|---------|-------------|
| `RuleJob` | medium_priority | 执行规则匹配与应用 | 修改交易属性本身不触发，但若规则**删除/创建交易**会间接触发 |
| `AutoCategorizeJob` | medium_priority | 自动分类交易 | 修改分类不影响余额计算 |

#### 市场数据预加载类

| Job名称 | 队列 | 功能描述 | 为何不触发重算 |
|---------|------|---------|-------------|
| `ImportMarketDataJob` | scheduled | 每日批量预加载汇率和证券价格 | 预加载优化，账户同步时才按需使用 |

> **注意：** 市场数据预加载不触发重算，但**每个账户同步时会即时补充获取缺失的市场数据**。预加载只是减少重复请求。

#### 其他不触发任务

| Job名称 | 队列 | 功能描述 |
|---------|------|---------|
| `AssistantResponseJob` | default | AI助手响应处理 |
| `FamilyDataExportJob` | default | 家庭数据导出 |
| `StripeEventHandlerJob` | default | Stripe支付事件处理 |
| `SecurityHealthCheckJob` | default | 安全健康检查 |
| `AutoDetectMerchantsJob` | default | 自动识别商户 |

---

## 4. 持仓数据在账户余额同步更新时的协作逻辑与边界

### 4.1 余额计算执行流程

```
Balance::Materializer.materialize_balances
  ↓
1. materialize_holdings → 先计算持仓
   └── Holding::Materializer
       └── Holding::ForwardCalculator / ReverseCalculator
  ↓
2. calculate_balances → 再计算余额
   └── Balance::ForwardCalculator / ReverseCalculator
       └── Balance::SyncCache → 缓存持仓数据供余额计算使用
  ↓
3. persist_balances → 持久化余额
  ↓
4. purge_stale_balances → 清理过期数据
  ↓
5. update_account_info → 更新账户缓存余额（仅正向计算）
```

### 4.2 持仓与余额的协作机制

#### 正向计算（ForwardCalculator）

**适用账户：** 手动账户（非关联账户）

**计算顺序：** 期初 → 期末

```ruby
# Balance::ForwardCalculator
def calculate
  # 1. 从期初锚点获取起始余额
  start_cash_balance = derive_cash_balance_on_date_from_total(
    total_balance: account.opening_anchor_balance,
    date: account.opening_anchor_date
  )
  
  # 2. 逐日迭代计算
  calc_start_date.upto(calc_end_date).map do |date|
    # a. 获取当日估值锚点（如有则直接使用）
    valuation = sync_cache.get_valuation(date)
    
    # b. 或通过交易推导当日余额
    if valuation.nil?
      end_cash_balance = derive_end_cash_balance(...)
      end_non_cash_balance = derive_end_non_cash_balance(...)
    end
    
    # c. 计算当日市场价值变动（依赖持仓数据）
    flows = flows_for_date(date)
    market_value_change = market_value_change_on_date(date, flows)
    
    # d. 构建当日余额记录
    build_balance(...)
  end
end
```

#### 反向计算（ReverseCalculator）

**适用账户：** 关联账户（如Plaid同步的账户）

**计算顺序：** 期末 → 期初

```ruby
# Balance::ReverseCalculator
def calculate
  # 1. 从期末锚点获取结束余额
  end_cash_balance = derive_cash_balance_on_date_from_total(
    total_balance: account.current_anchor_balance,
    date: account.current_anchor_date
  )
  
  # 2. 反向逐日迭代计算
  account.current_anchor_date.downto(account.opening_anchor_date).map do |date|
    # a. 遇期初锚点则重置
    if use_opening_anchor_for_date?(date)
      end_cash_balance = derive_cash_balance_on_date_from_total(...)
      start_cash_balance = end_cash_balance
    else
      # b. 反向推导期初
      start_cash_balance = derive_start_cash_balance(...)
      start_non_cash_balance = derive_start_non_cash_balance(...)
    end
    
    # c. 构建余额记录
    build_balance(...)
  end
end
```

#### 市场价值变动计算

**位置：** `app/models/balance/base_calculator.rb:47-57`

```ruby
def market_value_change_on_date(date, flows)
  return 0 unless account.balance_type == :investment
  
  # 1. 获取日初、日末持仓价值
  start_of_day_holdings_value = holdings_value_for_date(date.prev_day)
  end_of_day_holdings_value = holdings_value_for_date(date)
  
  # 2. 计算持仓总变化
  change_holdings_value = end_of_day_holdings_value - start_of_day_holdings_value
  
  # 3. 计算交易导致的持仓变化
  net_buy_sell_value = flows[:non_cash_inflows] - flows[:non_cash_outflows]
  
  # 4. 差值即为市场价值变动（涨跌）
  change_holdings_value - net_buy_sell_value
end
```

### 4.3 持仓计算的边界条件

#### 正向持仓计算（ForwardCalculator）

**位置：** `app/models/holding/forward_calculator.rb`

**特点：**
- 从空持仓开始（`empty_portfolio`）
- 逐日应用交易记录
- 所有价格来自市场数据

```ruby
def generate_starting_portfolio
  empty_portfolio  # 所有证券数量初始化为0
end
```

#### 反向持仓计算（ReverseCalculator）

**位置：** `app/models/holding/reverse_calculator.rb`

**特点：**
- 从当前持仓快照开始（`portfolio_snapshot`）
- 反向逐日撤销交易
- 当日价格使用持仓数据（Plaid提供），历史价格使用市场数据

```ruby
# 当前日用持仓价格，历史日用市场价格
holdings += build_holdings(current_portfolio, date, 
  price_source: date == Date.current ? "holding" : nil)
```

### 4.4 持仓与余额的边界划分

| 维度 | 持仓（Holdings） | 余额（Balances） |
|------|-----------------|-----------------|
| **存储粒度** | 按证券 + 日期 | 按账户 + 日期 |
| **数据来源** | 交易记录 × 证券价格 | 期初锚点 + 交易记录 + 持仓价值 + 估值锚点 |
| **计算方向** | 正向/反向均可 | 正向/反向与账户类型绑定 |
| **更新触发** | 余额计算前必先更新持仓 | 依赖持仓计算结果进行余额推导 |
| **持久化策略** | upsert 按 (account_id, security_id, date, currency) 去重 | upsert 按 (account_id, date, currency) 去重 |
| **清理逻辑** | 删除早于 start_date 或不在投资组合中的持仓 | 删除早于/晚于计算窗口的余额 |

### 4.5 数据缓存与共享机制

#### Balance::SyncCache

**位置：** `app/models/balance/sync_cache.rb`

**功能：** 在余额计算期间缓存和转换数据
- `get_valuation(date)` - 获取当日估值
- `get_holdings(date)` - 获取当日持仓（关键协作点）
- `get_entries(date)` - 获取当日交易

**货币转换：**
```ruby
# 持仓金额自动转换为账户货币
converted_holding.amount = converted_holding.amount_money.exchange_to(
  account.currency,
  date: h.date,
  fallback_rate: 1
).amount
```

### 4.6 投资账户余额构成公式

对于 `:investment` 类型账户：

```
总余额 = 现金余额 + 持仓总价值

当日持仓总价值 = Σ(持仓数量 × 证券当日价格)

市场价值变动 = (日末持仓价值 - 日初持仓价值) - 净买入卖出金额

现金余额 = 期初现金余额 + 现金净流入 × flows_factor + 现金调整项

非现金余额 = 期初非现金余额 + 非现金净流入 × flows_factor + 市场价值变动 + 非现金调整项
```

---

## 5. 关键设计要点总结

### 5.1 双锚点设计
- **期初锚点（opening_anchor）** - 所有账户通用，确定计算起点
- **期末锚点（current_anchor）** - 仅关联账户使用，确定计算终点
- 锚点通过 Valuation 类型的 Entry 存储，与普通交易统一数据模型

### 5.2 双计算方向
- **正向计算** - 从过去到现在，适合手动账户，依赖期初锚点
- **反向计算** - 从现在到过去，适合关联账户，依赖期末锚点，解决历史交易缺失问题

### 5.3 持仓-余额协作
- 持仓是余额的上游依赖，余额计算前必须先更新持仓
- 持仓提供非现金部分的逐日价值，余额负责整体账户的现金流和价值聚合
- 市场价值变动是连接持仓与余额的关键桥梁

### 5.4 窗口化增量重算
- 按最小变更日期计算重算窗口，避免全量重算
- 日期变更时自动扩展重算范围，保证数据连续性
- 同步进行中不中断，通过窗口扩展保证下一次同步覆盖完整

### 5.5 层级同步架构
- Family 级同步作为入口，下发给 PlaidItem 和手动账户
- 窗口参数完整透传，子任务继承父任务时间范围
- 通过 parent_id 关联，实现状态聚合与后处理

### 5.6 去重与幂等性
- 所有持久化操作使用 upsert，确保重复执行不产生重复数据
- 同步任务去重机制避免重复计算
- 数据库唯一索引保证数据一致性

---

*报告生成日期：2025-07-24*
*代码版本：基于 schema version 2025_07_24_115507*