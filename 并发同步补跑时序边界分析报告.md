# 并发同步补跑时序边界分析报告

## 核心结论速览

| 指标 | 时间 | 前提条件 |
|-----|------|---------|
| **最早恢复补跑** | 24小时 + 0分钟 | sync 卡住后立即遇上整点清理 |
| **最晚恢复补跑** | 24小时 + 59分59秒 | sync 卡住后立即错过整点，需等待下一小时 |
| **最坏叠加延迟** | ~48小时 | 连续两次 worker 重启，第二次又卡整小时边界 |

---

## 时序链路完整分析

### 1. 关键常量与配置

**代码位置**：`app/models/sync.rb:4-57`

```ruby
STALE_AFTER = 24.hours           # 24 小时超时阈值

scope :incomplete, -> { 
  where("syncs.status IN (?)", %w[pending syncing])  # ⚠️  不包含 stale!
}

def self.clean
  incomplete.where("syncs.created_at < ?", STALE_AFTER.ago).find_each(&:mark_stale!)
end
```

**调度配置**：`config/schedule.yml:10-14`

```yaml
clean_syncs:
  cron: "0 * * * *"  # ⚠️  每小时第 0 分钟执行
  class: "SyncCleanerJob"
```

---

### 2. 状态转移链路

```
sync_later 创建
    ↓
  pending
    ↓ (SyncJob 开始)
  syncing ←─┐
    ↓        │ Worker 卡住/重启，Job 丢失
  (卡住)     │ 状态永远停在 syncing
    ↓        │
  等待 24 小时 + 整点 cron 触发
    ↓
Sync.clean 执行 mark_stale!
    ↓
  stale ←─────┘
    ↓
  下一次 sync_later 看到 incomplete.first = nil
    ↓
  创建新的 sync ✅ 恢复补跑
```

---

### 3. 最早恢复补跑时序

**场景**：sync 卡住的时间点恰好是整点（0分0秒），立即触发 cron 清理

```
T0 (HH:00:00): sync 创建，立即 start! 进入 syncing
                │
                ├─ Worker 立即重启，Job 丢失，状态卡在 syncing
                │
T0+24h (HH:00:00): 刚好满 24 小时
                │
                ├─ SyncCleanerJob 在同一整点执行
                │  incomplete.where(created_at < 24h.ago) 命中
                │  mark_stale! → 状态变为 stale
                │
T0+24h+ε:      新账目变更调用 sync_later
                │  incomplete.first → nil（stale 不在集合中）
                │
                └─ ✅ 创建新 sync，恢复补跑
```

**最早恢复时间**：**24小时 + 0分钟**

---

### 4. 最晚恢复补跑时序

**场景**：sync 卡住的时间点是整点刚过（如 13:00:01），错过当次清理，需等待一整小时

```
T0 (13:00:01):  sync 创建并 start! 进入 syncing
                │
                ├─ Worker 重启，Job 丢失，状态卡在 syncing
                │
T0+24h (次日 13:00:01): 满 24 小时
                │  但上一次 cron 是 13:00:00（1秒前），刚错过
                │
T0+24h+59m59s (次日 13:59:59): 
                │  继续等待 cron
                │
T0+24h+60m (次日 14:00:00):
                │  SyncCleanerJob 终于执行
                │  incomplete.where(created_at < 24h.ago) 命中
                │  mark_stale! → 状态变为 stale
                │
T0+24h+60m+ε:  新账目变更调用 sync_later
                │  看到 stale 不在 incomplete 中
                │
                └─ ✅ 创建新 sync，恢复补跑
```

**最晚恢复时间**：**24小时 + 59分59秒**

---

### 5. incomplete 集合变化的关键时刻

| 时间点 | 状态 | incomplete 包含？ | 能否创建新 sync？ |
|-------|------|------------------|------------------|
| T0 ~ T0+24h | syncing | ✅ 是 | ❌ 否（找到旧 sync） |
| T0+24h ~ cron 执行 | syncing | ✅ 是 | ❌ 否（仍在集合中） |
| cron 执行后 | stale | ❌ 否 | ✅ 是 |

**关键边界**：
- `created_at < 24h.ago` 是**严格小于**，等于的不会被清理
- 哪怕只差 1 秒满 24 小时，也要等下一个整点

---

### 6. 可能继续延迟的前提条件

#### 前提 1：SyncCleanerJob 本身执行失败

**原因**：
- Sidekiq scheduled 队列堆积
- 数据库连接耗尽
- 部署时 worker 未启动

**额外延迟**：下一个整点（再加 1 小时）

**叠加最坏情况**：24h + 2h

#### 前提 2：stale 状态没有被正确传播到所有实例

**原因**：
- 数据库事务未提交
- 跨实例缓存不一致

**额外延迟**：直到下一次数据库查询看到新状态

**叠加最坏情况**：~几十秒到几分钟

#### 前提 3：连续两次 worker 重启卡在整点边界

```
T0: 第一次卡住
T0+24h+60m: 第一次恢复，创建新 sync
T0+24h+60m+ε: 新 sync 又卡在 syncing（worker 又重启）
T0+48h+120m: 第二次恢复
```

**叠加最坏情况**：~48小时

#### 前提 4：高并发下锁竞争

**代码位置**：`app/models/concerns/syncable.rb:15-16`

```ruby
Sync.transaction do
  with_lock do  # ⚠️  悲观锁
    sync = self.syncs.incomplete.first
    # ...
  end
end
```

**原因**：
- 大量并发 `sync_later` 导致锁等待
- 死锁超时

**额外延迟**：锁超时时间（通常几秒到几十秒）

---

### 7. 与窗口扩展机制的交互

**卡住前的状态**：

```
T0: sync A (syncing)
T0+1h: 新账目变更
       ↳ 找到 sync A (syncing)
       ↳ expand_window_if_needed
       ↳ return unless pending? → 静默丢弃窗口
```

**24小时 + 1小时后恢复**：

```
T0+25h: sync A 被标记为 stale
T0+25h+ε: 新账目变更
          ↳ incomplete.first → nil
          ↳ 创建新 sync B
          ↳ 窗口 = 本次变更的日期
```

**窗口丢失问题**：
- T0+1h 那次变更的窗口**永久丢失**了（虽然实际上是全量计算，不影响结果）
- 新 sync B 只使用 T0+25h 那次变更的窗口
- 中间所有变更的窗口参数都丢失了（但又是全量计算，恰好没问题）

---

## 架构风险总结

### 风险 1：静默的 24 小时黑洞

**现象**：sync 卡住后，24小时内所有新变更的窗口扩展都会被静默丢弃，没有任何告警。

**风险等级**：🟠 中高（当前被全量计算掩盖）

**代码证据**：
```ruby
def expand_window_if_needed(new_window_start_date, new_window_end_date)
  return unless pending?  # 没有日志，没有告警，静默 return
  # ...
end
```

### 风险 2：stale 状态没有任何后续处理

**现象**：被标记为 stale 的 sync 就永远停在那里，没有：
- 重试机制
- 告警通知
- 数据一致性检查

**风险等级**：🟡 中

### 风险 3：每小时一次的清理频率可能不够

**现象**：高并发场景下，worker 频繁重启导致大量 sync 卡在 syncing，用户需要等 24 小时才能恢复。

**风险等级**：🟡 中

---

## 优化建议

### 短期优化：增加告警和日志

```ruby
def expand_window_if_needed(new_window_start_date, new_window_end_date)
  if syncing?
    Rails.logger.warn(
      "[Sync #{id}] Cannot expand window while syncing. " \
      "New window: [#{new_window_start_date}, #{new_window_end_date}]. " \
      "Will be covered by next sync or after stale cleanup in #{STALE_AFTER}."
    )
    return
  end
  
  return unless pending?
  # ...
end
```

### 中期优化：降低 STALE_AFTER 阈值

```ruby
STALE_AFTER = 1.hour  # 从 24 小时改为 1 小时
```

**理由**：
- 正常 sync 执行时间通常在几秒到几分钟
- 1 小时未完成基本就是卡住了
- 减少用户等待时间

### 长期优化：健康检查 + 自动恢复

1. **增加 syncing 时长告警**：超过 10 分钟未完成发送告警
2. **心跳机制**：sync 执行过程中定期更新 `updated_at`
3. **自动重试**：检测到卡住后自动创建新 sync
4. **stale 统计指标**：监控 stale sync 的数量和频率

---

## 完整时序边界总结

```
                    sync 卡在 syncing
                            │
                            │
        ┌───────────────────┴───────────────────┐
        │                                         │
  最早边界：卡在整点                          最晚边界：卡在整点刚过
  0分钟后就遇到 cron                        等满59分59秒才遇到 cron
        │                                         │
        ▼                                         ▼
  ┌───────────┐                             ┌───────────┐
  │  24h+0m  │                             │ 24h+59m59s │
  └───────────┘                             └───────────┘
        │                                         │
        └───────────────────┬─────────────────────┘
                            │
                            ▼
                    状态变为 stale
                            │
                            ▼
                    下一次 sync_later 
                            │
                            ▼
                    ✅ 创建新 sync，恢复补跑
```

---

*分析日期：2025-07-24*
*代码版本：基于 schema version 2025_07_24_115507*