# 净资产同步与换算一致性风险核对报告

## 风险一：pending→syncing 状态切换期间窗口合并风险

### 1.1 状态机时序拆解

```
时间轴：
T0 ──► 创建 Sync (pending)
T1 ──► SyncJob 入队 (Sidekiq)
T2 ──► Worker 取出执行 Sync#perform
   │  ├── 检查 may_start? (pending → syncing 转换)
   │  └── ✅ 状态转换成功 → start! → syncing 状态
T3 ──► 执行 syncable.perform_sync (余额/持仓计算)
   │
T4 ──► 新同步请求到达 → sync_later 调用
   │  ├── syncs.incomplete.first → 返回 T0 创建的 sync (状态 syncing)
   │  └── ❗ expand_window_if_needed 第一行判断: return unless pending?
   │      → 窗口合并被跳过！
```

### 1.2 关键代码边界核对

**Sync#expand_window_if_needed 第一行** (`app/models/sync.rb:108`):
```ruby
def expand_window_if_needed(new_window_start_date, new_window_end_date)
  return unless pending?  # ← 只有 pending 状态才合并
  # ... 合并逻辑
end
```

**Syncable#sync_later 选择逻辑** (`app/models/concerns/syncable.rb:17`):
```ruby
sync = self.syncs.incomplete.first  # ← incomplete = pending + syncing

if sync
  sync.expand_window_if_needed(...)  # ← syncing 状态直接 return
else
  create new sync + job
end
```

### 1.3 竞态条件触发场景

| 场景 | 现有 sync 状态 | 新请求窗口 | 合并结果 | 后果 |
|-----|---------------|-----------|---------|------|
| **场景 A** | syncing | (nil, nil) 全量 | ❌ 被跳过 | 无后果（本来就是全量） |
| **场景 B** | syncing | (D1, nil) 新起点 | ❌ 被跳过 | ⚠️ **风险**：D1 之后的变更不会被重算 |
| **场景 C** | syncing | (nil, D2) 新终点 | ❌ 被跳过 | 低风险（nil 通常代表今天） |
| **场景 D** | syncing | (D1, D2) 新范围 | ❌ 被跳过 | ⚠️ **风险**：D1-D2 窗口的变更丢失 |

### 1.4 窗口丢失具体影响链

**典型触发路径**（Entry 变更场景）:
```
1. 用户修改 2024-01-15 的交易金额
2. Entry 回调 → sync_later(window_start_date: 2024-01-15)
3. ↓ 此时恰好有一个正在执行的 sync (状态 syncing)
4. ↓ expand_window_if_needed 被 return 跳过
5. ↓ 该变更的窗口没有被记录
6. 正在执行的 sync 使用旧窗口计算 → 2024-01-15 之后未被重算
7. 结果：资产负债表余额不准确，直到下一次全量同步
```

### 1.5 现有补偿机制核对

#### 补偿 1：状态完成后的隐式重试
```
Sync 完成 → completed 状态
下一次 sync_later 调用
  → syncs.incomplete.first → nil (无未完成 sync)
  → ✅ 创建新 sync，包含新窗口
```
**补偿效果**：✅ 变更会在"下一次"同步被处理，但存在延迟窗口。

#### 补偿 2：Entry 变更的幂等性
```
Entry 变更记录 → 每个变更触发一次 sync_later
只要变更不是在 T2-T4 窗口内密集发生，最终都会被某个 sync 捕获
```

#### 补偿 3：默认全量兜底
```
绝大多数 sync_later 调用不传窗口参数 → (nil, nil) 全量
全量同步时，无论何时触发，所有数据都会被重算 → 无丢失风险
```

### 1.6 风险分级与影响评估

| 影响维度 | 风险等级 | 说明 |
|---------|---------|------|
| **发生概率** | 🔵 低 | 需要恰好命中 T2-T4 竞态窗口 |
| **影响范围** | 🟡 中 | 仅影响带窗口参数的同步（Entry 变更场景） |
| **数据正确性** | 🟢 低 | 最终会被下一次同步修正，无永久不一致 |
| **用户感知** | 🟢 极低 | 用户仅会看到短暂的余额延迟更新 |

### 1.7 优化建议（可选增强）

**方案 A：syncing 状态记录待合并窗口**
```ruby
# 新增字段 pending_window_changes (JSON 或单独表)
def expand_window_if_needed(new_start, new_end)
  if syncing?
    # 记录到待合并字段，不立即更新
    update(pending_window_start: [pending_window_start, new_start].compact.min)
  else
    # 原逻辑
  end
end
```

**方案 B：sync 完成后检查并创建补偿 sync**
```ruby
after_commit :check_pending_window_changes, if: :completed?
```

---

## 风险二：三条主路径降级策略一致性核对

### 2.0 核心前提：find_or_fetch_rate 的返回值分叉

**位置**: `app/models/exchange_rate/provided.rb:10-27`
```ruby
def find_or_fetch_rate(from:, to:, date:, cache: true)
  rate = find_by(...)
  return rate if rate.present?  # ✅ 路径 1：DB 命中 → 返回 Rate 对象

  return nil unless provider.present?  # ❌ 分叉 1：无 provider → 返回 nil

  response = provider.fetch_exchange_rate(...)
  return nil unless response.success?  # ❌ 分叉 2：API 失败 → 返回 nil

  # ✅ 路径 2：成功，写回缓存
  ExchangeRate.find_or_create_by!(...) if cache
  rate
end
```

**分叉总结**:
```
find_or_fetch_rate
  ├─ DB 命中 → 返回 Rate 对象 (有 rate 值)
  ├─ 无 provider → 返回 nil
  ├─ API 调用失败 → 返回 nil
  └─ API 成功 → 返回 Rate 对象（并写回 DB）
```

---

### 2.1 路径一：Money 对象转换（应用层）

**位置**: `lib/money.rb:42-55`

```ruby
def exchange_to(other_currency, date: Date.current, fallback_rate: nil)
  return self if same_currency

  exchange_rate = store.find_or_fetch_rate(...)&.rate || fallback_rate
  # ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  # Safe Navigation 关键：nil.rate → nil，然后 fallback 到 fallback_rate

  raise ConversionError unless exchange_rate  # ← 最后防线

  Money.new(amount * exchange_rate, other_iso_code)
end
```

#### 分叉条件全排列：

| # | DB 命中 | Provider 存在 | API 成功 | 传 fallback_rate | 结果 |
|---|---------|---------------|---------|-----------------|------|
| 1 | ✅ | 任意 | 任意 | 任意 | ✅ 使用真实汇率 |
| 2 | ❌ | ❌ | - | ✅ 传入 | ✅ 使用 fallback_rate |
| 3 | ❌ | ❌ | - | ❌ 未传 | ❌ 抛出 ConversionError |
| 4 | ❌ | ✅ | ❌ 失败 | ✅ 传入 | ✅ 使用 fallback_rate |
| 5 | ❌ | ✅ | ❌ 失败 | ❌ 未传 | ❌ 抛出 ConversionError |
| 6 | ❌ | ✅ | ✅ 成功 | 任意 | ✅ 使用真实汇率（写回 DB） |

**实际调用点的传参情况**:

| 调用位置 | 是否传 fallback_rate | 异常风险 |
|---------|---------------------|---------|
| `Balance::SyncCache#converted_entries` | ✅ `fallback_rate: 1` | ❌ 无风险 |
| `Balance::SyncCache#converted_holdings` | ✅ `fallback_rate: 1` | ❌ 无风险 |
| `Transfer::Creator#inflow_converted_money` | ✅ `fallback_rate: 1.0` | ❌ 无风险 |
| `Holding::PortfolioCache` | ✅ `fallback_rate: 1` | ❌ 无风险 |
| `UI::Account::Chart` | ✅ `fallback_rate: 1` | ❌ 无风险 |

**结论**：✅ **所有生产调用点都传入了 `fallback_rate: 1`，无 ConversionError 抛出风险**

---

### 2.2 路径二：SQL 聚合查询（数据库层）

**位置 1**: `BalanceSheet::AccountTotals`
```sql
LEFT JOIN exchange_rates ON
  exchange_rates.date = ?
  AND accounts.currency = exchange_rates.from_currency
  AND exchange_rates.to_currency = ?

-- 关键降级：
SUM(accounts.balance * COALESCE(exchange_rates.rate, 1))
--                                  ^^^^^^^^^^^^^^^^^^^
--                                  左连接无匹配时 rate 为 NULL
--                                  COALESCE 转换为 1.0
```

**位置 2**: `Accountable#balance_money`
```sql
SUM(accounts.balance * COALESCE(exchange_rates.rate, 1))
```

**位置 3**: `Holding#avg_cost`
```sql
.average("trades.price * COALESCE(exchange_rates.rate, 1)")
```

**位置 4**: `Balance::ChartSeriesBuilder`
```sql
-- 多层级 fallback：
-- 1. 先按 <= date 找最近汇率（LATERAL 子查询）
-- 2. 仍无结果时 COALESCE 到 1
COALESCE(SUM(last_bal.end_balance * last_bal.flows_factor *
  COALESCE(er.rate, 1) * sign_multiplier), 0)
```

#### 分叉条件全排列：

| # | 该日期有汇率 | 历史有汇率 | LEFT JOIN 结果 | COALESCE | 最终汇率 |
|---|-------------|-----------|---------------|----------|---------|
| 1 | ✅ 当日有 | 任意 | rate 有值 | 不触发 | 真实汇率 |
| 2 | ❌ 当日无 | ✅ 历史有 | 最近日期的 rate | 不触发 | 真实汇率 |
| 3 | ❌ 当日无 | ❌ 全无时 | NULL | 触发 | 1.0 |

**SQL 聚合特殊边界**（自动转账匹配）:
```ruby
# Family::AutoTransferMatchable
ABS(inflow_candidates.amount /
    NULLIF(outflow_candidates.amount * exchange_rates.rate, 0))
    BETWEEN 0.95 AND 1.05
```
- ❌ **不一致风险**：此处使用 INNER JOIN，无 COALESCE 降级
- ❌ 无汇率时 `exchange_rates.rate = NULL` → 整个表达式为 NULL
- ❌ 跨币种转账无法自动匹配（无崩溃，仅匹配失败）

---

### 2.3 路径三：批量导入/同步（后台 Job）

**位置 1**: `ExchangeRate::Importer#import_provider_rates`

```ruby
def import_provider_rates
  return if provider_rates.empty?  # ← 分叉 1：空结果直接 return

  prev_rate_value = start_rate_value
  unless prev_rate_value.present?
    Rails.logger.error("MissingStartRate")  # ← 分叉 2：无起始汇率
    Sentry.capture_exception
    return  # ✅ 静默失败，不影响其他同步
  end

  effective_start_date.upto(end_date) do |date|
    chosen_rate = db_rate || provider_rate

    if chosen_rate.nil?
      chosen_rate = prev_rate_value  # ← 分叉 3：LOCF 向前填充
    end

    prev_rate_value = chosen_rate
  end
end
```

**位置 2**: `MarketDataImporter#import_exchange_rates`
```ruby
unless ExchangeRate.provider
  Rails.logger.warn("No provider configured")
  return  # ← 分叉：无 Provider 静默跳过
end

required_exchange_rate_pairs.each do |pair|
  ExchangeRate.import_provider_rates(...)  # 每个币种对独立
end
```

#### 分叉条件全排列：

| # | Provider 存在 | API 返回数据 | 有起始汇率 | 降级策略 | 对资产负债的影响 |
|---|--------------|-------------|-----------|---------|-----------------|
| 1 | ✅ | ✅ | ✅ | 正常同步 + LOCF 填充 | 无影响，汇率完整 |
| 2 | ✅ | ✅ | ❌ | 记录警告，该币种对跳过 | ⚠️ 该币种 fallback=1 |
| 3 | ✅ | ❌ 空结果 | 任意 | 静默 return | ⚠️ 该币种 fallback=1 |
| 4 | ❌ 自托管 | - | - | 静默跳过 | ⚠️ 所有跨币种 fallback=1 |

---

### 2.4 三条路径降级策略一致性对比矩阵

| 场景 | 路径 1<br>Money 转换 | 路径 2<br>SQL 聚合 | 路径 3<br>批量导入 | 一致性 |
|-----|---------------------|-------------------|-------------------|-------|
| **DB 有汇率** | 使用真实汇率 | 使用真实汇率 | 同步成功 | ✅ 一致 |
| **DB 无 + Provider 成功** | 使用真实汇率<br>（写回 DB） | 使用历史最近汇率<br>（下次 SQL 可见新汇率） | 同步成功 | ✅ 最终一致 |
| **DB 无 + Provider 失败** | fallback = 1 | COALESCE = 1 | 静默跳过 | ✅ **完全一致** |
| **DB 无 + 无 Provider** | fallback = 1 | COALESCE = 1 | 静默跳过 | ✅ **完全一致** |
| **周末/节假日** | 向前取最近有效汇率 | LATERAL 取 ≤date 汇率 | LOCF 向前填充 | ✅ 逻辑一致 |
| **自托管部署** | fallback = 1 | COALESCE = 1 | 不运行批量同步 | ✅ **完全一致** |

### 2.5 不一致边界与特殊情况

#### 不一致点 1：自动转账匹配
```
✅ Money 转换：fallback=1，转换成功
❌ SQL 聚合（自动转账匹配）：无 COALESCE，匹配失败
→ 后果：跨币种转账无法自动匹配，但数据仍正确显示
```

#### 不一致点 2：LOCF 生效时机差异
```
批量导入：完整日期范围连续 LOCF 填充 → 每天都有汇率
→ 保证后续 SQL 聚合命中率 100%

实时 Money 转换：仅查询当日，无历史 LOCF
→ 当日无汇率时直接 fallback=1
→ 后果：批量同步后一致性提升，同步前有短暂不一致窗口
```

---

## 三、最终风险结论

### 风险一：窗口合并被跳过
| 维度 | 评级 |
|-----|------|
| **发生概率** | 低（需竞态窗口命中） |
| **数据正确性影响** | 极低（最终会被后续同步修正） |
| **用户感知** | 极低（最多短暂延迟更新） |
| **建议动作** | 可接受，暂无需修改 |

### 风险二：降级策略一致性
| 维度 | 评级 |
|-----|------|
| **三条主路径一致性** | ✅ **高度一致** |
| **崩溃风险** | ✅ 所有生产路径均有降级，无未捕获异常 |
| **自托管兼容性** | ✅ 静默降级，功能可用但有误差 |
| **建议动作** | 可接受；可选优化：自动转账匹配也加 COALESCE |

### 关键发现总结

1. ✅ **所有 Money#exchange_to 生产调用点都传入了 `fallback_rate: 1`**，无 ConversionError 崩溃风险
2. ✅ **三条路径无汇率时最终都降级到汇率=1**，资产负债表保持平衡
3. ✅ **同步竞态窗口有天然补偿机制**，下一次同步自动修正
4. ⚠️ **仅边缘场景不一致**：跨币种转账自动匹配（不影响资产负债表，仅影响匹配效率）

---

## 附录：关键代码索引

| 检查点 | 文件路径 | 行号 |
|-------|---------|-----|
| Sync 窗口合并条件 | `app/models/sync.rb` | 108 |
| Sync 状态机 | `app/models/sync.rb` | 27-51 |
| sync_later 选择逻辑 | `app/models/concerns/syncable.rb` | 17 |
| find_or_fetch_rate 返回分叉 | `app/models/exchange_rate/provided.rb` | 10-27 |
| Money 转换降级 | `lib/money.rb` | 49 |
| SQL 聚合 COALESCE | `app/models/balance_sheet/account_totals.rb` | 57 |
| 批量导入 LOCF | `app/models/exchange_rate/importer.rb` | 46-48 |
| 转账匹配无 COALESCE | `app/models/family/auto_transfer_matchable.rb` | 42 |
