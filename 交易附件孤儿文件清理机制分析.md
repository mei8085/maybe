# 交易附件孤儿文件清理机制分析

## 1. 概述

本系统基于 Rails ActiveStorage 框架实现文件上传与存储管理。孤儿文件（Orphan File）是指已上传到存储服务但不再被任何业务记录引用的文件。系统通过多层机制确保这些文件能够被及时识别和回收。

## 2. 核心架构

### 2.1 ActiveStorage 三表结构

系统使用 ActiveStorage 提供的三张核心表来管理文件：

| 表名 | 作用 | 关键字段 |
|------|------|----------|
| `active_storage_blobs` | 存储文件元数据和存储位置引用 | `key`, `filename`, `content_type`, `byte_size`, `checksum`, `service_name` |
| `active_storage_attachments` | 关联表，连接 blobs 和业务模型 | `name`, `record_type`, `record_id`, `blob_id` |
| `active_storage_variant_records` | 记录图片变体信息 | `blob_id`, `variation_digest` |

**代码参考**：
- 数据库迁移：[20240426162500_create_active_storage_tables.active_storage.rb](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/db/migrate/20240426162500_create_active_storage_tables.active_storage.rb#L1-L57)
- 数据库 schema：[schema.rb](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/db/schema.rb#L50-L76)

### 2.2 存储服务配置

系统支持多种存储后端，通过 [config/storage.yml](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/config/storage.yml#L1-L25) 配置：

```yaml
# 本地磁盘存储
local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# 亚马逊 S3
amazon:
  service: S3
  access_key_id: <%= ENV["S3_ACCESS_KEY_ID"] %> 
  secret_access_key: <%= ENV["S3_SECRET_ACCESS_KEY"] %>
  region: <%= ENV["S3_REGION"] || "us-east-1" %>
  bucket: <%= ENV["S3_BUCKET"] %>

# Cloudflare R2 (S3 兼容)
cloudflare:
  service: S3
  endpoint: https://<%= ENV['CLOUDFLARE_ACCOUNT_ID'] %>.r2.cloudflarestorage.com
  bucket: <%= ENV['CLOUDFLARE_BUCKET'] %>
```

环境配置参考：[production.rb](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/config/environments/production.rb#L34-L37)

## 3. 附件上传与引用关联机制

### 3.1 模型附件声明

系统中以下模型使用了 ActiveStorage 附件：

| 模型 | 附件声明 | 用途 |
|------|----------|------|
| [User](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/app/models/user.rb#L25-L28) | `has_one_attached :profile_image` | 用户头像 |
| [PlaidItem](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/app/models/plaid_item.rb#L16) | `has_one_attached :logo` | 金融机构 Logo |
| [FamilyExport](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/app/models/family_export.rb#L4) | `has_one_attached :export_file` | 数据导出文件 |
| [Account](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/app/models/account.rb#L27) | `has_one_attached :logo` | 账户 Logo |

### 3.2 上传流程

#### 3.2.1 普通表单上传

以 [FamilyDataExportJob](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/app/jobs/family_data_export_job.rb#L10-L14) 为例：

```ruby
family_export.export_file.attach(
  io: zip_file,
  filename: family_export.filename,
  content_type: "application/zip"
)
```

**流程说明**：
1. 创建 `ActiveStorage::Blob` 记录，生成唯一 `key`
2. 将文件内容上传到配置的存储服务（本地磁盘 / S3 / R2）
3. 创建 `ActiveStorage::Attachment` 记录，关联业务记录与 blob

#### 3.2.2 前端上传交互

前端通过 [file_upload_controller.js](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/app/javascript/controllers/file_upload_controller.js#L1-L74) 处理上传交互：
- 监听文件选择事件，显示文件名
- 表单提交时显示上传状态
- 配合 Rails ActiveStorage JavaScript 库完成直传

### 3.3 引用关联的核心原理

ActiveStorage 通过多态关联实现附件与任意业务模型的关联：

```
业务记录 (e.g., User)
    ↓
active_storage_attachments (record_type, record_id, name, blob_id)
    ↓
active_storage_blobs (key, filename, byte_size, checksum, service_name)
    ↓
实际文件存储 (Disk / S3 / R2)
```

**关键约束**：
- `active_storage_attachments` 有唯一索引：`[record_type, record_id, name, blob_id]`
- 一个 blob 可以被多个 attachment 引用（实现文件去重）

## 4. 孤儿文件识别机制

### 4.1 孤儿文件的定义

孤儿文件是指满足以下条件的 `active_storage_blobs` 记录：
1. 在 `active_storage_attachments` 表中没有任何关联记录
2. 创建时间超过一定阈值（通常为 1 天，提供"宽限期"）

### 4.2 Rails 内置识别方法

Rails 7+ 提供了 `ActiveStorage::Blob.unattached` 作用域，其 SQL 逻辑大致为：

```sql
SELECT * FROM active_storage_blobs
LEFT JOIN active_storage_attachments 
  ON active_storage_attachments.blob_id = active_storage_blobs.id
WHERE active_storage_attachments.id IS NULL
  AND active_storage_blobs.created_at < [阈值时间]
```

### 4.3 孤儿文件产生的场景

1. **用户中断上传**：文件已上传到存储，但用户关闭页面，表单未提交
2. **附件替换**：用户更新附件时，旧的 blob 失去引用
3. **记录删除**：业务记录被删除，关联的 attachment 被删除，但 blob 未被清理
4. **直接上传失败**：Direct Upload 完成但后续业务逻辑失败

## 5. 孤儿文件回收清理机制

### 5.1 主动清理（记录删除时）

#### 5.1.1 模型级联删除

系统通过 `dependent: :destroy` 确保关联记录被清理：

```ruby
# [entry.rb](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/app/models/entry.rb#L10)
delegated_type :entryable, types: Entryable::TYPES, dependent: :destroy

# [family.rb](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/app/models/family.rb#L16-L33)
has_many :users, dependent: :destroy
has_many :accounts, dependent: :destroy
has_many :family_exports, dependent: :destroy
```

#### 5.1.2 显式 Purge 操作

[User 模型](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/app/models/user.rb#L115-L173) 在用户注销时显式清理附件：

```ruby
def purge
  if last_user_in_family?
    family.destroy
  else
    destroy
  end
end

# 验证失败时也主动 purge
def ensure_valid_profile_image
  return unless profile_image.attached?
  
  unless profile_image.content_type.in?(%w[image/jpeg image/png])
    errors.add(:profile_image, "must be a JPEG or PNG")
    profile_image.purge  # 立即清理无效文件
  end
end
```

#### 5.1.3 异步清理

用户注销时通过 [UserPurgeJob](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/app/jobs/user_purge_job.rb#L1-L9) 异步清理：

```ruby
class UserPurgeJob < ApplicationJob
  def perform(user)
    user.purge
  end
end
```

### 5.2 定时清理（定期巡检）

#### 5.2.1 Rails 内置清理任务

Rails 提供了内置的清理任务：

```bash
# 清理所有未关联的 blobs（包含变体）
rails active_storage:purge_unattached

# 清理未使用的变体记录和文件
rails active_storage:cleanup
```

`purge_unattached` 任务会：
1. 查找 `created_at < 1.day.ago` 且无 attachment 关联的 blobs
2. 对每个 blob 调用 `purge` 方法
3. `purge` 方法会删除存储服务上的实际文件 + 删除 blob 记录

#### 5.2.2 自定义清理实现示例

参考业界标准实践，清理任务的实现逻辑如下：

```ruby
# 参考实现（Rails 框架内部）
class CleanupOrphanedBlobsJob < ApplicationJob
  def perform
    ActiveStorage::Blob
      .unattached
      .where(created_at: ..1.day.ago)  # 1天宽限期
      .find_each(&:purge_later)        # 异步删除
  end
end
```

### 5.3 存储服务端清理

对于 S3 / Cloudflare R2 等对象存储服务，还可以配置生命周期规则：

- 配置临时上传目录的文件自动过期
- 配置标记为"待删除"的文件在 N 天后自动清理

## 6. 现有定时任务配置

系统当前的定时任务通过 [config/schedule.yml](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/config/schedule.yml#L1-L20) 配置：

| 任务 | 频率 | 作用 |
|------|------|------|
| `ImportMarketDataJob` | 工作日 22:00 UTC | 导入市场数据 |
| **`SyncCleanerJob`** | **每小时** | **清理过期同步任务（状态清理）** |
| `SecurityHealthCheckJob` | 工作日 02:00 UTC | 证券数据健康检查 |

**注意**：当前系统未显式配置 `ActiveStorage` 孤儿文件的定期清理任务。建议添加：

```yaml
# 建议添加到 schedule.yml
purge_unattached_blobs:
  cron: "0 3 * * *"  # 每天凌晨 3 点
  class: "ActiveStorage::PurgeUnattachedJob"
  queue: "scheduled"
  description: "Purge unattached ActiveStorage blobs older than 24 hours"
```

## 7. 清理流程完整示意图

```
用户上传文件
    ↓
创建 Blob + 上传到存储
    ↓
┌─ 表单提交成功？ ──否─┐
│    ↓ 是              │
│ 创建 Attachment       │
│    ↓                  │
│ 业务记录使用中        │
│    ↓                  │
│ 业务记录删除 ────────┘
│    ↓
│ Attachment 级联删除
│    ↓
│ Blob 成为孤儿（无关联）
│    ↓
│ 等待 24 小时宽限期
│    ↓
└─→ purge_unattached 任务执行
     ↓
  调用 blob.purge
     ↓
  删除存储文件 + 删除 Blob 记录
```

## 8. 关键代码索引

### 8.1 模型层
- [User#purge](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/app/models/user.rb#L115-L121) - 用户数据清理
- [User#ensure_valid_profile_image](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/app/models/user.rb#L167-L174) - 无效文件即时清理
- [Entry](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/app/models/entry.rb#L1-L99) - 交易记录核心模型
- [FamilyExport](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/app/models/family_export.rb#L1-L22) - 导出文件附件

### 8.2 任务层
- [UserPurgeJob](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/app/jobs/user_purge_job.rb#L1-L9) - 用户数据异步清理
- [SyncCleanerJob](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/app/jobs/sync_cleaner_job.rb#L1-L7) - 同步状态清理
- [FamilyDataExportJob](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/app/jobs/family_data_export_job.rb#L1-L22) - 附件创建示例

### 8.3 配置层
- [storage.yml](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/config/storage.yml#L1-L25) - 存储服务配置
- [schedule.yml](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/config/schedule.yml#L1-L20) - 定时任务配置
- [sidekiq.rb](file:///d:/fz/0601-1/solo-dogfeeding/code/33-maybe/config/initializers/sidekiq.rb#L1-L16) - Sidekiq 配置

## 9. 优化建议

### 9.1 立即实施
1. **添加孤儿文件定期清理任务**：在 `schedule.yml` 中添加 `active_storage:purge_unattached` 的定时执行
2. **配置宽限期**：确保清理任务使用至少 24 小时的宽限期，避免误删正在处理的文件

### 9.2 长期优化
1. **实现文件去重**：利用 `checksum` 字段实现相同文件复用，减少存储成本
2. **添加清理日志**：记录每次清理的文件数量和释放的存储空间
3. **监控告警**：当孤儿文件数量异常增长时触发告警
4. **软删除机制**：重要文件先标记为待删除，人工确认后再物理删除

## 10. 总结

本系统的孤儿文件清理机制是**"被动清理为主，主动清理为辅"**的三层架构：

1. **第一层**：模型删除时通过 `dependent: :destroy` 和 `purge` 主动清理
2. **第二层**：Rails 内置 `ActiveStorage::Blob.unattached` 作用域识别孤儿文件
3. **第三层**：通过定时任务定期调用 `purge` 回收存储资源

虽然当前系统未显式配置 ActiveStorage 的定期清理任务，但框架本身提供了完整的机制支持，只需添加定时任务配置即可实现完整的孤儿文件自动回收。
