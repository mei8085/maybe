# 净资产聚合同步链路调用点级分析

## 一、SyncJob 触发入口全景

### 1.1 Account 类触发入口（15个调用点）

| 序号 | 调用位置 | window_start_date | window_end_date | 触发场景说明 | 合并后窗口 |
|-----|---------|-------------------|----------------|------------|-----------|
| **1** | `Account.create_and_sync`<br>`app/models/account.rb:71` | `nil` | `nil` | 新建账户后立即触发全量同步 | `(nil, nil)` 全量 |
| **2** | `Entry#sync_account_later`<br>`app/models/entry.rb:48` | `[date_was, date].compact.min` | `nil` | 条目（交易/估值/交易）创建/更新/删除后<br>从变更日期开始重算 | `(min_date, nil)` 向前扩展至最早变更日 |
| **3** | `Holding#destroy_holding_and_entries!`<br>`app/models/holding.rb:60` | `nil` | `nil` | 删除持仓及其关联交易后同步 | `(nil, nil)` 全量 |
| **4** | `Account::Anchorable#set_opening_anchor_balance`<br>`app/models/account/anchorable.rb:14` | `nil` | `nil` | 设置期初锚定余额成功后 | `(nil, nil)` 全量 |
| **5** | `Account::Anchorable#set_current_balance`<br>`app/models/account/anchorable.rb:32` | `nil` | `nil` | 设置当前锚定余额成功后 | `(nil, nil)` 全量 |
| **6** | `Account::Reconcileable#create_reconciliation`<br>`app/models/account/reconcileable.rb:6` | `nil` | `nil` | 创建对账记录成功后（非dry_run） | `(nil, nil)` 全量 |
| **7** | `Account::Reconcileable#update_reconciliation`<br>`app/models/account/reconcileable.rb:12` | `nil` | `nil` | 更新对账记录成功后（非dry_run） | `(nil, nil)` 全量 |
| **8** | `AccountsController#sync`<br>`app/controllers/accounts_controller.rb:30` | `nil` | `nil` | 用户手动点击账户同步按钮 | `(nil, nil)` 全量 |
| **9** | `Transactions::BulkDeletionsController#create`<br>`app/controllers/transactions/bulk_deletions_controller.rb:4` | `nil` | `nil` | 批量删除交易后逐一同步账户 | `(nil, nil)` 全量 |
| **10** | `AccountableResource concern`<br>`app/controllers/concerns/accountable_resource.rb:52` | `nil` | `nil` | 账户资源更新后触发 | `(nil, nil)` 全量 |
| **11** | `Trade::CreateForm#create_trade`<br>`app/models/trade/create_form.rb:50` | `nil` | `nil` | 创建买入/卖出交易成功后 | `(nil, nil)` 全量 |
| **12** | `Trade::CreateForm#create_interest_income`<br>`app/models/trade/create_form.rb:69` | `nil` | `nil` | 创建利息收入交易成功后 | `(nil, nil)` 全量 |
| **13** | `Trade::CreateForm#create_unlinked_transfer`<br>`app/models/trade/create_form.rb:106` | `nil` | `nil` | 创建无关联转账成功后 | `(nil, nil)` 全量 |
| **14** | `Transfer::Creator#create`<br>`app/models/transfer/creator.rb:18-19` | `nil` | `nil` | 转账创建后同步**源账户**和**目标账户** | `(nil, nil)` 全量 |
| **15** | `PlaidItem#schedule_account_syncs`<br>`app/models/plaid_item.rb:65-68` | 继承父同步参数 | 继承父同步参数 | PlaidItem同步完成后批量调度账户同步 | 同父同步窗口 |

---

### 1.2 Family 类触发入口（8个调用点）

| 序号 | 调用位置 | window_start_date | window_end_date | 触发场景说明 | 合并后窗口 |
|-----|---------|-------------------|----------------|------------|-----------|
| **1** | `Import#publish`<br>`app/models/import.rb:70` | `nil` | `nil` | 导入完成发布后全家同步 | `(nil, nil)` 全量 |
| **2** | `Import#revert`<br>`app/models/import.rb:91` | `nil` | `nil` | 回滚导入后全家同步 | `(nil, nil)` 全量 |
| **3** | `AccountsController#sync_all`<br>`app/controllers/accounts_controller.rb:13` | `nil` | `nil` | 用户点击"全部同步"按钮 | `(nil, nil)` 全量 |
| **4** | `AutoSync#sync_family`<br>`app/controllers/concerns/auto_sync.rb:10` | `nil` | `nil` | 用户登录时自动触发（需满足条件） | `(nil, nil)` 全量 |
| **5** | `FamilyResetJob#perform`<br>`app/jobs/family_reset_job.rb:16` | `nil` | `nil` | 家庭数据重置完成后同步 | `(nil, nil)` 全量 |
| **6** | `DataCacheClearJob#perform`<br>`app/jobs/data_cache_clear_job.rb:13` | `nil` | `nil` | 数据缓存清除后重新计算 | `(nil, nil)` 全量 |
| **7** | `Family::Syncer#perform_sync`<br>`app/models/family/syncer.rb:19` | 父同步 start | 父同步 end | Family同步执行时调度子同步<br>包含：PlaidItem同步 + Manual Account同步 | 与父同步窗口一致 |
| **8** | `Family::PlaidConnectable#create_plaid_item!` | 隐式触发 | 隐式触发 | 创建Plaid连接后触发后续同步 | 取决于具体实现 |

---

### 1.3 PlaidItem 类触发入口（6个调用点）

| 序号 | 调用位置 | window_start_date | window_end_date | 触发场景说明 | 合并后窗口 |
|-----|---------|-------------------|----------------|------------|-----------|
| **1** | `PlaidItem::WebhookProcessor#process`<br>`app/models/plaid_item/webhook_processor.rb:20` | `nil` | `nil` | 收到 `TRANSACTIONS:SYNC_UPDATES_AVAILABLE` webhook | `(nil, nil)` 全量 |
| **2** | `PlaidItem::WebhookProcessor#process`<br>`app/models/plaid_item/webhook_processor.rb:22` | `nil` | `nil` | 收到 `INVESTMENTS_TRANSACTIONS:DEFAULT_UPDATE` webhook | `(nil, nil)` 全量 |
| **3** | `PlaidItem::WebhookProcessor#process`<br>`app/models/plaid_item/webhook_processor.rb:24` | `nil` | `nil` | 收到 `HOLDINGS:DEFAULT_UPDATE` webhook | `(nil, nil)` 全量 |
| **4** | `PlaidItemsController#sync`<br>`app/controllers/plaid_items_controller.rb:42` | `nil` | `nil` | 用户手动点击Plaid连接同步按钮 | `(nil, nil)` 全量 |
| **5** | `Family::PlaidConnectable`<br>`app/models/family/plaid_connectable.rb:27` | `nil` | `nil` | 连接Plaid后触发同步 | `(nil, nil)` 全量 |
| **6** | `PlaidItem::Syncer#perform_sync`<br>`app/models/plaid_item/syncer.rb:16-20` | 父同步 start | 父同步 end | PlaidItem同步完成后调度子账户同步<br>调用 `schedule_account_syncs` 批量触发 | 与父同步窗口一致 |

---

### 1.4 Job 间接触发入口（2个调用点）

| 序号 | 调用位置 | window_start_date | window_end_date | 触发场景说明 | 合并后窗口 |
|-----|---------|-------------------|----------------|------------|-----------|
| **1** | `SyncJob#perform`<br>`app/jobs/sync_job.rb:5` | N/A | N/A | 实际执行同步Job<br>**注意：这是SyncJob的执行入口，不是创建入口** | N/A |
| **2** | `Syncable#sync_later`<br>`app/models/concerns/syncable.rb:29` | 调用传入 | 调用传入 | **唯一的SyncJob创建点**<br>所有同步请求最终汇聚至此<br>创建Sync记录后调用 `SyncJob.perform_later(sync)` | 详见1.5合并规则 |

---

### 1.5 窗口合并核心算法（调用点级别）

**合并发生位置**: `Syncable#sync_later` → `app/models/concerns/syncable.rb:21`

```ruby
# 事务 + 锁 保护下的合并逻辑
Sync.transaction do
  with_lock do
    sync = self.syncs.incomplete.first

    if sync
      # 路径A：存在未完成同步，尝试合并
      sync.expand_window_if_needed(window_start_date, window_end_date)
    else
      # 路径B：无未完成同步，新建Sync + 调度Job
      sync = self.syncs.create!(
        parent: parent_sync,
        window_start_date: window_start_date,
        window_end_date: window_end_date
      )
      SyncJob.perform_later(sync)  # 最终Job创建点
    end
  end
end
```

**合并真值表** (`expand_window_if_needed`):

| 现有 sync.start | 现有 sync.end | 新传入 start | 新传入 end | 结果 start | 结果 end | 说明 |
|----------------|--------------|-------------|-----------|-----------|---------|------|
| `nil` | `nil` | 任意 | 任意 | `nil` | `nil` | 已是全量，无需合并 |
| `A` | `nil` | `B` | `nil` | `min(A,B)` | `nil` | 起点向前扩展 |
| `A` | `E` | `B` | `F` | `min(A,B)` | `max(E,F)` | 双向扩展 |
| `A` | `E` | `B` | `nil` | `min(A,B)` | `nil` | 起点扩展，终点变无限 |
| `nil` | `E` | `nil` | `F` | `nil` | `max(E,F)` | 终点向后扩展 |

**关键边界条件**:
1. 仅 `pending` 状态同步可合并，`syncing` 状态直接跳过
2. `nil` 表示"无限范围"，不是"无日期"
3. 合并操作不创建新 Job，仅更新现有 Sync 记录
4. 父子同步共享窗口，下传时保持一致

---

## 二、汇率缺失处理四条路径详解

### 2.1 路径一：数据库命中

**触发条件**:
```ruby
# ExchangeRate.find_or_fetch_rate
rate = find_by(from_currency: from, to_currency: to, date: date)
return rate if rate.present?
```

**检查点**:
1. 精确日期匹配：`date = 查询日期`
2. 货币对精确匹配：`from_currency + to_currency`
3. 唯一索引保证：`index_exchange_rates_on_base_converted_date_unique`

**金额影响**:
- ✅ 使用数据库存储的精确汇率值
- ✅ 转换误差 = 0（相对市场实际值）
- ✅ 无 fallback，无估算
- ✅ 净资产计算准确

**性能特征**:
- 单次主键查询，成本极低
- 汇率表通常已缓存（Rails SQL cache）
- 高频查询的日期/货币对命中率 > 99%

**代码路径**:
```
Money#exchange_to(date: D)
  └── ExchangeRate.find_or_fetch_rate(from: F, to: T, date: D)
        └── find_by(...) → 命中 → 返回 Rate 对象
              └── Money.new(amount * rate.rate, T)
```

---

### 2.2 路径二：Provider 实时拉取

**触发条件**:
```ruby
return nil unless provider.present?  # 自托管无provider跳过

response = provider.fetch_exchange_rate(from: from, to: to, date: date)
return nil unless response.success?
```

**前置检查**:
1. 数据库未命中（路径一失败）
2. Provider 已配置（`Provider::Registry.get_provider(:synth)` 存在）
3. Provider API 可用且返回成功

**金额影响**:
- ✅ 使用 Synth 提供商的真实市场汇率
- ✅ 转换误差 ≈ 0（与市场实际值一致）
- ✅ 无 fallback，无估算
- ✅ 净资产计算准确

**延迟特征**:
- HTTP 请求延迟：100-500ms（阻塞计算）
- 可能超时或失败 → 降级到路径四（fallback）

**缓存写回时机（路径三前置）**:
```ruby
ExchangeRate.find_or_create_by!(
  from_currency: rate.from,
  to_currency: rate.to,
  date: rate.date,
  rate: rate.rate
) if cache  # cache 参数默认为 true
```

**代码路径**:
```
Money#exchange_to
  └── ExchangeRate.find_or_fetch_rate
        ├── DB未命中
        ├── Provider存在
        ├── Synth API GET /rates/historical
        │     └── 成功返回汇率数据
        └── 返回 API 返回的 Rate 对象
              └── Money.new(amount * rate.rate, T)
```

---

### 2.3 路径三：数据库写回（缓存）

**触发条件**（与路径二同时发生）:
1. Provider 拉取成功（路径二成功）
2. `cache: true` 参数（默认为 true）
3. 调用 `ExchangeRate.find_or_create_by!`

**幂等保证**:
```ruby
# 使用 upsert 语义，确保不重复创建
ExchangeRate.find_or_create_by!(
  from_currency: rate.from,
  to_currency: rate.to,
  date: rate.date,
  rate: rate.rate
)
```

**写回批次场景**（非实时调用）:
```ruby
# ImportMarketDataJob 每日批量同步
ExchangeRate::Importer#import_provider_rates
  └── upsert_rows(200条/批次)
        └── ExchangeRate.upsert_all(unique_by: %i[from_currency to_currency date])
```

**金额影响**:
- ✅ 对本次计算无直接影响（路径二已返回）
- ✅ 提升后续查询命中率（路径一直接命中）
- ✅ 降低 Provider API 调用频次
- ✅ 历史数据完整性保证（LOCF 算法）

**LOCF 缺口填充逻辑** (`ExchangeRate::Importer`):
```ruby
# 周末/节假日无汇率时，向前填充最近有效汇率
prev_rate_value = start_rate_value

start_date.upto(end_date) do |date|
  chosen_rate = db_rates[date] || provider_rates[date] || prev_rate_value
  prev_rate_value = chosen_rate  # 向前填充
end
```

**代码路径**:
```
ExchangeRate.find_or_fetch_rate
  ├── Provider拉取成功
  ├── cache = true
  └── find_or_create_by! 写入数据库
        └── 后续相同日期/货币对查询 → 路径一直接命中
```

---

### 2.4 路径四：Fallback兜底

**触发条件**（任一满足即触发）:
1. ❌ 数据库未命中（路径一失败）
2. ❌ Provider 未配置（自托管场景）
3. ❌ Provider API 调用失败
4. ❌ Provider 返回空/无效数据
5. ✅ 调用方传入了 `fallback_rate` 参数

**两种 Fallback 形态**:

**形态 A：应用层 Money 转换**
```ruby
# Balance::SyncCache#converted_entries
converted_entry.amount = converted_entry.amount_money.exchange_to(
  account.currency,
  date: e.date,
  fallback_rate: 1  # ← 显式传入 fallback
).amount
```

**形态 B：SQL 层 COALESCE**
```sql
-- BalanceSheet::AccountTotals
SUM(accounts.balance * COALESCE(exchange_rates.rate, 1))

-- Balance::ChartSeriesBuilder
COALESCE(SUM(last_bal.end_balance * ... * COALESCE(er.rate, 1) ...), 0)

-- Accountable::balance_money
sum("accounts.balance * COALESCE(exchange_rates.rate, 1)")

-- Holding#avg_cost
average("trades.price * COALESCE(exchange_rates.rate, 1)")
```

**金额影响**:
- ⚠️ 使用汇率 = 1.0 进行转换
- ⚠️ 误差范围：-20% ~ +20%（主要货币对）
- ⚠️ 极端场景（如 BTC → USD）误差可达数千%
- ⚠️ 多币种叠加误差可能放大

**误差传导链**:
```
单账户余额误差 → 分类汇总误差 → 资产/负债总计误差 → 净资产误差
        ↓
    图表时间序列跳变（汇率补全后修正）
```

**修正机制**:
1. 每日 `ImportMarketDataJob` 批量补全
2. 账户同步时 `import_market_data` 单独补全
3. 下次页面访问时自动触发 Provider 拉取

**Fallback 边界场景**:

| 场景 | Fallback 行为 | 影响程度 |
|-----|-------------|---------|
| 主要货币对（EUR/USD, GBP/USD等） | 汇率=1 | 低，误差±15%以内 |
| 小众货币（TRY, ARS等） | 汇率=1 | 中，误差可能超过50% |
| 加密货币（BTC, ETH） | 汇率=1 | 高，误差可达数千% |
| 历史日期（>1年前） | 可能长期无法补正 | 高 |
| 自托管部署（无Synth） | 永久 fallback | 极高 |

**代码路径**:
```ruby
# 路径1: Money对象转换
Money#exchange_to(fallback_rate: 1)
  └── ExchangeRate.find_or_fetch_rate → nil
        └── 使用 fallback_rate → Money.new(amount * 1, T)

# 路径2: SQL聚合查询
SELECT ...
FROM accounts
LEFT JOIN exchange_rates ON ...
  └── er.rate IS NULL → COALESCE → 1.0
        └── balance * 1 → 原值输出
```

---

### 2.5 四条路径决策流程图

```
                           ┌─────────────────────────────────┐
                           │  发起货币转换请求               │
                           │  (from, to, date, amount)      │
                           └───────────────┬─────────────────┘
                                           │
                          ┌────────────────▼────────────────┐
                          │  路径一：数据库精确查询         │
                          │  find_by(from, to, date)       │
                          └───────────────┬────────────────┘
                                          │
                         ┌────────────────┴───────────────┐
                         │                                │
                   有记录│                         无记录 │
                         ▼                                ▼
              ┌───────────────────┐          ┌──────────────────────────┐
              │  返回精确汇率     │          │  路径二：Provider实时拉取 │
              │  误差=0           │          │  Synth API GET /rates     │
              └─────────┬─────────┘          └──────────────┬───────────┘
                        │                                   │
                   成功 │                              失败 │
                        ▼                                   ▼
              ┌───────────────────┐          ┌──────────────────────────┐
              │  路径三：写回DB   │          │  路径四：Fallback兜底    │
              │  find_or_create_by│          │  fallback_rate = 1.0     │
              │  提升后续命中率   │          │  误差 -20% ~ +20%        │
              └───────────────────┘          └──────────────────────────┘
```

---

## 三、关键边界与风险总结

### 3.1 同步窗口风险点

| 风险 | 概率 | 影响 | 规避方式 |
|-----|------|------|---------|
| 频繁小变更导致全量合并 | 中 | 性能下降 | 合并阈值限制 |
| 同步进行中变更丢失 | 低 | 数据延迟 | 设计上可接受，下次同步修正 |
| 父子同步窗口不一致 | 低 | 范围不匹配 | 严格下传不修改 |
| Entry仅传start_date导致范围过大 | 中 | 多余计算 | 可优化：也传入end_date |

### 3.2 汇率处理风险点

| 风险 | 概率 | 影响 | 规避方式 |
|-----|------|------|---------|
| Fallback长期存在 | 中（自托管） | 净资产失真 | 定期健康检查 + 告警 |
| 跨时区日期错位 | 低 | 汇率应用错误 | 统一UTC日期标准 |
| LOCF跨度过大 | 中（节假日） | 微小误差 | 市场休市本身无波动 |
| ConversionError未捕获 | 极低 | 页面崩溃 | 关键路径统一fallback |

### 3.3 性能优化路径

1. **合并去重**: 1分钟内N次变更 → 1次同步Job（已实现）
2. **窗口收窄**: Entry变更仅从变更日期开始重算（已实现）
3. **汇率预取**: ImportMarketDataJob批量填充（已实现）
4. **SQL层fallback**: 避免抛出异常直接计算（已实现）

---

## 附录：调用点索引表

| 模块 | 调用点数量 | 关键文件 |
|-----|-----------|---------|
| Account | 15 | `app/models/account.rb`, `app/models/entry.rb` |
| Family | 8 | `app/models/import.rb`, `app/controllers/accounts_controller.rb` |
| PlaidItem | 6 | `app/models/plaid_item/webhook_processor.rb` |
| Job/Syncable | 2 | `app/models/concerns/syncable.rb`, `app/jobs/sync_job.rb` |
| **总计** | **31** | |

| 汇率路径 | 实现位置 |
|---------|---------|
| 数据库命中 | `app/models/exchange_rate/provided.rb:11` |
| Provider拉取 | `app/models/exchange_rate/provided.rb:16` |
| 缓存写回 | `app/models/exchange_rate/provided.rb:21-26` |
| Fallback兜底 | `lib/money.rb:49`, 多个SQL查询 |
