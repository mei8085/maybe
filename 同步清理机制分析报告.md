# 同步清理机制分析报告

## 1. 概述

本报告深入分析了系统中余额表（Balance）、交易（Transaction）与汇率（ExchangeRate）三个核心模型的数据清理策略，包括触发条件、执行顺序、数据一致性保障机制以及失败时的降级与重试路径。

---

## 2. 清理策略触发条件

### 2.1 余额表（Balance）清理触发条件

| 触发场景 | 触发时机 | 触发条件 | 代码位置 |
|---------|---------|---------|---------|
| 账户同步时 | 余额计算完成后 | `materialize_balances` 事务中 | `app/models/balance/materializer.rb:17` |
| 手动清除缓存 | 用户触发缓存清理 | `DataCacheClearJob` 执行时 | `app/jobs/data_cache_clear_job.rb:9` |
| 账户删除 | 账户销毁时 | 级联删除 `dependent: :destroy` | `app/models/account.rb:15` |

**核心触发逻辑 - `purge_stale_balances`**:
```ruby
def purge_stale_balances
  sorted_balances = @balances.sort_by(&:date)
  oldest_calculated_balance_date = sorted_balances.first&.date
  newest_calculated_balance_date = sorted_balances.last&.date
  deleted_count = account.balances.delete_by("date < ? OR date > ?", 
    oldest_calculated_balance_date, newest_calculated_balance_date)
end
```
*`app/models/balance/materializer.rb:74-80`*

### 2.2 交易（Transaction）清理触发条件

| 触发场景 | 触发时机 | 触发条件 | 代码位置 |
|---------|---------|---------|---------|
| **Plaid 同步清理** | 交易同步处理器执行时 | Plaid API 返回 `removed` 交易列表 | `app/models/plaid_account/transactions/processor.rb:17-21` |
| 账户删除 | 账户销毁时 | 级联删除通过 Entry 模型 | `app/models/account.rb:10` |
| 导入回滚 | 导入撤销时 | `revert_import` 操作 | `app/jobs/revert_import_job.rb` |
| 单条删除 | 用户手动删除 | Entry 销毁时级联 | `app/models/entry.rb:10` |
| 证券删除清理 | 证券健康检查失败时 | 删除关联交易数据 | `app/models/holding.rb:56` |

**Plaid 交易清理核心逻辑**:
```ruby
def process
  # 第一步：处理新增/修改交易（逐个事务，避免长时锁）
  modified_transactions.each do |transaction|
    PlaidEntry::Processor.new(...).process
  end

  # 第二步：批量删除被移除的交易（单一事务保证原子性）
  PlaidAccount.transaction do
    removed_transactions.each do |transaction|
      remove_plaid_transaction(transaction)
    end
  end
end

def remove_plaid_transaction(raw_transaction)
  account.entries.find_by(plaid_id: raw_transaction["transaction_id"])&.destroy
end
```
*`app/models/plaid_account/transactions/processor.rb:6-47`*

### 2.3 汇率（ExchangeRate）清理触发条件

| 触发场景 | 触发时机 | 触发条件 | 代码位置 |
|---------|---------|---------|---------|
| 缓存清除 | 数据缓存清理任务 | `DataCacheClearJob` 执行时 | `app/jobs/data_cache_clear_job.rb:6` |
| 强制刷新 | 市场数据导入时 | `clear_cache: true` 参数 | `app/models/exchange_rate/importer.rb:5` |
| 定时同步 | 每日市场数据同步 | `ImportMarketDataJob` 定时任务 | `config/schedule.yml:1-8` |

**汇率清理关键参数 - `clear_cache`**:
- `clear_cache: true` - 覆盖现有汇率数据（优先使用 provider 数据）
- `clear_cache: false` - 仅填充缺失的汇率数据（优先使用 DB 数据）

---

## 3. 清理操作执行顺序

### 3.1 完整同步主链流程（含交易清理）

```
┌───────────────────────────────────────────────────────────────────────┐
│                        完整同步主链流程                                   │
└───────────────────────────────────────────────────────────────────────┘
                                    ↓
0. SyncCleanerJob (每小时执行)
   └─ 标记超过24小时未完成的同步为 stale
                                    ↓
1. Family#sync_later (家庭级同步触发)
   └─ Family::Syncer#perform_sync
       ├─ 应用规则引擎
       └─ 调度子同步（PlaidItem + 手工账户）
                                    ↓
2. PlaidItem#sync_later (Plaid 数据源同步)
   └─ PlaidItem::Importer#import
       ├─ 获取账户基本信息
       └─ PlaidItem.transaction 包裹
           ├─ PlaidAccount::Importer#import (逐个账户导入)
           │   └─ PlaidAccount::Transactions::Processor#process
           │       ├─ 【步骤 A】逐个处理 modified_transactions (单事务)
           │       └─ 【步骤 B】PlaidAccount.transaction 批量删除 removed_transactions ← 交易清理
           └─ 更新 next_cursor（增量同步标记）
                                    ↓
3. Account#sync_later (账户余额同步)
   └─ Account::Syncer#perform_sync
       ├─ import_market_data (导入市场数据)
       │   └─ MarketDataImporter#import_exchange_rates
       │       └─ ExchangeRate::Importer#import_provider_rates
       │           └─ 【批次 1..N】每批 200 条独立 upsert ← 汇率分批次写入
       └─ materialize_balances (余额计算，Balance.transaction 包裹)
           ├─ materialize_holdings (持仓计算)
           │   └─ purge_stale_holdings (清理过期持仓)
           ├─ calculate_balances (计算余额)
           ├─ persist_balances (持久化余额)
           └─ purge_stale_balances (清理过期余额) ← 余额清理
                                    ↓
4. Family#perform_post_sync (后同步处理)
   └─ auto_match_transfers! (跨账户转账自动匹配)
```

### 3.2 交易清理执行时序详解

| 阶段 | 操作 | 事务范围 | 一致性约束 |
|-----|------|---------|-----------|
| **前置阶段** | 处理新增/修改交易 | **每条交易独立事务** | 避免长时数据库锁，容忍部分失败 |
| **清理阶段** | 删除 Plaid 标记为 removed 的交易 | **PlaidAccount 单事务** | 所有删除原子化，要么全删要么全不删 |
| **后置阶段** | 账户余额重新计算 | **Balance 单事务** | 基于最新交易数据重算，保证余额一致性 |

**关键设计考量**:
- 新增/修改交易使用单条事务：避免批量处理时持有长时锁影响其他用户
- 删除交易使用批量事务：保证被移除交易的清理原子性
- 交易清理先于余额计算：确保余额基于最新交易状态计算

### 3.3 数据缓存清除执行顺序

```
DataCacheClearJob#perform (ActiveRecord::Base.transaction 包裹)
  ├─ ExchangeRate.delete_all          ← 第一步：删除所有汇率
  ├─ Security::Price.delete_all        ← 第二步：删除所有证券价格
  ├─ 遍历所有账户
  │   ├─ account.balances.delete_all   ← 第三步：删除账户余额
  │   └─ account.holdings.delete_all   ← 第四步：删除账户持仓
  └─ family.sync_later                 ← 第五步：触发重新同步
```
*`app/jobs/data_cache_clear_job.rb:4-15`*

---

## 4. 数据一致性保障机制

### 4.1 事务保障的真实边界

> **重要修正**：并非所有清理都在同一事务内！各模型的事务范围差异显著：

| 模型 | 事务范围 | 清理操作事务归属 | 风险等级 |
|-----|---------|----------------|---------|
| **Balance** | `Balance.transaction` 单事务包裹 | ✅ 余额计算 + 余额清理 + 持仓清理 在同一事务 | 低 |
| **Transaction** | 分两级：单条事务 + PlaidAccount 事务 | ⚠️ 删除操作在同一事务，但新增/修改是独立事务 | 中 |
| **ExchangeRate** | **无外层事务**，每批独立执行 | ❌ 每批 200 条是独立 upsert，跨批次无事务保障 | 高 |

**余额表单事务保障**:
```ruby
def materialize_balances
  Balance.transaction do  # 单一事务包裹全部操作
    materialize_holdings
    calculate_balances
    persist_balances
    purge_stale_balances  # 与计算在同一事务，保证一致性
    update_account_info if strategy == :forward
  end
end
```
*`app/models/balance/materializer.rb:9-23`*

### 4.2 原子性操作 - Upsert 机制

| 模型 | Upsert 唯一约束 | 批次大小 | 事务边界 | 代码位置 |
|-----|----------------|---------|---------|---------|
| Balance | `[:account_id, :date, :currency]` | 全部一次 | Balance.transaction | `app/models/balance/materializer.rb:60-71` |
| Holding | `[:account_id, :security_id, :date, :currency]` | 全部一次 | Balance.transaction | `app/models/holding/materializer.rb:32-37` |
| ExchangeRate | `[:from_currency, :to_currency, :date]` | **每批 200 条** | **无外层事务** | `app/models/exchange_rate/importer.rb:72-76` |

**汇率分批次写入实现**:
```ruby
def upsert_rows(rows)
  batch_size = 200
  
  total_upsert_count = 0
  
  rows.each_slice(batch_size) do |batch|
    # 每批都是独立的 upsert_all，没有外层事务包裹！
    upserted_ids = ExchangeRate.upsert_all(
      batch,
      unique_by: %i[from_currency to_currency date],
      returning: [ "id" ]
    )
    
    total_upsert_count += upserted_ids.count
  end
  
  total_upsert_count
end
```
*`app/models/exchange_rate/importer.rb:66-82`*

### 4.3 汇率分批次写入的部分落库风险

**风险场景描述**:
当导入 N 天汇率数据时，数据按每 200 条分批写入。如果第 K 批写入失败（如网络中断、数据库连接断开、死锁等）：
- 批次 1..K-1：**已成功落库**，数据持久化
- 批次 K..N：**未写入**，数据丢失
- 结果：汇率数据出现断层，中间某些日期缺失

**风险传导路径**:
```
汇率分批次写入失败
    ↓
    ├─ 部分日期汇率已落库，后续日期缺失
    ├─ 余额计算时对缺失汇率使用 fallback_rate = 1
    │   └─ 导致外币账户余额计算出现系统性偏差
    └─ 持仓估值时汇率换算不准确
        └─ 投资收益统计失真
```

**失败后的恢复路径**:

| 恢复机制 | 触发条件 | 恢复逻辑 | 效果 |
|---------|---------|---------|------|
| **幂等重试** | 下一次定时同步触发 | `clear_cache: false` 模式下，DB 已有数据保留，缺失数据从 provider 重新获取 | ✅ 自动填补断层，无需人工干预 |
| **强制刷新** | 用户手动触发缓存清理 | `DataCacheClearJob` 删除全部汇率后重新全量同步 | ✅ 彻底重建，但同步成本高 |
| **LOCF 降级** | 余额计算实时触发 | 使用最近有效汇率向前填充 | ⚠️ 临时缓解，但数据存在偏差 |

**关键恢复保障 - 幂等性设计**:
```ruby
# clear_cache = false 时的优先级策略：DB 已有数据优先
chosen_rate = if clear_cache
  provider_rate_value || db_rate_value   # 强制刷新：provider 优先
else
  db_rate_value || provider_rate_value   # 正常模式：DB 优先，缺失才补
end
```
*`app/models/exchange_rate/importer.rb:39-43`*

> 由于幂等设计，部分落库不是致命问题——下一次同步只会"补缺"，不会重复写入或覆盖正确数据。这是分批次设计的核心考量：**牺牲事务原子性，换取数据库并发性和内存友好性**。

### 4.4 数据库级别约束

```ruby
# ExchangeRate 唯一性约束
validates :date, uniqueness: { scope: %i[from_currency to_currency] }
# app/models/exchange_rate.rb:5

# Balance 存在性约束
validates :account, :date, :balance, presence: true
# app/models/balance.rb:6
```

### 4.5 级联删除保障

```ruby
class Account < ApplicationRecord
  has_many :entries, dependent: :destroy
  has_many :transactions, through: :entries, source: :entryable, source_type: "Transaction"
  has_many :holdings, dependent: :destroy
  has_many :balances, dependent: :destroy
end
# app/models/account.rb:10,14,15
```

---

## 5. 降级与重试路径

### 5.1 同步状态机与降级策略

**Sync 状态机定义**:
```ruby
aasm column: :status, timestamps: true do
  state :pending, initial: true
  state :syncing
  state :completed
  state :failed
  state :stale  # 降级状态：超过24小时未完成

  event :mark_stale do
    transitions from: %i[pending syncing], to: :stale
  end
end
```
*`app/models/sync.rb:27-52`*

**Stale 清理触发**:
```ruby
class << self
  def clean
    incomplete.where("syncs.created_at < ?", STALE_AFTER.ago).find_each(&:mark_stale!)
  end
end
```
*`app/models/sync.rb:54-58`*

### 5.2 汇率获取降级策略

**策略 1：LOCF (Last Observation Carried Forward)**
```ruby
gapfilled_rates = effective_start_date.upto(end_date).map do |date|
  db_rate_value = db_rates[date]&.rate
  provider_rate_value = provider_rates[date]&.rate
  
  chosen_rate = if clear_cache
    provider_rate_value || db_rate_value
  else
    db_rate_value || provider_rate_value
  end

  # LOCF 降级：使用前一个有效汇率
  if chosen_rate.nil?
    chosen_rate = prev_rate_value
  end
  
  prev_rate_value = chosen_rate
end
```
*`app/models/exchange_rate/importer.rb:35-58`*

**策略 2：Provider 错误降级**
```ruby
def import_market_data
  Account::MarketDataImporter.new(account).import_all
rescue => e
  # 降级：即使市场数据同步失败，也不影响整个账户同步
  Rails.logger.error("Error syncing market data for account #{account.id}: #{e.message}")
  Sentry.capture_exception(e)
end
```
*`app/models/account/syncer.rb:31-36`*

### 5.3 任务重试机制

**全局重试配置**:
```ruby
class ApplicationJob < ActiveJob::Base
  retry_on ActiveRecord::Deadlocked  # 死锁自动重试
end
```
*`app/jobs/application_job.rb:2`*

**销毁任务失败降级**:
```ruby
class DestroyJob < ApplicationJob
  def perform(model)
    model.destroy
  rescue => e
    # 降级：重置状态让用户可以重新尝试
    model.update!(scheduled_for_deletion: false)
  end
end
```
*`app/jobs/destroy_job.rb:4-8`*

**账户销毁降级**:
```ruby
def destroy
  super
rescue => e
  # 如果销毁失败，回退到 disabled 状态
  disable! if may_disable?
  raise e
end
```
*`app/models/account.rb:97-104`*

### 5.4 汇率计算降级

```ruby
converted_entry.amount = converted_entry.amount_money.exchange_to(
  account.currency,
  date: e.date,
  fallback_rate: 1  # 降级：找不到汇率时使用 1:1
).amount
```
*`app/models/balance/sync_cache.rb:22-28`*

---

## 6. 定时任务调度配置

| 任务名称 | Cron 表达式 | 执行频率 | 描述 |
|---------|------------|---------|------|
| `clean_syncs` | `0 * * * *` | 每小时 | 清理过期同步状态 |
| `import_market_data` | `0 22 * * 1-5` | 工作日 22:00 UTC | 导入市场数据（汇率、证券价格） |

*配置文件: `config/schedule.yml`*

---

## 7. 总结

### 7.1 清理机制设计原则

1. **事务分级保障**：余额强一致性单事务，汇率分批次牺牲原子性换取并发性
2. **渐进式降级**：从 LOCF 到 fallback_rate，多层降级保障
3. **状态机管理**：使用 AASM 状态机管理同步生命周期
4. **后台异步**：所有重量级操作通过 Sidekiq 异步执行
5. **幂等设计**：所有写入操作支持重试，避免部分失败造成数据错乱

### 7.2 关键设计模式与权衡

| 模式/权衡 | 应用场景 | 收益 | 代价 |
|-----------|---------|------|------|
| **Upsert 批量写入** | 余额、持仓、汇率数据更新 | 高性能，避免重复 | 需维护唯一约束 |
| **LOCF 缺失值填充** | 汇率周末/节假日数据缺失 | 用户无感知 | 数据存在微小偏差 |
| **状态机管理** | Sync 生命周期状态流转 | 状态清晰可追溯 | 状态转换复杂度 |
| **分批次无事务** | 汇率大批量写入 | 内存友好，不持有长时锁 | 部分落库风险 |
| **单条独立事务** | 新增/修改交易处理 | 避免锁竞争 | 部分失败不影响整体 |

### 7.3 故障恢复路径总览

```
同步失败
   ↓
   ├─ 标记为 failed 状态
   ├─ 记录错误信息到 sync.error
   ├─ Sentry 异常告警
   └─ 用户可通过重新触发 sync_later 重试

24小时未完成
   ↓
   └─ SyncCleanerJob 标记为 stale

汇率分批写入失败
   ↓
   ├─ 前面批次已落库，后面批次丢失
   └─ 下次定时同步以 DB 优先模式补缺（幂等恢复）

交易清理失败
   ↓
   ├─ PlaidAccount 事务回滚，删除全部撤销
   └─ 保留已处理的新增/修改交易（独立事务已提交）

销毁失败
   ↓
   ├─ 重置 scheduled_for_deletion 标志
   └─ 账户状态回退为 disabled
```
