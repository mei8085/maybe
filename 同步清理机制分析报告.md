# 同步清理机制分析报告

## 1. 概述

本报告深入分析了系统中余额表（Balance）、交易（Transaction）与汇率（ExchangeRate）三个核心模型的数据清理策略，包括触发条件、执行顺序、数据一致性保障机制以及失败时的降级与重试路径。

---

## 2. 清理策略触发条件

### 2.1 余额表（Balance）清理触发条件

| 触发场景 | 触发时机 | 触发条件 | 代码位置 |
|---------|---------|---------|---------|
| 账户同步时 | 余额计算完成后 | `materialize_balances` 事务中 | `app/models/balance/materializer.rb:17` |
| 手动清除缓存 | 用户触发缓存清理 | `DataCacheClearJob` 执行时 | `app/jobs/data_cache_clear_job.rb:9` |
| 账户删除 | 账户销毁时 | 级联删除 `dependent: :destroy` | `app/models/account.rb:15` |

**核心触发逻辑 - `purge_stale_balances`**:
```ruby
def purge_stale_balances
  sorted_balances = @balances.sort_by(&:date)
  oldest_calculated_balance_date = sorted_balances.first&.date
  newest_calculated_balance_date = sorted_balances.last&.date
  deleted_count = account.balances.delete_by("date < ? OR date > ?", 
    oldest_calculated_balance_date, newest_calculated_balance_date)
end
```
*`app/models/balance/materializer.rb:74-80`*

### 2.2 交易（Transaction）清理触发条件

| 触发场景 | 触发时机 | 触发条件 | 代码位置 |
|---------|---------|---------|---------|
| 账户删除 | 账户销毁时 | 级联删除通过 Entry 模型 | `app/models/account.rb:10` |
| 导入回滚 | 导入撤销时 | `revert_import` 操作 | `app/jobs/revert_import_job.rb` |
| 单条删除 | 用户手动删除 | Entry 销毁时级联 | `app/models/entry.rb:10` |

### 2.3 汇率（ExchangeRate）清理触发条件

| 触发场景 | 触发时机 | 触发条件 | 代码位置 |
|---------|---------|---------|---------|
| 缓存清除 | 数据缓存清理任务 | `DataCacheClearJob` 执行时 | `app/jobs/data_cache_clear_job.rb:6` |
| 强制刷新 | 市场数据导入时 | `clear_cache: true` 参数 | `app/models/exchange_rate/importer.rb:5` |
| 定时同步 | 每日市场数据同步 | `ImportMarketDataJob` 定时任务 | `config/schedule.yml:1-8` |

**汇率清理关键参数 - `clear_cache`**:
- `clear_cache: true` - 覆盖现有汇率数据（优先使用 provider 数据）
- `clear_cache: false` - 仅填充缺失的汇率数据（优先使用 DB 数据）

---

## 3. 清理操作执行顺序

### 3.1 完整同步流程中的清理顺序

```
┌─────────────────────────────────────────────────────────┐
│                    Sync 触发流程                          │
└─────────────────────────────────────────────────────────┘
                              ↓
1. SyncCleanerJob (每小时执行)
   └─ 标记超过24小时未完成的同步为 stale
                              ↓
2. Account#sync_later (账户同步)
   └─ Account::Syncer#perform_sync
       ├─ import_market_data (导入市场数据)
       │   └─ MarketDataImporter#import_exchange_rates
       │       └─ 汇率数据 upsert (含可选清理)
       └─ materialize_balances (余额计算)
           ├─ materialize_holdings (持仓计算)
           │   └─ purge_stale_holdings (清理过期持仓)
           ├─ calculate_balances (计算余额)
           ├─ persist_balances (持久化余额)
           └─ purge_stale_balances (清理过期余额) ← 余额清理
                              ↓
3. Family#perform_post_sync (家庭后同步)
   └─ auto_match_transfers! (自动匹配转账)
```

### 3.2 数据缓存清除执行顺序

```
DataCacheClearJob#perform
  ├─ ExchangeRate.delete_all          ← 第一步：删除所有汇率
  ├─ Security::Price.delete_all        ← 第二步：删除所有证券价格
  ├─ 遍历所有账户
  │   ├─ account.balances.delete_all   ← 第三步：删除账户余额
  │   └─ account.holdings.delete_all   ← 第四步：删除账户持仓
  └─ family.sync_later                 ← 第五步：触发重新同步
```
*`app/jobs/data_cache_clear_job.rb:4-15`*

---

## 4. 数据一致性保障机制

### 4.1 数据库事务保障

**余额表事务**:
```ruby
def materialize_balances
  Balance.transaction do  # 开启事务
    materialize_holdings
    calculate_balances
    persist_balances
    purge_stale_balances  # 在同一事务中清理
    update_account_info if strategy == :forward
  end
end
```
*`app/models/balance/materializer.rb:9-23`*

### 4.2 原子性操作 - Upsert 机制

| 模型 | Upsert 唯一约束 | 代码位置 |
|-----|----------------|---------|
| Balance | `[:account_id, :date, :currency]` | `app/models/balance/materializer.rb:60-71` |
| Holding | `[:account_id, :security_id, :date, :currency]` | `app/models/holding/materializer.rb:32-37` |
| ExchangeRate | `[:from_currency, :to_currency, :date]` | `app/models/exchange_rate/importer.rb:72-76` |

**余额表 Upsert 示例**:
```ruby
account.balances.upsert_all(
  @balances.map { |b| b.attributes.slice(...).merge("updated_at" => current_time) },
  unique_by: %i[account_id date currency]
)
```

### 4.3 数据库级别约束

```ruby
# ExchangeRate 唯一性约束
validates :date, uniqueness: { scope: %i[from_currency to_currency] }
# app/models/exchange_rate.rb:5

# Balance 存在性约束
validates :account, :date, :balance, presence: true
# app/models/balance.rb:6
```

### 4.4 级联删除保障

```ruby
class Account < ApplicationRecord
  has_many :entries, dependent: :destroy
  has_many :transactions, through: :entries, source: :entryable, source_type: "Transaction"
  has_many :holdings, dependent: :destroy
  has_many :balances, dependent: :destroy
end
# app/models/account.rb:10,14,15
```

---

## 5. 降级与重试路径

### 5.1 同步状态机与降级策略

**Sync 状态机定义**:
```ruby
aasm column: :status, timestamps: true do
  state :pending, initial: true
  state :syncing
  state :completed
  state :failed
  state :stale  # 降级状态：超过24小时未完成

  event :mark_stale do
    transitions from: %i[pending syncing], to: :stale
  end
end
```
*`app/models/sync.rb:27-52`*

**Stale 清理触发**:
```ruby
class << self
  def clean
    incomplete.where("syncs.created_at < ?", STALE_AFTER.ago).find_each(&:mark_stale!)
  end
end
```
*`app/models/sync.rb:54-58`*

### 5.2 汇率获取降级策略

**策略 1：LOCF (Last Observation Carried Forward)**
```ruby
gapfilled_rates = effective_start_date.upto(end_date).map do |date|
  db_rate_value = db_rates[date]&.rate
  provider_rate_value = provider_rates[date]&.rate
  
  chosen_rate = if clear_cache
    provider_rate_value || db_rate_value
  else
    db_rate_value || provider_rate_value
  end

  # LOCF 降级：使用前一个有效汇率
  if chosen_rate.nil?
    chosen_rate = prev_rate_value
  end
  
  prev_rate_value = chosen_rate
end
```
*`app/models/exchange_rate/importer.rb:35-58`*

**策略 2：Provider 错误降级**
```ruby
def import_market_data
  Account::MarketDataImporter.new(account).import_all
rescue => e
  # 降级：即使市场数据同步失败，也不影响整个账户同步
  Rails.logger.error("Error syncing market data for account #{account.id}: #{e.message}")
  Sentry.capture_exception(e)
end
```
*`app/models/account/syncer.rb:31-36`*

### 5.3 任务重试机制

**全局重试配置**:
```ruby
class ApplicationJob < ActiveJob::Base
  retry_on ActiveRecord::Deadlocked  # 死锁自动重试
end
```
*`app/jobs/application_job.rb:2`*

**销毁任务失败降级**:
```ruby
class DestroyJob < ApplicationJob
  def perform(model)
    model.destroy
  rescue => e
    # 降级：重置状态让用户可以重新尝试
    model.update!(scheduled_for_deletion: false)
  end
end
```
*`app/jobs/destroy_job.rb:4-8`*

**账户销毁降级**:
```ruby
def destroy
  super
rescue => e
  # 如果销毁失败，回退到 disabled 状态
  disable! if may_disable?
  raise e
end
```
*`app/models/account.rb:97-104`*

### 5.4 汇率计算降级

```ruby
converted_entry.amount = converted_entry.amount_money.exchange_to(
  account.currency,
  date: e.date,
  fallback_rate: 1  # 降级：找不到汇率时使用 1:1
).amount
```
*`app/models/balance/sync_cache.rb:22-28`*

---

## 6. 定时任务调度配置

| 任务名称 | Cron 表达式 | 执行频率 | 描述 |
|---------|------------|---------|------|
| `clean_syncs` | `0 * * * *` | 每小时 | 清理过期同步状态 |
| `import_market_data` | `0 22 * * 1-5` | 工作日 22:00 UTC | 导入市场数据（汇率、证券价格） |

*配置文件: `config/schedule.yml`*

---

## 7. 总结

### 7.1 清理机制设计原则

1. **事务一致性**：所有清理操作在数据库事务内执行，确保原子性
2. **渐进式降级**：从 LOCF 到 fallback_rate，多层降级保障
3. **状态机管理**：使用 AASM 状态机管理同步生命周期
4. **后台异步**：所有重量级操作通过 Sidekiq 异步执行

### 7.2 关键设计模式

| 模式 | 应用场景 |
|-----|---------|
| **Upsert 批量写入** | 余额、持仓、汇率数据更新 |
| **LOCF 缺失值填充** | 汇率周末/节假日数据缺失 |
| **状态机管理** | Sync 生命周期状态流转 |
| **级联删除** | 账户删除时关联数据清理 |

### 7.3 故障恢复路径

```
同步失败
   ↓
   ├─ 标记为 failed 状态
   ├─ 记录错误信息到 sync.error
   ├─ Sentry 异常告警
   └─ 用户可通过重新触发 sync_later 重试

24小时未完成
   ↓
   └─ SyncCleanerJob 标记为 stale

销毁失败
   ↓
   ├─ 重置 scheduled_for_deletion 标志
   └─ 账户状态回退为 disabled
```
