# 净资产聚合链路薄弱点补充分析

## 一、同步 Job 触发业务动作及窗口合并下传规则

### 1.1 触发同步的业务动作全景

#### 1.1.1 账户层级 (Account)

| 业务场景 | 触发位置 | 窗口参数 | 说明 |
|---------|---------|---------|------|
| **账户创建** | `Account.create_and_sync` | 无窗口（全量同步） | 创建账户后立即触发同步计算余额历史 |
| **条目增删改** | `Entry#sync_account_later` | `window_start_date: [date_was, date].min` | 交易/估值/交易变更时，从变更日期开始重算 |
| **持仓删除** | `Holding#destroy_holding_and_entries!` | 无窗口 | 删除持仓及关联条目后触发同步 |
| **锚定操作** | `Account::Anchorable` | 无窗口 | 设置/移除余额锚点后触发重算 |
| **对账操作** | `Account::Reconcileable` | 无窗口 | 对账成功后触发余额重算 |
| **手动更新** | 控制器层 | 无窗口 | 用户手动编辑账户信息后触发 |

#### 1.1.2 家庭层级 (Family)

| 业务场景 | 触发位置 | 窗口参数 | 说明 |
|---------|---------|---------|------|
| **导入操作** | `Import#family.sync_later` | 无窗口 | 数据导入完成后触发全家同步 |
| **自动同步** | `AutoSync` concern | 无窗口 | 定期或手动触发全家数据同步 |
| **数据清除** | `DataCacheClearJob` | 无窗口 | 清除缓存后重新计算 |
| **重置操作** | `FamilyResetJob` | 无窗口 | 家庭数据重置后同步 |

#### 1.1.3 Plaid 集成层级 (PlaidItem)

| 业务场景 | 触发位置 | 窗口参数 | 说明 |
|---------|---------|---------|------|
| **Webhook触发** | `PlaidItem::WebhookProcessor` | 无窗口 | Plaid推送数据变更时触发 |
| **手动刷新** | `PlaidItemsController` | 无窗口 | 用户手动刷新Plaid数据 |
| **数据导入后** | `PlaidItem::Syncer` | 继承父同步窗口 | 拉取Plaid数据后调度账户同步 |

#### 1.1.4 交易相关场景

| 业务场景 | 触发位置 | 窗口参数 | 说明 |
|---------|---------|---------|------|
| **转账匹配** | `Transfer::Creator` | 无窗口 | 创建转账后同步源/目标账户 |
| **交易创建** | `Trade::CreateForm` | 无窗口 | 创建/更新/删除交易后同步 |
| **批量删除** | `Transactions::BulkDeletionsController` | 无窗口 | 批量删除交易后逐一同步账户 |

---

### 1.2 窗口合并规则详解

#### 1.2.1 核心合并逻辑

**位置**: `Sync#expand_window_if_needed`

```ruby
def expand_window_if_needed(new_window_start_date, new_window_end_date)
  return unless pending?  # 仅pending状态可合并
  return if self.window_start_date.nil? && self.window_end_date.nil?  # 已是全量窗口

  # start_date 取最小值（更早的日期 = 更宽的窗口）
  earliest_start_date = if self.window_start_date && new_window_start_date
    [ self.window_start_date, new_window_start_date ].min
  else
    nil  # 任一为nil则全量开始
  end

  # end_date 取最大值（更晚的日期 = 更宽的窗口）
  latest_end_date = if self.window_end_date && new_window_end_date
    [ self.window_end_date, new_window_end_date ].max
  else
    nil  # 任一为nil则全量结束
  end

  update(window_start_date: earliest_start_date, window_end_date: latest_end_date)
end
```

#### 1.2.2 合并语义说明

| 现有窗口 | 新窗口 | 合并结果 | 语义 |
|---------|--------|---------|------|
| `(nil, nil)` | 任意 | `(nil, nil)` | 全量同步保持不变 |
| `(A, nil)` | `(B, nil)` | `(min(A,B), nil)` | 开始日期向前扩展，结束无限 |
| `(nil, B)` | `(nil, C)` | `(nil, max(B,C))` | 开始无限，结束日期向后扩展 |
| `(A, B)` | `(C, D)` | `(min(A,C), max(B,D))` | 双向扩展取最大范围 |

**关键边界**:
- `nil` 代表"无限"，不是"无"
- 仅 `pending` 状态同步可合并，`syncing` 状态不接受合并
- 合并操作在 `Syncable#sync_later` 的事务锁内执行，保证原子性

---

### 1.3 窗口下传层级规则

#### 1.3.1 同步层级树

```
Family Sync (level 0)
  ├── PlaidItem Sync (level 1)
  │   └── Account Sync (level 2)
  │       ├── Balance Calculation
  │       └── Holding Calculation
  └── Manual Account Sync (level 1)
      ├── Balance Calculation
      └── Holding Calculation
```

#### 1.3.2 窗口传递路径

**路径1: Family → PlaidItem → Account**

```ruby
# Family::Syncer#perform_sync
child_syncables.each do |syncable|
  syncable.sync_later(
    parent_sync: sync,
    window_start_date: sync.window_start_date,  # 原样传递
    window_end_date: sync.window_end_date      # 原样传递
  )
end

# PlaidItem::Syncer#perform_sync → schedule_account_syncs
accounts.each do |account|
  account.sync_later(
    parent_sync: sync,
    window_start_date: sync.window_start_date,  # 继续原样传递
    window_end_date: sync.window_end_date
  )
end
```

**路径2: Entry变更 → Account**

```ruby
# Entry#sync_account_later
sync_start_date = [ date_previously_was, date ].compact.min
account.sync_later(window_start_date: sync_start_date)
# 注意：仅传递 start_date，end_date 为 nil（全量到今天）
```

#### 1.3.3 窗口下传设计原则

1. **父传子，子不传父**：窗口仅向下传递，子同步完成不影响父同步窗口
2. **最小化计算**：Entry变更仅传递变更日期作为起点，避免不必要全量计算
3. **全量兜底**：无窗口参数时默认为全量同步，保证数据一致性
4. **兄弟独立**：同一父同步下的多个子同步各自独立，窗口互不影响

---

## 二、金额换算边界场景详解

### 2.1 无汇率场景处理

#### 2.1.1 汇率查找链路

```
Money#exchange_to(from, to, date)
  └── ExchangeRate.find_or_fetch_rate(from, to, date)
        ├── DB查找 (from_currency, to_currency, date)
        │   └── 命中 → 返回汇率
        └── 未命中
            ├── Provider存在?
            │   ├── 否 → 返回 nil
            │   └── 是 → 调用 Provider API
            │       ├── 成功 → 缓存并返回
            │       └── 失败 → 返回 nil
            └── 无 Provider → 返回 nil
```

#### 2.1.2 无汇率时的行为分支

**分支1: 有 fallback_rate**

```ruby
# 位置: Balance::SyncCache#converted_entries
converted_entry.amount = converted_entry.amount_money.exchange_to(
  account.currency,
  date: e.date,
  fallback_rate: 1  # ← 提供 fallback
).amount
# 结果：无汇率时使用汇率=1进行转换
```

**分支2: 无 fallback_rate → 抛出异常**

```ruby
# 位置: lib/money.rb:51
raise ConversionError.new(
  from_currency: iso_code,
  to_currency: other_iso_code,
  date: date
) unless exchange_rate
```

#### 2.1.3 无汇率场景分布

| 场景 | 处理方式 | 位置 |
|-----|---------|------|
| **余额计算缓存** | `fallback_rate: 1` | `Balance::SyncCache` |
| **SQL聚合转换** | `COALESCE(er.rate, 1)` | `Balance::ChartSeriesBuilder` |
| **账户汇总查询** | `COALESCE(er.rate, 1)` | `BalanceSheet::AccountTotals` |
| **持仓平均成本** | `COALESCE(er.rate, 1)` | `Holding#avg_cost` |
| **转账自动匹配** | `JOIN + NULLIF` | `Family::AutoTransferMatchable` |
| **Money对象转换** | 抛出 `ConversionError` | `Money#exchange_to` |

---

### 2.2 Fallback 机制深度解析

#### 2.2.1 Fallback 率的语义选择

**Fallback = 1 的含义**:
- 假设两种货币 1:1 等值
- 对于 USD → EUR，1 USD = 1 EUR（实际约 0.92）
- 误差范围：通常在 ±20% 以内（主要货币对）

**为什么选择 1 而不是其他值**:
1. **计算中性**：乘法单位元，金额数值不变
2. **错误友好**：即使汇率错误，资产负债表仍平衡
3. **可追溯**：后续汇率同步后，下次计算自动修正
4. **实现简单**：无需复杂估算逻辑

#### 2.2.2 Fallback 影响评估

**场景示例**:
- 账户A：EUR 1000 余额
- 家庭本位币：USD
- 实际汇率：EUR → USD = 1.08
- Fallback 汇率：1

```
Fallback 转换: 1000 EUR × 1 = 1000 USD
实际应为: 1000 EUR × 1.08 = 1080 USD
误差: -80 USD (-7.4%)
```

**误差传导**:
1. 单个账户余额误差 → 资产总计误差
2. 多币种账户误差叠加 → 净资产总值误差
3. 时间序列图表出现阶梯状跳变（汇率同步时修正）

#### 2.2.3 Fallback 修正时机

**自动修正触发点**:
1. `ImportMarketDataJob` 每日同步（22:00 UTC）
2. Account 同步时 `import_market_data` 补全缺失汇率
3. 页面访问时按需 fetch（`find_or_fetch_rate`）

**修正后效果**:
- 下次余额重算时使用真实汇率
- 历史余额系列数据回溯更新
- 净资产图表跳变恢复正常

---

### 2.3 生效时区边界

#### 2.3.1 时区使用分布

| 场景 | 时区 | 说明 |
|-----|------|------|
| **汇率日期** | 日期类型（无时区） | `exchange_rates.date` 为 `date` 类型 |
| **市场数据同步** | America/New_York (EST/EDT) | 美股/外汇市场收盘时间基于纽约时区 |
| **应用默认** | Rails 配置时区 | `Date.current` / `Time.current` 使用应用时区 |
| **用户操作** | 用户本地时区 | 前端显示转换，后端存储仍用应用时区 |

#### 2.3.2 市场数据同步时区细节

**位置**: `ExchangeRate::Importer#import_provider_rates`

```ruby
# 只在工作日进行完整同步
today_est = Date.current.in_time_zone("America/New_York").to_date
return if today_est.saturday? || today_est.sunday?
```

**时区不一致风险**:
- 应用时区（如 UTC） vs 纽约时区（UTC-5/UTC-4）
- 日期变更时刻不同步可能导致：
  - 周一 00:30 UTC = 周日 19:30 EST → 判定为周日不同步
  - 周五 23:00 UTC = 周五 18:00 EST → 周五收盘数据未同步

#### 2.3.3 汇率日期匹配边界

**SQL 层匹配逻辑** (`Balance::ChartSeriesBuilder`):

```sql
LEFT JOIN LATERAL (
  SELECT er.rate
  FROM exchange_rates er
  WHERE er.from_currency = accounts.currency
    AND er.to_currency = :target_currency
    AND er.date <= d.date    -- 小于等于图表日期
  ORDER BY er.date DESC     -- 取最近的
  LIMIT 1
) er ON TRUE
```

**关键特性**:
- 非精确匹配：使用 `<=` 而非 `=`
- 向前兼容：缺失某天汇率时，使用历史最近汇率
- 无汇率时回退：COALESCE 到 1

---

### 2.4 精度处理机制

#### 2.4.1 存储精度

**数据库层**:
- `balances.balance`: `numeric` 类型（PostgreSQL 任意精度）
- `holdings.amount`: `numeric` 类型
- `exchange_rates.rate`: `numeric` 类型

**应用层**:
```ruby
# lib/money.rb:35
@amount = obj.is_a?(Money) ? obj.amount : BigDecimal(obj.to_s)
# → 所有金额以 BigDecimal 存储，无精度损失
```

#### 2.4.2 计算精度

**算术运算** (`Money::Arithmetic`):
- 加减乘除直接使用 BigDecimal 运算
- 无中间舍入，保持最大精度
- 结果精度为运算数精度的最大值

**汇率转换**:
```ruby
# Money#exchange_to:53
Money.new(amount * exchange_rate, other_iso_code)
# → BigDecimal × BigDecimal = BigDecimal
# → 转换过程无精度损失
```

#### 2.4.3 显示精度

**格式化层** (`Money::Formatting`):
```ruby
# 按货币配置精度显示
{
  precision: currency.default_precision,  # USD=2, JPY=0, etc.
  # ...
}
```

**精度矩阵示例** (来自 `config/currencies.yml`):

| 货币 | ISO | default_precision | 说明 |
|-----|-----|-------------------|------|
| 美元 | USD | 2 | 美分精度 |
| 欧元 | EUR | 2 | 欧分精度 |
| 日元 | JPY | 0 | 无小数 |
| 比特币 | BTC | 8 | Satoshi 精度 |
| 英镑 | GBP | 2 | 便士精度 |

#### 2.4.4 精度舍入边界

**舍入发生时机**:
1. **显示格式化**：用户可见金额按货币精度舍入
2. **导出报表**：CSV/Excel 导出时舍入到显示精度
3. **数据库迁移**：罕见情况下需字段类型调整

**舍入不发生时机** ✅:
- 余额计算中间过程
- 汇率转换计算
- 持仓估值计算
- 净资产聚合汇总
- 所有数据库存储操作

---

## 三、关键风险点与优化建议

### 3.1 同步窗口风险

| 风险 | 影响 | 建议 |
|-----|------|------|
| 频繁小窗口合并导致全量 | 性能下降 | 增加合并阈值，超过N次合并直接设为全量 |
| 窗口传递丢失 end_date | 计算范围过大 | Entry变更时也传递 end_date 约束 |
| 同步状态竞态条件 | 重复创建同步 | 当前已用事务锁保护，风险较低 |

### 3.2 汇率处理风险

| 风险 | 影响 | 建议 |
|-----|------|------|
| Fallback=1 持续时间过长 | 净资产偏离真实值 | 增加缺失汇率告警，加快补全速度 |
| 跨时区日期匹配错误 | 汇率应用错位 | 统一使用纽约时区日期作为汇率基准日 |
| ConversionError 未捕获 | 页面崩溃 | 关键路径统一提供 fallback，顶层救援 |

### 3.3 精度风险

| 风险 | 影响 | 建议 |
|-----|------|------|
| 显示精度误导用户 | 看起来"差一分钱" | 界面注明"显示精度"，悬停显示完整值 |
| 浮点运算潜入 | 累计误差 | 代码审查确保所有金额路径使用 BigDecimal |

---

## 附录: 关键代码索引

| 主题 | 文件路径 |
|-----|---------|
| 同步核心逻辑 | `app/models/concerns/syncable.rb` |
| 窗口合并逻辑 | `app/models/sync.rb:107-127` |
| 汇率查找 | `app/models/exchange_rate/provided.rb:10-28` |
| 货币转换 | `lib/money.rb:42-55` |
| 余额缓存转换 | `app/models/balance/sync_cache.rb:21-45` |
| 精度格式化 | `lib/money/formatting.rb` |
| 货币配置 | `config/currencies.yml` |
