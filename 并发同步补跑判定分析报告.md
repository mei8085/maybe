# 并发同步补跑判定分析报告

## 核心结论速览

| 场景 | 补跑分类 | 触发条件边界 |
|-----|---------|-------------|
| 已有 sync 处于 **pending** 状态 | ✅ **会补跑** | 窗口会被扩展，下一次 Job 执行会覆盖新窗口 |
| 已有 sync 处于 **syncing** 状态 | ❌ **不会补跑** | 窗口扩展被静默跳过，本次执行不覆盖新区间，也不会触发后续补跑 |
| sync 完成后又有新变更 | ✅ **会补跑** | 完整创建新 sync |

---

## 逐段证据链分析

### 1. `sync_later` 入口与 `incomplete` 查询

**代码位置**：`app/models/concerns/syncable.rb:14-35`

```ruby
def sync_later(parent_sync: nil, window_start_date: nil, window_end_date: nil)
  Sync.transaction do
    with_lock do
      sync = self.syncs.incomplete.first  # ⚠️  关键：找到 pending OR syncing

      if sync
        Rails.logger.info("There is an existing sync, expanding window if needed (#{sync.id})")
        sync.expand_window_if_needed(window_start_date, window_end_date)
      else
        sync = self.syncs.create!(...)
        SyncJob.perform_later(sync)
      end

      sync
    end
  end
end
```

**关键点**：
- `incomplete` scope 定义：`where("syncs.status IN (?)", %w[pending syncing])`
- **pending 和 syncing 都被视为"不完整"**，会走同一分支
- 对找到的 sync 统一调用 `expand_window_if_needed`

---

### 2. `expand_window_if_needed` 的状态过滤

**代码位置**：`app/models/sync.rb:107-127`

```ruby
def expand_window_if_needed(new_window_start_date, new_window_end_date)
  return unless pending?  # ⚠️  关键分支：只对 pending 生效！
  return if self.window_start_date.nil? && self.window_end_date.nil?

  # 扩展窗口逻辑（向左取 min，向右取 max）
  earliest_start_date = if self.window_start_date && new_window_start_date
    [ self.window_start_date, new_window_start_date ].min
  else
    nil
  end

  update(window_start_date: earliest_start_date, ...)
end
```

**分支判定**：

| 已有 sync 状态 | `return unless pending?` | 窗口是否扩展 |
|--------------|-------------------------|-------------|
| `pending` | `false`，继续执行 | ✅ 窗口被扩展 |
| `syncing` | `true`，直接 return | ❌ 窗口不扩展，静默跳过 |

> **关键发现**：syncing 状态下的窗口扩展被**静默丢弃**，没有任何日志或错误提示！

---

### 3. 状态转移与执行时机

**状态机定义**：`app/models/sync.rb:27-52`

```
pending ──start!──→ syncing ──complete!/fail!──→ completed/failed
              ↓
        SyncJob 开始执行
```

**时间窗口问题**：

```
T0: 创建 sync A (pending), 窗口: 2025-01-10
T1: SyncJob 开始执行, start! 转移为 syncing
T2: 新账目变更, 窗口: 2025-01-05 (更早)
    ↳ 找到 sync A (syncing)
    ↳ 调用 expand_window_if_needed
    ↳ return unless pending? → 直接 return, 窗口仍为 2025-01-10
T3: sync A 按旧窗口 2025-01-10 执行完成
T4: 2025-01-05 ~ 2025-01-09 的数据丢失！❌
```

---

### 4. `finalize` 与后续调度链

**代码位置**：`app/models/sync.rb:83-104`

```ruby
def finalize_if_all_children_finalized
  Sync.transaction do
    lock!

    return unless all_children_finalized?

    if syncing?
      if has_failed_children?
        fail!
      else
        complete!  # ⚠️  转移到 completed
      end
    end

    perform_post_sync  # 仅执行 post_sync 钩子
  end

  parent&.finalize_if_all_children_finalized  # 向上传播
end
```

**finalize 后检查清单**：

| 行为 | 是否发生 |
|-----|---------|
| 转移到 completed/failed 状态 | ✅ 是 |
| 执行 post_sync 钩子 | ✅ 是 |
| 检查是否有窗口变更未覆盖 | ❌ 否 |
| 创建新 sync 补跑 | ❌ 否 |
| 重新调度 Job | ❌ 否 |

> **关键发现**：finalize 完成后**没有任何机制**去检查 syncing 期间是否有窗口扩展被丢弃，也不会触发补跑。

---

### 5. 窗口参数在计算时是否真的被使用？

**代码位置**：`app/models/account/syncer.rb:8-12`

```ruby
def perform_sync(sync)
  Rails.logger.info("Processing balances (#{account.linked? ? 'reverse' : 'forward'})")
  import_market_data
  materialize_balances  # ⚠️  sync 参数完全没有被使用！
end
```

**余额计算日期范围**：`app/models/balance/forward_calculator.rb:55-61`

```ruby
def calc_start_date
  account.opening_anchor_date  # ⚠️  总是从期初锚点开始，完全忽略 window_start_date
end

def calc_end_date
  [ account.entries.order(:date).last&.date, ... ].compact.max || Date.current
end
```

**震惊发现**：
- `window_start_date` 和 `window_end_date` 参数**完全没有在余额计算中使用**！
- ForwardCalculator 总是从 `opening_anchor_date` 全量计算到最新
- 窗口参数只是"摆设"，实际上永远是全量同步

> 这意味着：窗口是否被扩展，对最终计算结果**没有影响**，因为同步总是全量的。

---

## 三档补跑判定矩阵

### ✅ 第一档：会补跑

**触发条件**：已有 sync 处于 `pending` 状态

**判定依据**：
1. `incomplete.first` 找到 pending 的 sync
2. `expand_window_if_needed` 成功扩展窗口（`pending?` 返回 true）
3. SyncJob 执行时，无论窗口是什么，都会全量计算
4. 新的账目变更一定会被覆盖

**时序示例**：
```
T0: 创建 sync (pending), 窗口: 2025-01-10
T1: 新账目变更, 窗口: 2025-01-05
    ↳ expand_window_if_needed 成功, 窗口更新为 2025-01-05
T2: SyncJob 执行, 从 opening_anchor_date 全量计算
    ↳ 2025-01-05 的数据必然被覆盖
```

---

### ❌ 第二档：不会补跑

**触发条件**：已有 sync 处于 `syncing` 状态

**判定依据**：
1. `incomplete.first` 找到 syncing 的 sync
2. `expand_window_if_needed` 因 `return unless pending?` 直接返回
3. 窗口**没有**被扩展，新变更的日期范围丢失
4. SyncJob 继续按原状态执行（实际是全量）
5. finalize 后没有补跑机制

**好消息**：由于窗口参数实际上没有被使用，syncing 状态下的窗口丢失**不会导致最终数据不一致**（因为总是全量计算）。

**坏消息**：这是一个"双重错误恰好抵消"的架构巧合，不是设计上的正确性保证。

---

### ✅ 第三档：条件补跑（sync 完成后有新变更）

**触发条件**：上一个 sync 已经 `completed` / `failed`，新变更到达

**判定依据**：
1. `incomplete.first` 返回 nil（没有 pending 或 syncing）
2. 创建全新的 sync 记录
3. `SyncJob.perform_later(sync)` 调度新任务
4. 新 sync 会全量计算所有数据

---

## 架构问题总结

### 问题 1：静默的窗口丢弃

**现象**：syncing 状态下调用 `expand_window_if_needed` 会静默 return，没有任何日志或错误。

**风险等级**：🟡 中（当前被全量计算掩盖）

**代码证据**：
```ruby
def expand_window_if_needed(new_window_start_date, new_window_end_date)
  return unless pending?  # 没有日志，直接返回
  # ...
end
```

### 问题 2：窗口参数是"死代码"

**现象**：`window_start_date` 和 `window_end_date` 只在创建和扩展时写入数据库，**从未在计算时读取**。

**风险等级**：🟡 中（技术债务，未来优化窗口计算时会踩坑）

**代码证据**：
```ruby
# ForwardCalculator 完全忽略窗口
def calc_start_date
  account.opening_anchor_date  # 总是全量
end
```

### 问题 3：注释与实现不一致

**现象**：注释写的是"增量窗口优化"，但代码实现是"全量计算"。

```ruby
# If a sync is pending, we can adjust the window if new syncs are created with a wider window.
def expand_window_if_needed(...)
```

**风险等级**：🟡 低（误导维护者）

---

## 建议的修复方向

### 短期方案（低风险）

在 `expand_window_if_needed` 中增加 syncing 状态的日志：

```ruby
def expand_window_if_needed(new_window_start_date, new_window_end_date)
  if syncing?
    Rails.logger.warn("Sync #{id} is syncing, cannot expand window. New window [#{new_window_start_date}, #{new_window_end_date}] will be covered by next full sync.")
    return
  end
  
  return unless pending?
  # ... 原有逻辑
end
```

### 中长期方案

1. **删除窗口参数**：既然总是全量计算，就删除所有窗口相关代码，简化复杂度
2. **实现真正增量计算**：让 Calculator 真正尊重 `window_start_date`，只计算增量部分
3. **syncing 后补跑机制**：记录 syncing 期间到达的变更，完成后自动触发新 sync

---

## 附录：状态转移完整路径

```
                    sync_later 被调用
                        │
                        ▼
              查找 incomplete (pending+syncing)
                        │
           ┌────────────┴────────────┐
           │                         │
     找到 sync                  没有找到 sync
           │                         │
           ▼                         ▼
    expand_window_if_needed     创建新 sync
           │                   SyncJob.perform_later
           ▼
    ┌─────┴─────┐
    │           │
  pending     syncing
    │           │
    ▼           ▼
  扩展窗口    静默丢弃
    │
    ▼
  SyncJob 执行（全量计算）
    │
    ▼
  finalize → completed
           (无后续补跑)
```

---

*分析日期：2025-07-24*
*代码版本：基于 schema version 2025_07_24_115507*