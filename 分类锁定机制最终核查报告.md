# 分类锁定机制最终核查报告

## 一、执行路径总览

### 1.1 参数传递完整链路

```
入口点
  ↓ (参数: ignore_attribute_locks = true/false)
RulesController#apply / Family::Syncer#perform_sync
  ↓ (透传参数)
Rule#apply_later(ignore_attribute_locks: X)
  ↓ (序列化到 Sidekiq)
RuleJob#perform(rule, ignore_attribute_locks: X)
  ↓ (透传参数)
Rule#apply(ignore_attribute_locks: X)
  ↓ (透传给每个 Action)
Rule::Action#apply(matching_resources_scope, ignore_attribute_locks: X)
  ↓ (透传参数)
SetTransactionCategory#execute(transaction_scope, value, ignore_attribute_locks: X)
  ├─ 分支1：SQL 层过滤
  │    unless ignore_attribute_locks
  │      scope = scope.enrichable(:category_id)  ← ❌ 当 =true 时跳过此过滤
  └─ 分支2：应用层二次校验 (enrich_attribute)
       ↓
       enrich_attributes
         locked?(attr_key)  ← ✅ 无论参数如何，此处始终执行校验
         self[attr_key.to_s] == attr_value  ← ✅ 值相同判断始终执行
```

---

## 二、逐层代码验证与分支分析

### 2.1 入口层：参数来源

#### 2.1.1 RulesController 入口

**文件位置**：`app/controllers/rules_controller.rb:34-38`

```ruby
def apply
  @rule.update!(active: true)
  @rule.apply_later(ignore_attribute_locks: true)  # ✅ 强制 = true
  redirect_back_or_to rules_path, notice: "#{@rule.resource_type.humanize} rule activated"
end
```

**关键结论**：用户手动激活规则时，**强制传入 `ignore_attribute_locks = true`**

#### 2.1.2 Family::Syncer 同步入口

**文件位置**：`app/models/family/syncer.rb:8-15`

```ruby
def perform_sync(sync)
  family.sync_trial_status!

  Rails.logger.info("Applying rules for family #{family.id}")
  family.rules.each do |rule|
    rule.apply_later  # ✅ 使用默认值 false
  end
  # ...
end
```

**关键结论**：同步后自动应用规则时，**使用默认值 `ignore_attribute_locks = false`**

### 2.2 Job 透传层

**文件位置**：`app/jobs/rule_job.rb:4-6`

```ruby
def perform(rule, ignore_attribute_locks: false)
  rule.apply(ignore_attribute_locks: ignore_attribute_locks)
end
```

**行为**：纯透传，不修改参数值

### 2.3 Rule 核心层

**文件位置**：`app/models/rule.rb:42-46`

```ruby
def apply(ignore_attribute_locks: false)
  actions.each do |action|
    action.apply(matching_resources_scope, ignore_attribute_locks: ignore_attribute_locks)
  end
end
```

**行为**：遍历所有 Action，纯透传参数

### 2.4 SetTransactionCategory 执行器：双重分支点

**文件位置**：`app/models/rule/action_executor/set_transaction_category.rb:10-26`

```ruby
def execute(transaction_scope, value: nil, ignore_attribute_locks: false)
  category = family.categories.find_by_id(value)

  scope = transaction_scope

  # ╔══════════════════════════════════════════════════════════════╗
  # ║ 分支点 1：SQL WHERE 层过滤（仅当 ignore_attribute_locks = false 时执行）
  # ║ 跳过条件：ignore_attribute_locks = true
  # ╚══════════════════════════════════════════════════════════════╝
  unless ignore_attribute_locks
    scope = scope.enrichable(:category_id)
  end

  # ╔══════════════════════════════════════════════════════════════╗
  # ║ 分支点 2：遍历后的应用层二次校验（无论参数如何，内部都有校验）
  # ║ 校验逻辑由 enrich_attribute 内部实现，不受此参数影响
  # ╚══════════════════════════════════════════════════════════════╝
  scope.each do |txn|
    txn.enrich_attribute(
      :category_id,
      category.id,
      source: "rule"
    )
  end
end
```

### 2.5 enrichable Scope：SQL 层过滤实现

**文件位置**：`app/models/concerns/enrichable.rb:18-22`

```ruby
included do
  scope :enrichable, ->(attrs) {
    attrs = Array(attrs).map(&:to_s)
    json_condition = attrs.each_with_object({}) { |attr, hash| hash[attr] = true }
    where.not(Arel.sql("#{table_name}.locked_attributes ?| array[:keys]"), keys: attrs)
  }
end
```

**生成的 SQL 伪代码**：
```sql
WHERE NOT (locked_attributes ?| ARRAY['category_id'])
-- 等价于：排除 locked_attributes 中包含 'category_id' 键的记录
```

**关键结论**：
- 当 `ignore_attribute_locks = false` 时 → **已锁定交易被 SQL 层直接排除，不会进入 each 循环**
- 当 `ignore_attribute_locks = true` 时 → **跳过此过滤，已锁定交易会进入 each 循环**

### 2.6 enrich_attribute：应用层双校验

**文件位置**：`app/models/concerns/enrichable.rb:34-51`

```ruby
def enrich_attributes(attrs, source:, metadata: {})
  # ╔══════════════════════════════════════════════════════════════╗
  # ║ 双重校验逻辑（无论 ignore_attribute_locks 参数如何，始终执行）
  # ║ reject 条件：三者满足其一就跳过更新
  # ║  1. locked?(attr_key)          → 🔒 属性已锁定？
  # ║  2. 属于忽略列表（id, timestamps）→ ❌ 系统保护字段
  # ║  3. self[attr_key] == attr_value → 🔄 值未变化？
  # ╚══════════════════════════════════════════════════════════════╝
  enrichable_attrs = Array(attrs).reject do |attr_key, attr_value|
    locked?(attr_key) || 
    ignored_enrichable_attributes.include?(attr_key) || 
    self[attr_key.to_s] == attr_value
  end

  # 只有通过全部校验的属性才会被更新
  ActiveRecord::Base.transaction do
    enrichable_attrs.each do |attr, value|
      self.send("#{attr}=", value)
      # ... 记录变更日志
    end
    save
  end
end
```

**locked? 校验实现**（第 53-55 行）：
```ruby
def locked?(attr)
  locked_attributes[attr.to_s].present?  # 检查 JSONB 中是否存在该键
end
```

---

## 三、ignore_attribute_locks 参数影响对照表

| 校验层级 | 校验内容 | `ignore_attribute_locks = false` | `ignore_attribute_locks = true` |
|----------|----------|----------------------------------|---------------------------------|
| **SQL 层** | `enrichable(:category_id)` Scope | ✅ 生效（排除锁定交易） | ❌ 跳过（全部交易进入循环） |
| **应用层校验 1** | `locked?(attr_key)` | ✅ 生效 | ✅ **仍然生效**（关键发现！） |
| **应用层校验 2** | 值相同判断 `self[...] == value` | ✅ 生效 | ✅ 始终生效 |
| **应用层校验 3** | 忽略字段列表（id/timestamps） | ✅ 生效 | ✅ 始终生效 |

### 🔴 关键发现

`ignore_attribute_locks = true` **仅跳过 SQL 层的预过滤，但不会跳过应用层的 `locked?` 校验！**

**原因**：参数只影响 `execute` 方法中是否调用 `scope.enrichable(:category_id)`，而 `enrich_attribute` 内部的 `locked?` 检查是独立的逻辑，不依赖该参数。

---

## 四、三类交易场景状态变化详解

### 4.1 场景 1：已锁定交易（locked_attributes 包含 "category_id"）

**前置状态**：
```ruby
txn.locked_attributes = { "category_id" => "2026-05-15T10:30:00Z" }
txn.category_id = 100  # 原始分类："餐饮"
```

**规则目标分类**：`200`（"交通"）

#### 当 ignore_attribute_locks = false（同步自动执行场景）

| 阶段 | 行为 | 结果 |
|------|------|------|
| SQL 层过滤 | `scope.enrichable(:category_id)` 生效 | ✅ 该交易被 WHERE 条件排除 |
| each 循环 | 未进入 | ❌ 不执行 enrich_attribute |
| **最终状态** | category_id 不变 | `100` → `100` |

#### 当 ignore_attribute_locks = true（用户手动激活场景）

| 阶段 | 行为 | 结果 |
|------|------|------|
| SQL 层过滤 | 跳过 enrichable 过滤 | ✅ 该交易进入 each 循环 |
| enrich_attribute | 调用 `txn.enrich_attribute(:category_id, 200, source: "rule")` | |
| → locked? 校验 | `locked?("category_id")` → **true** | ✅ 触发 reject 条件 |
| → 值更新 | 被 reject 过滤，**不执行更新** | |
| **最终状态** | category_id 不变 | `100` → `100` |

**🔴 关键结论**：即使 `ignore_attribute_locks = true`，**已锁定交易的 category_id 也不会被覆盖！**

---

### 4.2 场景 2：未锁定但值相同交易

**前置状态**：
```ruby
txn.locked_attributes = {}  # 未锁定
txn.category_id = 200       # 已等于规则目标分类
```

**规则目标分类**：`200`（与当前值相同）

#### 当 ignore_attribute_locks = false

| 阶段 | 行为 | 结果 |
|------|------|------|
| SQL 层过滤 | enrichable 检查通过（未锁定） | ✅ 进入 each 循环 |
| enrich_attribute | 调用方法 | |
| → locked? 校验 | false（通过） | |
| → 值相同判断 | `200 == 200` → **true** | ✅ 触发 reject 条件 |
| → 值更新 | 被 reject 过滤，不执行更新 | |
| **最终状态** | category_id 不变 | `200` → `200` |

#### 当 ignore_attribute_locks = true

结果与上述完全相同 → `category_id` 保持 `200` 不变

**🔵 关键结论**：值相同时无论参数如何都不会更新，避免无意义的写操作

---

### 4.3 场景 3：未锁定且值不同交易

**前置状态**：
```ruby
txn.locked_attributes = {}  # 未锁定
txn.category_id = 100       # 当前分类与规则目标不同
```

**规则目标分类**：`200`

#### 当 ignore_attribute_locks = false

| 阶段 | 行为 | 结果 |
|------|------|------|
| SQL 层过滤 | enrichable 检查通过 | ✅ 进入 each 循环 |
| enrich_attribute | 调用方法 | |
| → locked? 校验 | false（通过） | |
| → 值相同判断 | `100 == 200` → false（通过） | |
| → 忽略字段检查 | category_id 不在列表中（通过） | |
| → 更新执行 | 全部校验通过，执行更新 | |
| → DataEnrichment 日志 | 创建/更新记录（source: "rule"） | |
| **最终状态** | category_id 更新 | `100` → `200` |

#### 当 ignore_attribute_locks = true

结果与上述完全相同 → `category_id` 更新为 `200`

**🟢 关键结论**：未锁定且值不同时，两种参数模式下都会更新分类

---

## 五、三类场景结果汇总表

| 场景类型 | `ignore_attribute_locks = false` 结果 | `ignore_attribute_locks = true` 结果 | 最终 category_id 是否相同？ |
|----------|---------------------------------------|--------------------------------------|----------------------------|
| **🔒 已锁定交易** | 不更新（SQL 层排除） | 不更新（应用层 locked? 校验阻止） | ✅ 完全相同 |
| **🔄 未锁定但值相同** | 不更新（应用层值相同判断阻止） | 不更新（应用层值相同判断阻止） | ✅ 完全相同 |
| **✅ 未锁定且值不同** | 更新（全部校验通过） | 更新（全部校验通过） | ✅ 完全相同 |

### ⚠️ 终极结论

**`ignore_attribute_locks` 参数仅影响性能，不影响最终业务结果！**

- `= false`：SQL 层预过滤掉锁定交易 → 进入循环的记录更少 → 性能更好
- `= true`：SQL 层不过滤 → 更多记录进入循环但被应用层校验拒绝 → 多消耗一些 CPU 但结果一致

**设计意图推测**：
- 同步自动执行（= false）：追求性能，提前过滤
- 用户手动激活（= true）：追求"确定性"反馈，让用户感觉规则"真的生效了"，即使结果一样

---

## 六、参数分支完整对照决策树

```
SetTransactionCategory#execute 调用
  │
  ├─ ignore_attribute_locks = ?
  │
  ├─ [= false]
  │   │
  │   └─ scope.enrichable(:category_id) → SQL 排除锁定交易
  │       │
  │       └─ 遍历未锁定交易集合
  │           │
  │           ├─ 场景 A：值 == 当前值 → enrich_attribute reject（值相同）→ ❌ 不更新
  │           └─ 场景 B：值 != 当前值 → enrich_attribute 通过全部校验 → ✅ 更新
  │
  └─ [= true]
      │
      └─ 跳过 enrichable → 全部匹配交易进入循环
          │
          ├─ 场景 A：已锁定交易 → enrich_attribute 检测 locked? → ❌ 不更新
          ├─ 场景 B：未锁定但值相同 → enrich_attribute 检测值相同 → ❌ 不更新
          └─ 场景 C：未锁定且值不同 → 全部校验通过 → ✅ 更新
```

---

## 七、关键代码行定位索引

| 功能点 | 文件路径 | 代码行 |
|--------|----------|--------|
| RulesController 入口参数 | `app/controllers/rules_controller.rb` | 36 |
| Family::Syncer 入口参数 | `app/models/family/syncer.rb` | 14 |
| RuleJob 透传 | `app/jobs/rule_job.rb` | 4-5 |
| Rule#apply 透传 | `app/models/rule.rb` | 42-44 |
| SetTransactionCategory 分支判断 | `app/models/rule/action_executor/set_transaction_category.rb` | 15-17 |
| enrichable Scope SQL 过滤 | `app/models/concerns/enrichable.rb` | 18-22 |
| enrich_attributes 双重校验 | `app/models/concerns/enrichable.rb` | 35-37 |
| locked? 方法实现 | `app/models/concerns/enrichable.rb` | 53-55 |
| Transaction 包含 Enrichable | `app/models/entryable.rb` | 11 |
