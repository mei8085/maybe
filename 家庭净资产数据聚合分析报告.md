# 家庭净资产数据聚合分析报告

## 一、数据流向总览

```
数据源层                  计算层                    聚合层                    展示层
┌──────────────┐      ┌──────────────┐        ┌──────────────┐        ┌──────────────┐
│ 账户余额     │      │ Balance      │        │ AccountTotals│        │ BalanceSheet │
│ (Account)    │─────▶│ Materializer │───────▶│              │───────▶│ 净资产视图   │
└──────────────┘      └──────────────┘        └──────────────┘        └──────────────┘
                           ▲
┌──────────────┐           │                   ┌──────────────┐
│ 持仓估值     │───────────┘                   │ NetWorth     │
│ (Holding)    │                               │ SeriesBuilder│───────▶│ 历史趋势     │
│              │───────▶ Holding               └──────────────┘        │ 图表系列     │
│              │        Materializer                                  └──────────────┘
└──────────────┘
                           ▲
┌──────────────┐           │
│ 跨币种汇率   │───────────┘
│ (Exchange    │
│  Rate)       │
└──────────────┘
```

## 二、核心模型协作机制

### 2.1 账户余额计算 (Balance)

**位置**: `app/models/balance/materializer.rb`

**计算策略**:
- **Forward策略**：从最早日期向前推演，适用于手动账户
- **Reverse策略**：从最新余额反向推演，适用于同步链接账户

**核心流程**:
```ruby
Balance::Materializer
  └── materialize_balances
      ├── materialize_holdings  # 先计算持仓
      ├── calculate_balances    # 计算余额系列
      │   └── ForwardCalculator / ReverseCalculator
      ├── persist_balances      # 批量upsert
      ├── purge_stale_balances  # 清理过期数据
      └── update_account_info   # 更新账户当前余额
```

**余额字段结构**:
| 字段 | 说明 |
|------|------|
| `start_balance` | 期初总余额 |
| `start_cash_balance` | 期初现金余额 |
| `start_non_cash_balance` | 期初非现金余额 |
| `cash_inflows/outflows` | 现金流出入 |
| `non_cash_inflows/outflows` | 非现金流出入 |
| `net_market_flows` | 市场波动净值 |
| `end_balance` | 期末总余额 |
| `flows_factor` | 流向系数：资产=1，负债=-1 |

### 2.2 持仓估值计算 (Holding)

**位置**: `app/models/holding/materializer.rb`

**核心流程**:
```ruby
Holding::Materializer
  └── materialize_holdings
      ├── calculate_holdings
      │   └── ForwardCalculator / ReverseCalculator
      ├── persist_holdings      # 按唯一键upsert
      └── purge_stale_holdings  # 清理无交易的持仓
```

**持仓唯一键**: `[account_id, security_id, date, currency]`

**估值计算**:
- 通过交易记录计算每日持仓数量 (`qty`)
- 结合证券历史价格 (`price`) 计算持仓金额
- 金额 = 数量 × 价格 (`amount = qty × price`)

### 2.3 汇率转换机制 (ExchangeRate)

**位置**: `lib/money.rb`

**核心方法**:
```ruby
Money#exchange_to(other_currency, date:, fallback_rate:)
  ├── 同源货币直接返回
  ├── 从ExchangeRate表查询历史汇率
  │   └── LATERAL查询：取<=date的最新汇率
  ├── 无汇率时使用fallback_rate
  └── 金额 × 汇率 = 转换后金额
```

**汇率数据源**:
- Provider::Synth - 第三方市场数据提供商
- 支持批量范围查询和单日查询
- 每日定时同步 + 账户同步时补全

## 三、后台Job触发时机

### 3.1 Job调度总览

| Job名称 | 触发时机 | 队列 | 说明 |
|---------|---------|------|------|
| `ImportMarketDataJob` | 每日22:00 UTC (周一至周五) | `scheduled` | 全量同步市场数据 |
| `SyncJob` | 数据变更时触发 | `high_priority` | 账户同步计算 |
| `SyncCleanerJob` | 每小时 | `scheduled` | 清理过期同步记录 |
| `SecurityHealthCheckJob` | 每日02:00 UTC | `scheduled` | 证券数据健康检查 |

### 3.2 同步触发链路

**账户同步触发点**:
```ruby
1. Account.create_and_sync  # 创建账户后立即同步
2. Account.sync_later        # 数据变更时延迟同步
3. Family.sync_later         # 家庭级同步触发子账户同步
   └── Family::Syncer#perform_sync
       ├── 应用规则
       └── 调度子账户同步
           ├── PlaidItem 同步
           └── Manual Account 同步
               └── Account::Syncer#perform_sync
                   ├── import_market_data   # 补全市场数据
                   └── materialize_balances # 计算余额系列
```

**同步去重机制**:
```ruby
Syncable#sync_later
  ├── 查询是否存在未完成同步
  │   ├── 存在：扩展时间窗口
  │   └── 不存在：创建新同步记录
  └── SyncJob.perform_later
```

### 3.3 市场数据同步时序

```
每日市场数据同步 (ImportMarketDataJob)
  │
  ├── 确定所有用户需要的汇率对
  ├── 确定所有用户需要的证券价格
  ├── 从Synth Provider批量获取
  └── upsert到ExchangeRate/SecurityPrice表
        │
        ▼
账户同步时 (Account::Syncer)
  │
  ├── 检查该账户需要的市场数据是否已存在
  │   ├── 存在：直接使用
  │   └── 不存在：单独补全
  └── 执行余额计算
```

## 四、计算缓存层次设计

### 4.1 缓存层级结构

```
层级4: 视图层缓存
└── BalanceSheet展示组件缓存

层级3: 聚合计算缓存
├── AccountTotals#query          # 账户余额转换结果
└── NetWorthSeriesBuilder        # 净资产时间序列

层级2: 实例变量缓存
├── @assets/@liabilities         # ClassificationGroup
├── BalanceSheet实例变量
└── Account#current_holdings     # 当前持仓查询

层级1: 数据库持久化 (准物化视图)
├── balances表                   # 每日余额系列
└── holdings表                   # 每日持仓系列
```

### 4.2 缓存键设计

**家庭级缓存键**:
```ruby
Family#build_cache_key(key, invalidate_on_data_updates:)
  ├── 组成部分：
  │   ├── family_id
  │   ├── 自定义key标识
  │   ├── latest_sync_completed_at  # 数据更新时失效
  │   └── accounts.maximum(:updated_at)
  └── 示例："family_123_balance_sheet_20240115_20240114"
```

**具体缓存应用**:

| 缓存点 | 缓存键 | 失效条件 |
|--------|--------|---------|
| 账户行数据 | `balance_sheet_account_rows` | 账户数据更新 |
| 净资产系列 | `balance_sheet_net_worth_series` | 账户数据更新 + 时间段 |

### 4.3 缓存失效策略

**主动失效**:
- 同步完成时更新 `latest_sync_completed_at`
- 账户更新时更新 `updated_at` 时间戳
- 通过缓存键包含这些时间戳实现自动失效

**被动清理**:
```ruby
# stale数据清理
Balance::Materializer#purge_stale_balances
  └── 删除早于账户起始日期的余额记录

Holding::Materializer#purge_stale_holdings
  └── 删除无对应交易记录的持仓
```

## 五、分组维度选择逻辑

### 5.1 一级分组：资产/负债分类

```ruby
BalanceSheet
  ├── assets  (ClassificationGroup)
  │   └── classification: "asset"
  └── liabilities (ClassificationGroup)
      └── classification: "liability"
```

**分类来源**: `Account#classification` 枚举值
- 资产账户：银行、投资、房产、车辆等
- 负债账户：信用卡、贷款等

### 5.2 二级分组：账户类型分组

```ruby
ClassificationGroup#account_groups
  ├── 按 accountable_type 分组
  │   ├── Depository (银行存款)
  │   ├── CreditCard (信用卡)
  │   ├── Investment (投资账户)
  │   ├── Crypto (加密货币)
  │   ├── Property (房产)
  │   ├── Vehicle (车辆)
  │   ├── OtherAsset (其他资产)
  │   ├── Loan (贷款)
  │   └── OtherLiability (其他负债)
  └── 按 Accountable::TYPES 定义的顺序手动排序
```

**分组排序逻辑**:
- 遵循 `Accountable::TYPES` 中定义的业务优先级顺序
- 使用数组索引进行排序，未定义类型排到最后

### 5.3 三级分组：账户明细

```ruby
AccountGroup#accounts
  └── AccountRow (包装类)
      ├── account: 原始账户对象
      ├── converted_balance: 转换后余额
      └── is_syncing: 同步状态标记
```

## 六、金额单位转换边界

### 6.1 货币转换边界矩阵

| 层级 | 转换时机 | 转换位置 | 汇率日期 | 说明 |
|------|---------|---------|---------|------|
| 账户层 | 余额计算时 | `Balance::SyncCache` | 记录日期 | 账户本位币转换 |
| 持仓层 | 持仓计算时 | `Holding::Materializer` | 持仓日期 | 证券价格货币转换 |
| 条目层 | 条目查询时 | `Balance::SyncCache` | 条目日期 | 交易/估值货币转换 |
| 聚合层 | 汇总查询时 | SQL LATERAL JOIN | 图表日期点 | 跨账户币种统一 |
| 展示层 | 构建系列时 | `Money#exchange_to` | 查询日期 | 最终展示货币 |

### 6.2 SQL级聚合转换

**位置**: `app/models/balance/chart_series_builder.rb:93-149`

```sql
-- 核心转换逻辑
SELECT
  d.date,
  -- 余额 × 流向系数 × 汇率 × 符号修正
  COALESCE(SUM(
    last_bal.end_balance * 
    last_bal.flows_factor * 
    COALESCE(er.rate, 1) * 
    :sign_multiplier::integer
  ), 0) AS end_balance
FROM dates d
CROSS JOIN accounts
-- LATERAL子查询取最近余额
LEFT JOIN LATERAL (...) last_bal ON TRUE
-- LATERAL子查询取最近汇率
LEFT JOIN LATERAL (
  SELECT er.rate
  FROM exchange_rates er
  WHERE er.from_currency = accounts.currency
    AND er.to_currency = :target_currency
    AND er.date <= d.date
  ORDER BY er.date DESC
  LIMIT 1
) er ON TRUE
```

### 6.3 应用级转换 fallback

**位置**: `lib/money.rb:42-55`

```ruby
def exchange_to(other_currency, date: Date.current, fallback_rate: nil)
  return self if 同源货币
  
  exchange_rate = 
    store.find_or_fetch_rate(...)&.rate || 
    fallback_rate
  
  raise ConversionError unless exchange_rate
  
  Money.new(amount * exchange_rate, other_iso_code)
end
```

**FallBack策略**:
1. 优先使用数据库中已有的历史汇率
2. 无数据时调用Provider API实时获取
3. 仍无数据时使用传入的 `fallback_rate` (如1.0)
4. 完全无数据时抛出 `ConversionError`

### 6.4 流向系数边界

**flows_factor 语义**:
- 资产账户: `flows_factor = 1` → 余额增加 = 净资产增加
- 负债账户: `flows_factor = -1` → 余额减少 = 净资产增加

**sign_multiplier 修正**:
```ruby
# 负债账户单独查看时需要取反
# 因为聚合时已包含 flows_factor=-1
# 用户期望看到正数余额
def sign_multiplier
  favorable_direction == "down" ? -1 : 1
end
```

## 七、净资产聚合公式

### 7.1 单账户余额聚合

```
期末余额 = 期初余额 + 现金流入 - 现金流出 + 非现金流入 - 非现金流出 + 市场波动净值
```

### 7.2 家庭净资产聚合

```
净资产 = Σ(资产账户余额 × 汇率) - Σ(负债账户余额 × 汇率)

或等价于：
净资产 = Σ(账户余额 × 流向系数 × 汇率)
其中：
  流向系数 = 1 (资产) 或 -1 (负债)
```

### 7.3 时间序列插值

```
对于每个日期点 d：
  余额 = 该账户在 <= d 的最近一条余额记录
  汇率 = 该货币对在 <= d 的最近一条汇率记录
  转换后余额 = 余额 × 汇率
  家庭净资产 = Σ所有账户(转换后余额 × 流向系数)
```

## 八、关键文件索引

| 功能模块 | 文件路径 |
|---------|---------|
| 余额计算 | `app/models/balance/materializer.rb` |
| 持仓计算 | `app/models/holding/materializer.rb` |
| 汇率转换 | `lib/money.rb` |
| 账户同步 | `app/models/account/syncer.rb` |
| 家庭同步 | `app/models/family/syncer.rb` |
| 资产负债表 | `app/models/balance_sheet.rb` |
| 账户汇总 | `app/models/balance_sheet/account_totals.rb` |
| 图表系列 | `app/models/balance/chart_series_builder.rb` |
| 市场数据Job | `app/jobs/import_market_data_job.rb` |
| 调度配置 | `config/schedule.yml` |
| 货币Provider | `app/models/provider/synth.rb` |

---

**报告生成时间**: 2024-06-14
**分析版本**: v1.0
**代码基线**: 109-maybe分支
