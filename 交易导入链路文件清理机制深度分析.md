# 交易导入链路文件清理机制深度分析

## 1. 核心发现：CSV 文件不使用 ActiveStorage

**最重要的结论**：交易导入的 CSV 文件**完全没有使用 Rails ActiveStorage** 进行存储。文件内容以纯文本形式直接存储在数据库的 `imports` 表中。

### 1.1 CSV 内容的实际存储位置

CSV 文件内容存储在 `imports` 表的两个 `text` 类型字段中：

| 字段名 | 类型 | 实际使用情况 | 说明 |
|--------|------|-------------|------|
| `raw_file_str` | `text` | ✅ **实际使用** | 存储用户上传的原始 CSV 文本内容 |
| `normalized_csv_str` | `text` | ❌ **未使用** | 数据库中存在，但代码中无任何引用 |

**代码证据**：
- Schema 定义：[db/schema.rb#L353-L383](db/schema.rb#L353-L383)
- 上传控制器写入：[app/controllers/import/uploads_controller.rb#L19](app/controllers/import/uploads_controller.rb#L19)
- 模型读取解析：[app/models/import.rb#L250](app/models/import.rb#L250)

### 1.2 CSV 上传流程

上传流程在 [app/controllers/import/uploads_controller.rb](app/controllers/import/uploads_controller.rb#L16-L28) 中实现，支持两种输入方式：

```ruby
def csv_str
  @csv_str ||= upload_params[:csv_file]&.read || upload_params[:raw_file_str]
end
```

**流程详解**：

```
用户上传
   ├─ 方式1: 选择 CSV 文件
   │        → params[:import][:csv_file] (ActionDispatch::Http::UploadedFile)
   │        → 调用 .read 读取为字符串
   │        → 存入 imports.raw_file_str
   │
   └─ 方式2: 直接粘贴 CSV 文本
            → params[:import][:raw_file_str] (String)
            → 直接存入 imports.raw_file_str
```

验证通过后保存：
```ruby
@import.assign_attributes(raw_file_str: csv_str, col_sep: upload_params[:col_sep])
@import.save!(validate: false)
```

### 1.3 Import 模型的关联数据结构

Import 模型拥有以下关联数据，全部在数据库中存储：

```ruby
# [app/models/import.rb#L39-L42](app/models/import.rb#L39-L42)
has_many :rows, dependent: :destroy       # CSV 行数据缓存
has_many :mappings, dependent: :destroy    # 字段映射配置
has_many :accounts, dependent: :destroy    # 导入创建的账户
has_many :entries, dependent: :destroy     # 导入创建的账目条目
```

数据流向图：
```
Import (imports 表)
  ├── raw_file_str (CSV 原始文本)
  ├── Import::Row (import_rows 表) - 解析后的每行数据
  ├── Import::Mapping (import_mappings 表) - 分类/标签/账户映射
  │     ├─ Import::CategoryMapping
  │     ├─ Import::TagMapping
  │     ├─ Import::AccountMapping
  │     └─ Import::AccountTypeMapping
  ├── Account (accounts 表, import_id 外键) - 导入创建的账户
  └── Entry (entries 表, import_id 外键) - 导入创建的账目条目
        └─ Transaction / Trade / Valuation (entryable 多态关联)
```

---

## 2. 删除 vs 撤销：数据清理行为对比

### 2.1 删除操作 (Destroy)

**触发入口**：[app/controllers/imports_controller.rb#L55-L59](app/controllers/imports_controller.rb#L55-L59)

```ruby
def destroy
  @import.destroy
  redirect_to imports_path, notice: "Your import has been deleted."
end
```

**级联删除链**（由 `dependent: :destroy` 驱动）：

| 数据 | 表 | 是否删除 | 机制 |
|------|-----|---------|------|
| Import 本身 | `imports` | ✅ 删除 | 直接 destroy |
| CSV 原始文本 | `imports.raw_file_str` | ✅ 删除 | 随记录删除 |
| Import::Row | `import_rows` | ✅ 删除 | `has_many :rows, dependent: :destroy` |
| Import::Mapping | `import_mappings` | ✅ 删除 | `has_many :mappings, dependent: :destroy` |
| 导入创建的 Account | `accounts` | ✅ 删除 | `has_many :accounts, dependent: :destroy` |
| 导入创建的 Entry | `entries` | ✅ 删除 | `has_many :entries, dependent: :destroy` |
| Entry 对应的 Transaction/Trade/Valuation | 各子表 | ✅ 删除 | Entry 的 `delegated_type ..., dependent: :destroy` |
| 映射创建的 Category/Tag | `categories`, `tags` | ❌ **保留** | 无直接级联，映射被删但实体保留 |

**代码证据**：
- Import 级联声明：[app/models/import.rb#L39-L42](app/models/import.rb#L39-L42)
- Entry 级联声明：[app/models/entry.rb#L10](app/models/entry.rb#L10)

### 2.2 撤销操作 (Revert)

**触发入口**：[app/controllers/imports_controller.rb#L41-L44](app/controllers/imports_controller.rb#L41-L44)

```ruby
def revert
  @import.revert_later
  redirect_to imports_path, notice: "Import is reverting in the background."
end
```

**Revert 实际执行逻辑**：[app/models/import.rb#L85-L96](app/models/import.rb#L85-L96)

```ruby
def revert
  Import.transaction do
    accounts.destroy_all   # 仅删除导入创建的账户
    entries.destroy_all    # 仅删除导入创建的账目条目
  end

  family.sync_later
  update! status: :pending  # 状态重置为 pending
end
```

**撤销操作的数据影响**：

| 数据 | 表 | 是否删除 | 说明 |
|------|-----|---------|------|
| Import 本身 | `imports` | ❌ **保留** | 记录保留，状态重置为 pending |
| CSV 原始文本 | `imports.raw_file_str` | ❌ **保留** | 字段保留完整内容 |
| Import::Row | `import_rows` | ❌ **保留** | 解析后的行数据继续保留 |
| Import::Mapping | `import_mappings` | ❌ **保留** | 映射配置完整保留 |
| 导入创建的 Account | `accounts` | ✅ 删除 | `accounts.destroy_all` |
| 导入创建的 Entry | `entries` | ✅ 删除 | `entries.destroy_all` |
| Entry 对应的 Transaction/Trade | 各子表 | ✅ 删除 | Entry 级联删除 |
| 状态字段 | `imports.status` | 🔄 修改 | `complete` → `pending` |

### 2.3 删除 vs 撤销：行为对比矩阵

| 操作 | Import 记录 | CSV 内容 | Rows | Mappings | 生成的 Accounts | 生成的 Entries | 状态 |
|------|------------|---------|------|----------|-----------------|-----------------|------|
| **Destroy** | ❌ 删除 | ❌ 删除 | ❌ 删除 | ❌ 删除 | ❌ 删除 | ❌ 删除 | (删除) |
| **Revert** | ✅ 保留 | ✅ 保留 | ✅ 保留 | ✅ 保留 | ❌ 删除 | ❌ 删除 | → pending |

---

## 3. Rails 通用存储功能的实际接入情况

### 3.1 ActiveStorage 使用清单

项目中只有 **4 个模型** 使用了 ActiveStorage 的 `has_one_attached`：

| 模型 | 附件声明 | 用途 | 接入状态 |
|------|---------|------|---------|
| [app/models/user.rb#L25-L28](app/models/user.rb#L25-L28) | `has_one_attached :profile_image` | 用户头像 | ✅ 正常接入 |
| [app/models/plaid_item.rb#L16](app/models/plaid_item.rb#L16) | `has_one_attached :logo` | 金融机构 Logo | ✅ 正常接入 |
| [app/models/family_export.rb#L4](app/models/family_export.rb#L4) | `has_one_attached :export_file` | 数据导出压缩包 | ✅ 正常接入 |
| [app/models/account.rb#L27](app/models/account.rb#L27) | `has_one_attached :logo` | 账户自定义 Logo | ✅ 正常接入 |

### 3.2 Import 链路未接入的 ActiveStorage 功能

**Import 模型完全没有使用 ActiveStorage**，以下功能全部缺失：

| Rails 提供的功能 | Import 是否使用 | 实际替代方案 |
|------------------|-----------------|-------------|
| `has_one_attached` / `has_many_attached` | ❌ 未使用 | CSV 内容直接存 `raw_file_str` 文本字段 |
| `active_storage_blobs` 表 | ❌ 未使用 | 无 blob 记录 |
| `active_storage_attachments` 表 | ❌ 未使用 | 无 attachment 关联 |
| 磁盘/S3 存储服务 | ❌ 未使用 | PostgreSQL `text` 类型存储 |
| Direct Upload 直传 | ❌ 未使用 | 文件通过表单提交，由 Rails 代理读取 |
| 图片变体 (Variant) | ❌ 未使用 | N/A（CSV 非图片） |
| `purge` / `purge_later` | ❌ 未使用 | 依赖 `dependent: :destroy` 级联删除 |
| `ActiveStorage::Blob.unattached` | ❌ 未使用 | 无孤儿 blob 检测机制 |

### 3.3 storage.yml 配置的实际使用情况

[config/storage.yml#L1-L25](config/storage.yml#L1-L25) 中配置了 4 个存储服务：

```yaml
local:       # 本地磁盘  → ✅ 开发/测试环境用于 User 头像、导出文件等
test:        # 测试磁盘  → ✅ 测试环境使用
amazon:      # AWS S3    → ⚠️ 配置存在，使用取决于 ACTIVE_STORAGE_SERVICE 环境变量
cloudflare:  # R2        → ⚠️ 配置存在，使用取决于 ACTIVE_STORAGE_SERVICE 环境变量
```

**注意**：这些存储服务仅服务于上述 4 个模型的附件，与 Import 的 CSV 文件完全无关。

---

## 4. 无人引用数据产生的场景分析

### 4.1 场景一：创建 Import 后中途放弃

**触发路径**：[app/controllers/imports_controller.rb#L22-L31](app/controllers/imports_controller.rb#L22-L31)

```ruby
def create
  import = Current.family.imports.create!(
    type: import_params[:type],
    account: account,
    date_format: Current.family.date_format,
  )
  redirect_to import_upload_path(import)
end
```

**数据残留**：
- `imports` 表新增一条记录（只有 `type`, `family_id`, `date_format` 等基础字段）
- `raw_file_str` 为 `nil`
- 无 rows、mappings
- **状态**：`pending`

**用户行为**：点击"新建导入"→ 创建记录跳转到上传页 → 用户直接关闭页面或导航离开

### 4.2 场景二：上传 CSV 但不完成配置

**触发路径**：用户完成 [app/controllers/import/uploads_controller.rb#L16-L28](app/controllers/import/uploads_controller.rb#L16-L28) 后未继续

**数据残留**：
- ✅ Import 记录完整
- ✅ `raw_file_str` 包含完整 CSV 内容
- ❌ 无 rows、mappings（尚未执行 `generate_rows_from_csv`）
- **状态**：`pending`

**用户行为**：上传 CSV 成功 → 跳转到 configuration 页面 → 用户离开

### 4.3 场景三：完成配置但不执行导入

**触发路径**：用户完成 [app/controllers/import/configurations_controller.rb#L9-L15](app/controllers/import/configurations_controller.rb#L9-L15)

```ruby
def update
  @import.update!(import_params)
  @import.generate_rows_from_csv  # 生成 rows
  @import.reload.sync_mappings     # 生成 mappings
  redirect_to import_clean_path(@import)
end
```

**数据残留**：
- ✅ Import 记录 + CSV 内容
- ✅ N 条 `import_rows` 记录
- ✅ N 条 `import_mappings` 记录（CategoryMapping / TagMapping / AccountMapping）
- **状态**：`pending`

**用户行为**：完成字段映射 → 进入清理/确认页面 → 用户不点击"确认导入"而离开

### 4.4 场景四：撤销 (Revert) 后不再处理

**触发路径**：用户对已完成的导入执行 revert

**数据残留**：
- ✅ Import 记录保留
- ✅ `raw_file_str` CSV 内容保留
- ✅ 所有 `import_rows` 保留
- ✅ 所有 `import_mappings` 保留
- ❌ 生成的 entries/accounts 已被删除
- **状态**：重置为 `pending`

**用户行为**：撤销后看到导入回到列表，状态为 pending，但用户不再重新导入或删除它

### 4.5 场景五：导入过程中异常失败（事务边界详解）

#### 4.5.1 整体调用链与事务层次

**触发入口**：[app/models/import.rb#L65-L75](app/models/import.rb#L65-L75)

```ruby
# 外层：publish 方法，无显式事务包裹
def publish
  raise MaxRowCountExceededError if row_count_exceeded?

  import!                  # ← 内层：各子类的 import! 有自己的 transaction

  family.sync_later
  update! status: :complete
rescue => error
  # 异常捕获在事务之外，此 update 会被持久化
  update! status: :failed, error: error.message
end
```

#### 4.5.2 内层事务：三个子类的 `import!` 方法

三个子类均使用 `transaction do` 包裹完整导入逻辑，实现全有或全无：

**TransactionImport**：[app/models/transaction_import.rb#L2-L33](app/models/transaction_import.rb#L2-L33)
```ruby
def import!
  transaction do                           # ← 开启事务
    mappings.each(&:create_mappable!)      #    创建 Category/Tag/Account
    # ... 构建 transactions ...
    Transaction.import!(transactions, recursive: true)  # 批量写入
  end                                      # ← 事务提交或整体回滚
end
```

**TradeImport**：[app/models/trade_import.rb#L2-L37](app/models/trade_import.rb#L2-L37)
```ruby
def import!
  transaction do
    mappings.each(&:create_mappable!)
    # ... find_or_create_security 构建 trades ...
    Trade.import!(trades, recursive: true)
  end
end
```

**AccountImport**：[app/models/account_import.rb#L4-L29](app/models/account_import.rb#L4-L29)
```ruby
def import!
  transaction do
    rows.each do |row|
      # ... 逐行创建 account 并设置 opening_balance ...
      account.save!
    end
  end
end
```

#### 4.5.3 `create_mappable!` 的具体行为

分类映射：[app/models/import/category_mapping.rb#L33-L38](app/models/import/category_mapping.rb#L33-L38)
```ruby
def create_mappable!
  return unless creatable?
  self.mappable = import.family.categories.find_or_create_by!(name: key)
  save!
end
```

标签映射：[app/models/import/tag_mapping.rb#L34-L39](app/models/import/tag_mapping.rb#L34-L39)
```ruby
def create_mappable!
  return unless creatable?
  self.mappable = import.family.tags.find_or_create_by!(name: key)
  save!
end
```

账户映射：[app/models/import/account_mapping.rb#L35-L47](app/models/import/account_mapping.rb#L35-L47)
```ruby
def create_mappable!
  return unless creatable?
  account = import.family.accounts.create_or_find_by!(name: key) do |new_account|
    new_account.balance = 0
    new_account.import = import          # ← 注意：此处设置了 import 外键
    new_account.currency = import.family.currency
    new_account.accountable = Depository.new
  end
  self.mappable = account
  save!
end
```

#### 4.5.4 事务边界示意图

```
publish 方法（无事务）
   │
   ├── 异常前检查（如 row_count_exceeded?）
   │      └─ 若抛异常 → rescue 捕获 → status: :failed（无数据残留）
   │
   └── import!（开启数据库事务）
          │
          ├── mappings.each(&:create_mappable!)
          │     ├── CategoryMapping#create_mappable! → INSERT categories
          │     ├── TagMapping#create_mappable!      → INSERT tags
          │     └── AccountMapping#create_mappable!   → INSERT accounts (import_id = self)
          │
          ├── 构建 Transaction/Trade/Entry 内存对象
          │
          ├── Transaction.import!(..., recursive: true)
          │     └── 批量 INSERT entries + transactions/trades
          │
          ├── ✅ 所有步骤成功 → 事务 COMMIT
          │     └── 全部数据持久化
          │
          └── ❌ 任何步骤抛异常 → 事务 ROLLBACK
                ├── categories 回滚（即便 find_or_create_by! 新建了分类）
                ├── tags 回滚
                ├── accounts 回滚
                ├── entries / transactions / trades 全部回滚
                └── publish 外层 rescue 捕获 → update! status: :failed（此操作在原事务外，会被持久化）
```

#### 4.5.5 失败场景下的数据残留总结

| 失败发生时机 | 分类/标签/账户资源 | 业务数据(Entry/Transaction) | 状态更新 |
|-------------|-------------------|--------------------------|---------|
| `import!` 之前（如行数超限检查） | ❌ 无残留 | ❌ 无残留 | ✅ `failed`（rescue 保存） |
| `create_mappable!` 执行中 | ❌ **整体回滚**，无残留 | ❌ 尚未创建 | ✅ `failed`（rescue 保存） |
| `Transaction.import!` 执行中 | ❌ **整体回滚**，无残留 | ❌ **整体回滚**，无残留 | ✅ `failed`（rescue 保存） |
| `family.sync_later` 调用异常 | ✅ 已提交，**保留** | ✅ 已提交，**保留** | ✅ `failed`（rescue 覆盖 complete） |
| `update! status: :complete` 异常 | ✅ 已提交，**保留** | ✅ 已提交，**保留** | ✅ `failed`（rescue 覆盖） |

**关键结论**：
- 只要异常发生在 `import!` 的事务内部（99% 的失败场景），**不会产生任何半成品数据**，分类、标签、账户、业务数据都会被完整回滚。
- 只有当异常发生在 `import!` 事务提交之后、`publish` 方法末尾的收尾阶段（概率极低），才会出现业务数据已提交但状态标记为 `failed` 的不一致情况。

### 4.6 场景六：映射创建的 Category/Tag 成为孤儿

`Import::CategoryMapping` 在导入时创建新分类：

这些 Category / Tag 记录：
- 直接属于 `family`（通过 `family_id` 关联）
- **不通过** `import_id` 与导入关联（注意：AccountMapping 创建的 Account 有 `import_id`，但 Category 和 Tag 没有）
- 当 Import 被删除或撤销时，**这些分类和标签不会被级联删除**
- 成为无人引用数据的典型案例

**对比**：
| 映射类型 | 创建的实体 | 是否有 import_id | Import 删除时是否级联删除 |
|---------|-----------|-----------------|------------------------|
| CategoryMapping | Category | ❌ 否 | ❌ 保留 |
| TagMapping | Tag | ❌ 否 | ❌ 保留 |
| AccountMapping | Account | ✅ 是（create_or_find_by! 时设置） | ✅ 删除（has_many :accounts, dependent: :destroy） |

### 4.7 各场景数据残留汇总

| 场景 | Import 记录 | raw_file_str | Rows | Mappings | 创建的 Category/Tag | 创建的 Entries/Accounts | 状态 |
|------|------------|-------------|------|----------|-------------------|----------------------|------|
| 创建后放弃 | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | pending |
| 上传后放弃 | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | pending |
| 配置后放弃 | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ | pending |
| Revert 后 | ✅ | ✅ | ✅ | ✅ | ⚠️ 保留 | ❌ | pending |
| Publish 事务内失败 | ✅ | ✅ | ✅ | ✅ | ❌ 回滚，无残留 | ❌ 回滚，无残留 | failed |
| Publish 事务后失败 | ✅ | ✅ | ✅ | ✅ | ✅ 已提交保留 | ✅ 已提交保留 | failed |
| 正常 Destroy | ❌ | ❌ | ❌ | ❌ | ⚠️ 保留 | ❌ | (删除) |

---

## 5. 完整生命周期数据流

### 5.1 导入完整流程

```
1. 新建导入 (POST /imports)
   └─ 写入 imports 表 (type, family_id, date_format)
   └─ 状态: pending

2. 上传 CSV (PATCH /imports/:id/upload)
   └─ 写入 imports.raw_file_str (CSV文本)
   └─ 状态: pending

3. 配置字段映射 (PATCH /imports/:id/configuration)
   └─ 写入 imports.*_col_label, date_format 等
   └─ 调用 generate_rows_from_csv → 批量写入 import_rows
   └─ 调用 sync_mappings → 写入/更新 import_mappings
   └─ 状态: pending

4. 清理数据 (GET /imports/:id/clean)
   └─ 页面展示 rows，用户可编辑修正
   └─ 用户编辑 PATCH /imports/:id/rows/:id → 更新 import_rows

5. 确认映射 (GET /imports/:id/confirm)
   └─ 页面展示 mappings 预览

6. 执行导入 (POST /imports/:id/publish)
   └─ 状态: importing
   └─ 开启事务 transaction do
         ├─ mappings.each(&:create_mappable!) → 创建 Category/Tag/Account
         ├─ rows.map(&build) → 构建 Transaction/Trade + Entry
         └─ Transaction.import!(..., recursive: true) → 批量写入 entries + transactions
   └─ 事务 COMMIT → 全部数据持久化
   └─ family.sync_later
   └─ 状态: complete
   └─ （若事务内任何异常 → ROLLBACK + rescue → 状态: failed）

7. 可选: 撤销 (POST /imports/:id/revert)
   └─ 状态: reverting
   └─ Import.transaction do
         accounts.destroy_all → 删除导入创建的账户
         entries.destroy_all → 删除导入创建的条目 (级联删 Transaction/Trade)
   └─ 状态: pending
```

### 5.2 删除流程

```
DELETE /imports/:id
  ↓
import.destroy
  ↓
  ├─ import_rows (dependent: :destroy) → 全部删除
  ├─ import_mappings (dependent: :destroy) → 全部删除
  ├─ accounts.where(import_id:) (dependent: :destroy)
  │     └─ 删除导入创建的账户及其所有关联
  ├─ entries.where(import_id:) (dependent: :destroy)
  │     └─ delegated_type dependent: :destroy
  │     └─ 删除 Transaction/Trade/Valuation
  └─ imports 记录本身删除
       └─ raw_file_str 文本随记录消失
```

**残留数据**（Delete 不会清理的）：
- `import_mappings.create_mappable!` 创建的 Category / Tag 记录
- 这些记录的 `family_id` 直接关联 Family，**无 `import_id` 外键**
- AccountMapping 创建的 Account 有 `import_id`，会被级联删除，Category/Tag 则不会

---

## 6. 清理机制的缺陷与优化建议

### 6.1 当前存在的问题

| 问题 | 影响 | 严重度 |
|------|-----|--------|
| 无定时任务清理 pending 状态的废弃 Import | 数据库积累垃圾数据 + CSV 文本占用存储空间 | 中 |
| Revert 保留完整 CSV 内容和配置数据 | 用户可能永远不再处理，数据永久残留 | 中 |
| Import 删除后，映射创建的 Category/Tag 成为孤儿 | 分类标签列表越来越乱，用户困惑 | 中 |
| Publish 事务后收尾阶段失败，业务数据已提交但状态为 failed | 数据状态不一致，需人工介入 | 低 |
| `normalized_csv_str` 字段完全无用 | 徒增 schema 复杂度 | 低 |

### 6.2 建议优化方案

**建议 1：添加定时清理任务**（推荐立即实施）

在 `config/schedule.yml` 中新增：
```yaml
cleanup_abandoned_imports:
  cron: "0 4 * * *"  # 每天凌晨 4 点
  class: "CleanupAbandonedImportsJob"
  queue: "scheduled"
  description: "Delete imports that have been pending for over 7 days"
```

任务实现：
```ruby
class CleanupAbandonedImportsJob < ApplicationJob
  def perform
    # 删除 7 天前就处于 pending/failed 状态且无人操作的导入
    Import.where(status: %w[pending failed])
          .where("updated_at < ?", 7.days.ago)
          .find_each(&:destroy)
  end
end
```

**建议 2：Publish 收尾阶段也纳入事务保护**

将 `publish` 方法改为：
```ruby
def publish
  raise MaxRowCountExceededError if row_count_exceeded?

  transaction do
    import!
    update! status: :complete
  end

  family.sync_later
rescue => error
  update! status: :failed, error: error.message
end
```

这样 `import!` 和状态更新都在同一个事务内，避免数据已提交但状态为 failed 的不一致。

**建议 3：Import 删除时清理映射创建的 Category/Tag 资源**

在 Import 模型中添加 `before_destroy` 回调：
```ruby
before_destroy :cleanup_created_resources

def cleanup_created_resources
  mappings.each do |mapping|
    next unless mapping.create_when_empty? && mapping.mappable
    resource = mapping.mappable
    # 仅当该资源无其他关联时才删除
    resource.destroy if resource.is_a?(Category) && resource.transactions.none?
    resource.destroy if resource.is_a?(Tag) && resource.taggings.none?
  end
end
```

**建议 4：Revert 时增加可选的完全清理参数**

```ruby
def revert(delete_import_data: false)
  Import.transaction do
    accounts.destroy_all
    entries.destroy_all

    if delete_import_data
      rows.destroy_all
      mappings.destroy_all
      update!(raw_file_str: nil, status: :pending)
    else
      update!(status: :pending)
    end
  end
end
```

---

## 7. 配置重置与单行编辑的清理逻辑详解

### 7.1 导入配置重置时的清理流程

用户在导入配置页面（Configuration）修改列映射后提交，会触发完整的行缓存与映射重建。

**触发入口**：[app/controllers/import/configurations_controller.rb#L9-L14](app/controllers/import/configurations_controller.rb#L9-L14)

```ruby
def update
  @import.update!(import_params)          # ① 更新列标签、日期格式等配置
  @import.generate_rows_from_csv          # ② 清除旧行缓存，重建新行
  @import.reload.sync_mappings            # ③ 清除旧映射，重建映射
  redirect_to import_clean_path(@import), notice: "Import configured successfully."
end
```

#### 7.1.1 `generate_rows_from_csv`：旧行缓存的全量清除重建

**实现位置**：[app/models/import.rb#L132-L154](app/models/import.rb#L132-L154)

```ruby
def generate_rows_from_csv
  rows.destroy_all                     # ← 第一步：删除当前 import 的所有旧行

  mapped_rows = csv_rows.map do |row|  # ← 第二步：重新解析 CSV，构建新行数据
    { account: row[account_col_label].to_s, date: row[date_col_label].to_s, ... }
  end

  rows.insert_all!(mapped_rows)        # ← 第三步：批量插入新行
end
```

**清理行为详解**：

| 步骤 | 操作 | SQL 等价 | 作用域 |
|------|------|---------|--------|
| 1 | `rows.destroy_all` | `DELETE FROM import_rows WHERE import_id = ?` | ✅ 严格限定在当前 import |
| 2 | `csv_rows.map` | 内存操作（解析 `raw_file_str`） | N/A |
| 3 | `rows.insert_all!` | `INSERT INTO import_rows (import_id, ...) VALUES (?, ...)` | ✅ 自动带上当前 import_id |

**MintImport 子类覆写**：[app/models/mint_import.rb#L4-L21](app/models/mint_import.rb#L4-L21) 也遵循完全相同的模式，先 `rows.destroy_all` 再 `rows.insert_all!`。

#### 7.1.2 `sync_mappings`：旧映射的清除与重建

**实现位置**：[app/models/import.rb#L156-L172](app/models/import.rb#L156-L172)

```ruby
def sync_mappings
  transaction do
    mapping_steps.each do |mapping_class|   # mapping_class = Import::CategoryMapping / TagMapping / ...
      mappables_by_key = mapping_class.mappables_by_key(self)

      # 第一步：基于当前 rows 数据，find_or_initialize 需要保留的映射
      updated_mappings = mappables_by_key.map do |key, mappable|
        mapping = mappings.find_or_initialize_by(
          key: key, import: self, type: mapping_class.name
        )
        mapping.mappable = mappable
        mapping.create_when_empty = key.present? && mappable.nil?
        mapping
      end

      updated_mappings.each { |m| m.save(validate: false) }   # 第二步：保存保留的映射

      # 第三步：删除不在保留列表中的旧映射
      mapping_class.where.not(id: updated_mappings.map(&:id)).destroy_all
    end
  end
end
```

**⚠️ 关键安全问题**：第三步 `mapping_class.where.not(id: ...).destroy_all` **没有加 `import_id` 过滤条件**。

以 `Import::CategoryMapping` 为例，实际执行的 SQL 是：
```sql
DELETE FROM import_mappings
WHERE type = 'Import::CategoryMapping'
  AND id NOT IN (1, 2, 3, ...)   -- 仅排除当前 import 刚保存的 mapping id
```

**问题分析**：
- 假设系统中存在 Import A（mapping id: 1,2,3）和 Import B（mapping id: 4,5,6）
- Import A 调用 `sync_mappings`，`updated_mappings` = [1,2,3]
- `where.not(id: [1,2,3]).destroy_all` → **会删除 id=4,5,6，即 Import B 的所有分类映射！**

**风险等级**：🔴 **高危**。每次用户完成配置提交、或在清理阶段编辑任意一行，都会触发 `sync_mappings`，可能删除其他导入记录的映射数据。

---

### 7.2 清理阶段单行数据修改的清理逻辑

在 Clean 页面，用户可以逐行编辑 CSV 解析后的行数据，修正错误的日期、金额、分类等。

**触发入口**：[app/controllers/import/rows_controller.rb#L4-L8](app/controllers/import/rows_controller.rb#L4-L8)

```ruby
def update
  @row.update_and_sync(row_params)    # ← 保存行修改并同步映射
  redirect_to import_row_path(@row.import, @row)
end
```

#### 7.2.1 `update_and_sync`：单行保存与全量映射同步

**实现位置**：[app/models/import/row.rb#L33-L37](app/models/import/row.rb#L33-L37)

```ruby
def update_and_sync(params)
  assign_attributes(params)     # ① 更新单行数据（category、tags、account 等字段）
  save!(validate: false)        # ② 保存单行（跳过校验，避免阻塞后续流程）
  import.sync_mappings          # ③ 对整个 import 重新执行 sync_mappings
end
```

**清理流程详解**：

```
用户编辑某行的 category 从 "Groceries" 改为 "Food"
   ↓
@row.update(category: "Food") → 更新 import_rows 表中该行记录
   ↓
save! → 单行持久化
   ↓
import.sync_mappings → 重新扫描所有 rows，重建 mappings
   ├── 对 CategoryMapping：
   │     扫描所有 rows 的 category 字段值
   │     "Food" 出现在 rows 中 → 保留/创建对应 mapping
   │     "Groceries" 不再出现在 rows 中 → 其 mapping 被销毁
   │     （⚠️ 同时可能误删其他 import 的映射）
   ├── 对 TagMapping：同理
   └── 对 AccountMapping：同理
```

**关键点**：每次单行编辑都会触发**全量**映射重建，而非增量更新。这保证了映射与行数据的一致性，但也意味着每次编辑都会执行 `mapping_class.where.not(id: ...).destroy_all`，从而面临 7.1.2 中所述的误删风险。

---

### 7.3 清理逻辑的作用域隔离与误删风险评估

#### 7.3.1 各清理操作的作用域对比

| 清理操作 | 代码位置 | 作用域限定方式 | 是否安全 |
|---------|---------|--------------|---------|
| `rows.destroy_all` | `generate_rows_from_csv` | 通过 `has_many :rows` 关联，自动带 `import_id = ?` | ✅ 安全 |
| `rows.insert_all!` | `generate_rows_from_csv` | 通过关联构建，自动填充 `import_id` | ✅ 安全 |
| `mappings.find_or_initialize_by(import: self, ...)` | `sync_mappings` 第一步 | 显式传入 `import: self` | ✅ 安全 |
| `mapping_class.where.not(id: ...).destroy_all` | `sync_mappings` 第三步 | ❌ **无 `import_id` 过滤** | 🔴 **高危，跨导入误删** |
| `accounts.destroy_all` | `revert` | 通过 `has_many :accounts` 关联 | ✅ 安全 |
| `entries.destroy_all` | `revert` | 通过 `has_many :entries` 关联 | ✅ 安全 |
| `mappings.each { |m| m.save }` | 用户确认页映射编辑 | 通过 `@import.mappings.find(params[:id])` 查找 | ✅ 安全 |

#### 7.3.2 误删场景复现

```
前置条件：
  Family 有两个导入同时进行：
    - Import #1（交易导入）：已配置完成，有 CategoryMapping(id=10, key="Food")、CategoryMapping(id=11, key="Transport")
    - Import #2（投资导入）：也已配置完成，有 CategoryMapping(id=20, key="Dividend")、CategoryMapping(id=21, key="Capital Gain")

时间线：
  T1: 用户在 Import #1 的 Clean 页面编辑某一行，将分类从 "Food" 改为 "Groceries"
  T2: 调用 @row.update_and_sync → 保存行数据
  T3: 调用 import.sync_mappings
       → mappables_by_key = { "Groceries" => nil, "Transport" => nil }
       → updated_mappings = [mapping(id=11, key="Transport"), mapping(id=22, key="Groceries")]
       → 保存这两个 mapping
       → 执行：Import::CategoryMapping.where.not(id: [11, 22]).destroy_all
       → ❗ SQL: DELETE FROM import_mappings
                 WHERE type = 'Import::CategoryMapping' AND id NOT IN (11, 22)
       → ❗ 删除了 id=10 (Import #1 的 "Food") 和 id=20、21 (Import #2 的全部映射)

结果：
  - Import #1 丢失了 "Food" 映射（这是预期行为）
  - Import #2 丢失了全部 CategoryMapping，用户再次打开时所有分类都变为未映射状态（严重 Bug）
```

#### 7.3.3 数据库层面的保护机制

查看 `import_mappings` 表的约束：[db/schema.rb#L185-L210](db/schema.rb#L185-L210)

| 约束 | 能否防止误删 |
|------|------------|
| `belongs_to :import` (存在性校验) | ❌ 不能。删除操作不触发关联校验 |
| `validates :key, uniqueness: { scope: [:import_id, :type] }` | ❌ 不能。仅约束创建和更新 |
| 外键 `import_id → imports.id` | ❌ 不能。只要 mapping 的 import_id 对应存在的 import 即可，不限制删除范围 |
| 数据库级触发器 | ❌ 无 |

**结论**：数据库层面没有任何机制能阻止 `sync_mappings` 误删其他导入的映射记录。

#### 7.3.4 修复建议

将 [app/models/import.rb#L169](app/models/import.rb#L169) 的危险代码：

```ruby
mapping_class.where.not(id: updated_mappings.map(&:id)).destroy_all
```

修改为以下任一安全版本：

**方案 A（推荐）**：通过关联限定作用域
```ruby
mappings.where(type: mapping_class.name)
        .where.not(id: updated_mappings.map(&:id))
        .destroy_all
```

**方案 B**：显式加 `import_id` 过滤
```ruby
mapping_class.where(import_id: id)
             .where.not(id: updated_mappings.map(&:id))
             .destroy_all
```

修正后执行的 SQL 将变为：
```sql
DELETE FROM import_mappings
WHERE import_id = 123           -- ← 新增的安全过滤
  AND type = 'Import::CategoryMapping'
  AND id NOT IN (11, 22)
```

这样只会删除当前 import 下不在保留列表中的映射，不会影响其他导入。

---

### 7.4 清理阶段其他相关操作的作用域验证

#### 7.4.1 用户在确认页修改映射

**触发入口**：[app/controllers/import/mappings_controller.rb#L4-L13](app/controllers/import/mappings_controller.rb#L4-L13)

```ruby
def update
  mapping = @import.mappings.find(params[:id])   # ← 通过 @import.mappings 关联查找

  mapping.update!(
    create_when_empty: create_when_empty,
    mappable: mappable,
    value: mapping_params[:value]
  )

  redirect_back_or_to import_confirm_path(@import)
end
```

**安全性**：✅ 通过 `@import.mappings.find(params[:id])` 查找，自动限定在当前 import 的映射范围内，无法跨导入操作。

#### 7.4.2 控制器层的 `set_import` 前置过滤器

所有 import 命名空间下的控制器都使用 `Current.family.imports.find(params[:import_id])` 来定位导入记录：

- [configurations_controller.rb#L18-L20](app/controllers/import/configurations_controller.rb#L18-L20)
- [cleans_controller.rb#L18-L21](app/controllers/import/cleans_controller.rb#L18-L21)
- [rows_controller.rb#L18-L21](app/controllers/import/rows_controller.rb#L18-L21)
- [mappings_controller.rb#L20-L22](app/controllers/import/mappings_controller.rb#L20-L22)

这保证了用户只能操作自己 Family 下的导入记录，提供了最外层的安全隔离。但这层保护**无法防御**模型内部 `sync_mappings` 的跨导入误删，因为误删操作发生在当前 import 已被正确加载之后，是模型内部方法逻辑缺陷导致的。

---

### 7.5 本章节小结

| 问题 | 结论 |
|------|-----|
| 配置重置时旧行缓存如何清除？ | `generate_rows_from_csv` 先 `rows.destroy_all` 再 `rows.insert_all!`，通过关联自动限定 `import_id`，✅ 安全。 |
| 配置重置时旧映射如何清除？ | `sync_mappings` 先找出要保留的 mapping 并保存，再 `where.not(id: ...).destroy_all` 删除其余。**此处缺少 `import_id` 过滤**，🔴 可能误删其他导入的映射。 |
| 单行编辑时的清理逻辑？ | `update_and_sync` 保存单行后调用 `import.sync_mappings` 全量重建映射。同上，🔴 存在误删风险。 |
| 数据库层面有无保护？ | 只有外键约束和唯一性校验，无法限制 DELETE 语句的作用域，❌ 无保护。 |
| 控制器层有无保护？ | 控制器通过 `@import.rows`、`@import.mappings` 关联查找记录，✅ 安全。但无法覆盖模型内部 `sync_mappings` 的全局删除操作。 |

---

## 8. 关键代码索引

### 7.1 控制器层
- [app/controllers/imports_controller.rb](app/controllers/imports_controller.rb) - 导入创建/删除/撤销入口
- [app/controllers/import/uploads_controller.rb](app/controllers/import/uploads_controller.rb) - CSV 上传处理
- [app/controllers/import/configurations_controller.rb](app/controllers/import/configurations_controller.rb) - 字段映射配置
- [app/controllers/import/confirms_controller.rb](app/controllers/import/confirms_controller.rb) - 确认预览

### 7.2 模型层
- [app/models/import.rb](app/models/import.rb) - 核心模型，包含 revert/publish 和关联声明
- [app/models/transaction_import.rb](app/models/transaction_import.rb) - 交易导入实现（含事务）
- [app/models/trade_import.rb](app/models/trade_import.rb) - 交易导入实现（含事务）
- [app/models/account_import.rb](app/models/account_import.rb) - 账户导入实现（含事务）
- [app/models/import/category_mapping.rb](app/models/import/category_mapping.rb) - 分类映射，`find_or_create_by!`
- [app/models/import/tag_mapping.rb](app/models/import/tag_mapping.rb) - 标签映射，`find_or_create_by!`
- [app/models/import/account_mapping.rb](app/models/import/account_mapping.rb) - 账户映射，`create_or_find_by!`（含 import_id）
- [app/models/import/row.rb](app/models/import/row.rb) - CSV 行数据模型
- [app/models/import/mapping.rb](app/models/import/mapping.rb) - 字段映射基类

### 7.3 任务层
- [app/jobs/import_job.rb](app/jobs/import_job.rb) - 异步执行导入
- [app/jobs/revert_import_job.rb](app/jobs/revert_import_job.rb) - 异步执行撤销

### 7.4 配置与 Schema
- [config/schedule.yml](config/schedule.yml) - 定时任务配置（当前无导入清理）
- [config/storage.yml](config/storage.yml) - ActiveStorage 配置（Import 不使用）
- [db/schema.rb imports 表](db/schema.rb#L353-L383) - imports 表结构

---

## 8. 总结

本项目的交易导入链路在文件存储和清理机制上有以下特点：

1. **存储方式非常规**：CSV 文件内容不使用 ActiveStorage，而以纯文本形式直接存入 `imports.raw_file_str` 字段，这是一个偏离 Rails 常规做法的设计选择。

2. **删除比撤销更彻底**：`destroy` 通过 `dependent: :destroy` 级联清理几乎所有从属数据，而 `revert` 只删除生成的业务数据，保留导入配置和原始 CSV。

3. **事务保护较完善**：三个子类的 `import!` 方法均使用 `transaction do` 包裹完整导入逻辑，`mappings.each(&:create_mappable!)` 与业务数据写入在同一事务内，**事务内失败不会产生任何半成品分类/标签/账户**。

4. **存在数据泄露点**：Import 删除或撤销时，通过映射创建的 Category/Tag 资源（无 `import_id` 外键）不会被级联清理，是最典型的"无人引用数据"来源。注意 AccountMapping 创建的 Account 有 `import_id`，会被正确级联删除。

5. **收尾阶段有隐患**：`publish` 方法本身无事务包裹，`import!` 提交后若 `family.sync_later` 或状态更新异常，会出现业务数据已提交但状态标记为 `failed` 的不一致（概率低）。

6. **缺少自动清理机制**：当前系统没有任何针对废弃导入（pending/failed 状态超过 N 天）的定时清理任务，依赖用户手动删除。

7. **ActiveStorage 能力闲置**：项目完整配置了本地磁盘、S3、Cloudflare R2 等多种存储服务，但这些能力完全没有参与到导入链路中。
